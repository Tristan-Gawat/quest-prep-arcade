// Pre-written lessons for C# Module: Pattern Matching

export const csharpPatternMatchingLessons = [
  {
    title: "What is Pattern Matching?",
    definition: "Pattern matching in C# is a feature that tests values against patterns to extract information, check types, compare properties, and make complex conditional logic more readable and concise.",
    explanation: `Pattern matching extends C#'s conditional capabilities beyond simple equality and boolean checks. It lets you test the shape, type, and content of data in a single expression, extracting relevant pieces as you go.\n\nThe simplest pattern is the type pattern: 'if (obj is string s)' tests whether obj is a string AND extracts it into variable s in one step. This replaces the old pattern of checking type then casting separately.\n\nProperty patterns let you test object properties inline: '{ Name: \"Admin\", Level: > 10 }' matches objects where Name is \"Admin\" AND Level exceeds 10. No need for separate if checks on each property.\n\nSwitch expressions with patterns are the most powerful form. They match a value against multiple patterns and return the result of the first match. The compiler can verify you've handled all possible cases (exhaustiveness checking).`,
    code: `// Type pattern\nobject data = "Hello, World!";\nif (data is string text)\n    Console.WriteLine(text.ToUpper()); // HELLO, WORLD!\n\nif (data is int number)\n    Console.WriteLine(number * 2); // won't execute\n\n// Property pattern\nvar player = new { Name = "Kai", Level = 25, HP = 0 };\nstring status = player switch\n{\n    { HP: 0 }                    => "Dead",\n    { Level: >= 20, HP: > 50 }   => "Elite warrior",\n    { Level: >= 20 }             => "Wounded elite",\n    { HP: > 0 }                  => "Active",\n    _                            => "Unknown"\n};\n\n// Relational and logical patterns\nint temp = 25;\nstring weather = temp switch\n{\n    < 0            => "Freezing",\n    >= 0 and < 15  => "Cold",\n    >= 15 and < 30 => "Pleasant",\n    >= 30          => "Hot"\n};\n\n// Switch with tuples\n(string role, int level) = ("mage", 30);\nint baseDmg = (role, level) switch\n{\n    ("warrior", >= 20) => 80,\n    ("mage", >= 20)    => 120,\n    ("rogue", >= 20)   => 100,\n    (_, >= 10)         => 50,\n    _                  => 20\n};`,
    breakdown: `\u2022 data is string text \u2014 Type pattern: checks type AND extracts value in one step. text is only available inside the if block.\n\n\u2022 { HP: 0 } \u2014 Property pattern matches when HP equals 0. No variable extraction needed, just a condition check.\n\n\u2022 { Level: >= 20, HP: > 50 } \u2014 Multiple property conditions combined. Both must match. More readable than separate && checks.\n\n\u2022 >= 0 and < 15 \u2014 Logical pattern combining two relational patterns. Reads naturally: 'between 0 and 15'.\n\n\u2022 (role, level) switch \u2014 Tuple pattern matching. Tests combinations of values simultaneously. Perfect for state machines.`,
    summary: "Pattern matching tests values against shapes, types, and conditions in one expression. Type patterns check and extract; property patterns test object state; relational patterns compare values."
  },
  {
    title: "How Pattern Matching works",
    definition: "Pattern matching compiles to efficient branching code. The compiler analyzes patterns for completeness, unreachable cases, and optimal evaluation order. Switch expressions with patterns often generate jump tables or decision trees.",
    explanation: `The C# compiler transforms pattern matching into efficient conditional code. Simple type patterns become 'isinst' IL instructions (fast type checks). Property patterns become field/property accesses with comparisons.\n\nFor switch expressions, the compiler builds a decision tree that minimizes the number of checks needed. It reorders patterns for efficiency while preserving the programmer's intended priority (top-to-bottom matching).\n\nExhaustiveness analysis verifies that all possible inputs are handled. For enums and sealed hierarchies, the compiler knows all possible types and warns if any are missing. The discard pattern (_) serves as a guaranteed catch-all.\n\nRelational and logical patterns (>, <, and, or, not) compile to standard comparison instructions. They're syntactic sugar that makes complex conditions more readable without any runtime overhead compared to equivalent if/else chains.`,
    code: `// How patterns compile\n// This switch expression:\nstring result = obj switch\n{\n    int n when n > 0  => "positive int",\n    int n             => "non-positive int",\n    string s          => $"string: {s}",\n    null              => "null",\n    _                 => "other"\n};\n\n// Compiles to something like:\n// if (obj is int n) {\n//     if (n > 0) return "positive int";\n//     return "non-positive int";\n// }\n// if (obj is string s) return $"string: {s}";\n// if (obj is null) return "null";\n// return "other";\n\n// Exhaustiveness checking\nenum Direction { North, South, East, West }\nstring Describe(Direction d) => d switch\n{\n    Direction.North => "Up",\n    Direction.South => "Down",\n    Direction.East  => "Right",\n    Direction.West  => "Left",\n    // Compiler warns if any enum value is missing!\n};\n\n// Nested property patterns (null-safe navigation)\nvar order = new { Customer = new { Address = new { City = "Tokyo" } } };\nif (order is { Customer: { Address: { City: var city } } })\n    Console.WriteLine($"Shipping to {city}");`,
    breakdown: `\u2022 Type patterns compile to 'isinst' \u2014 The IL instruction for type checking. Very fast, no boxing or allocation involved.\n\n\u2022 when n > 0 guard \u2014 Additional condition checked after the type pattern matches. Allows logic that can't be expressed as a pattern alone.\n\n\u2022 Enum exhaustiveness \u2014 The compiler knows all enum values and warns if any are missing from a switch. Prevents bugs when new enum values are added later.\n\n\u2022 Nested property patterns \u2014 { Customer: { Address: { City: var city } } } navigates objects safely. If any level is null, the pattern doesn't match (no exception).\n\n\u2022 Patterns are checked top-to-bottom \u2014 First matching pattern wins. Order matters for overlapping patterns.`,
    summary: "Patterns compile to efficient branching code with exhaustiveness checking. The compiler warns about unhandled cases and unreachable patterns. Patterns are evaluated top-to-bottom, first match wins."
  },
  {
    title: "Pattern Matching syntax & usage",
    definition: "Pattern matching syntax includes type patterns (is Type t), property patterns ({ Prop: value }), positional patterns, relational patterns (> 5), logical patterns (and, or, not), and list patterns ([first, .., last]).",
    explanation: `C# 7-11 introduced progressively more powerful patterns:\n\n- Type patterns: value is Type variable\n- Constant patterns: value is 42, value is null\n- Property patterns: value is { Prop: pattern }\n- Positional patterns: value is (x, y) for deconstructable types\n- Relational patterns: value is > 0 and < 100\n- Logical patterns: pattern and pattern, pattern or pattern, not pattern\n- List patterns (C# 11): value is [first, .., last]\n- var pattern: captures any value into a variable\n\nPatterns compose — you can nest any pattern inside another. A property pattern can contain relational patterns, which can be combined with logical patterns, creating arbitrarily complex matching expressions.\n\nThe 'when' guard adds extra conditions that can't be expressed as patterns alone. It's checked after the pattern matches, providing a last layer of filtering.`,
    code: `// List patterns (C# 11)\nint[] numbers = { 1, 2, 3, 4, 5 };\nstring desc = numbers switch\n{\n    []              => "empty",\n    [var single]    => $"just {single}",\n    [var f, .., var l] => $"from {f} to {l}",\n};\n\n// Positional patterns (with Deconstruct)\nrecord Point(int X, int Y);\nvar point = new Point(3, 4);\nstring quadrant = point switch\n{\n    (0, 0)       => "Origin",\n    (> 0, > 0)   => "Q1",\n    (< 0, > 0)   => "Q2",\n    (< 0, < 0)   => "Q3",\n    (> 0, < 0)   => "Q4",\n    _            => "On axis"\n};\n\n// Combined patterns with 'and', 'or', 'not'\nobject value = 42;\nif (value is int x and > 0 and < 100)\n    Console.WriteLine($"{x} is between 0 and 100");\n\nif (value is not null and not string)\n    Console.WriteLine("Non-null, non-string value");\n\n// var pattern captures anything\nif (value is var v && v.ToString()!.Length > 2)\n    Console.WriteLine($"{v} has long string form");`,
    breakdown: `\u2022 [] empty, [var single], [var f, .., var l] \u2014 List patterns match by length and content. The .. slice pattern matches any number of elements in between.\n\n\u2022 Positional pattern (> 0, > 0) \u2014 Works with records and types that have Deconstruct. Matches based on deconstructed component values.\n\n\u2022 is int x and > 0 and < 100 \u2014 Combines type check, value extraction, and range check in one expression. All conditions must be true.\n\n\u2022 is not null and not string \u2014 Negation patterns. 'not' inverts any pattern. Combine with 'and' for compound negative conditions.\n\n\u2022 var pattern \u2014 Matches anything and captures it. Useful when you want to name an intermediate value for further testing.`,
    summary: "List patterns match sequences by structure. Positional patterns work with Deconstruct. Logical combinators (and, or, not) compose patterns. var captures anything. Patterns nest arbitrarily."
  },
  {
    title: "Practical examples of Pattern Matching",
    definition: "Pattern matching simplifies complex conditional logic in games (state machines, collision handling), data processing (parsing, validation), and API responses (error handling, polymorphic deserialization).",
    explanation: `Game state machines map naturally to pattern matching. A character's state (Idle, Running, Jumping, Falling, Attacking) each responds differently to input events. Pattern matching on (state, input) tuples creates a clear, complete state transition table.\n\nAPI response handling benefits from pattern matching on response types. Match on status codes, error types, and response shapes to route handling logic clearly.\n\nData validation uses patterns to check multiple conditions simultaneously. A valid user registration might require matching on { Age: >= 18, Email: not null, Username.Length: >= 3 }.\n\nParsing and transformation pipelines use patterns to handle different input formats and convert them to uniform internal representations.`,
    code: `// === State Machine with Pattern Matching ===\nenum CharState { Idle, Running, Jumping, Falling, Attacking }\nenum Input { Jump, Attack, Move, Stop, Land }\n\nCharState Transition(CharState current, Input input) => (current, input) switch\n{\n    (CharState.Idle, Input.Jump)       => CharState.Jumping,\n    (CharState.Idle, Input.Move)       => CharState.Running,\n    (CharState.Idle, Input.Attack)     => CharState.Attacking,\n    (CharState.Running, Input.Jump)    => CharState.Jumping,\n    (CharState.Running, Input.Stop)    => CharState.Idle,\n    (CharState.Running, Input.Attack)  => CharState.Attacking,\n    (CharState.Jumping, Input.Land)    => CharState.Idle,\n    (CharState.Falling, Input.Land)    => CharState.Idle,\n    (CharState.Attacking, Input.Stop)  => CharState.Idle,\n    _                                  => current // stay in current state\n};\n\n// === Polymorphic handling without casting ===\nabstract record Shape;\nrecord Circle(double Radius) : Shape;\nrecord Rectangle(double W, double H) : Shape;\nrecord Triangle(double Base, double Height) : Shape;\n\ndouble Area(Shape shape) => shape switch\n{\n    Circle c    => Math.PI * c.Radius * c.Radius,\n    Rectangle r => r.W * r.H,\n    Triangle t  => 0.5 * t.Base * t.Height,\n    _           => throw new ArgumentException("Unknown shape")\n};`,
    breakdown: `\u2022 (current, input) switch \u2014 State machine as a pure function. Current state + input = next state. The entire transition table is visible at a glance.\n\n\u2022 _ => current \u2014 Default keeps current state for unhandled combinations. New states/inputs won't crash, they'll just be no-ops until handled.\n\n\u2022 abstract record Shape \u2014 Sealed hierarchy of shapes. Pattern matching replaces virtual method dispatch for operations that don't belong on the shape itself.\n\n\u2022 Circle c => Math.PI * c.Radius * c.Radius \u2014 Type pattern extracts the specific shape type and provides access to its properties for calculation.`,
    summary: "State machines expressed as tuple switches create clear transition tables. Polymorphic operations on sealed hierarchies use type patterns instead of virtual dispatch. Property patterns enable null-safe navigation."
  },
  {
    title: "Pattern Matching best practices",
    definition: "Best practices include using exhaustive switches for safety, ordering patterns from specific to general, preferring patterns over type casting chains, and using discard (_) for intentional catch-all cases.",
    explanation: `Prefer switch expressions over if/else chains when matching multiple patterns against the same value — they're more concise and the compiler checks exhaustiveness.\n\nOrder patterns from most specific to most general. The compiler matches top-to-bottom, so put narrow patterns first. A catch-all _ should always be last.\n\nUse property patterns to avoid null-checking chains. { Address: { City: var city } } safely navigates nested objects — if Address is null, the pattern simply doesn't match.\n\nAlways handle the default case explicitly (with _ or when the compiler proves exhaustiveness). Unhandled inputs at runtime throw MatchFailureException.\n\nDon't over-pattern. If a simple if statement is clearer, use it. Patterns shine with multiple branches, type hierarchies, and complex object shapes.`,
    code: `// DO: Use switch expressions for multi-case logic\nstring GetRank(int score) => score switch\n{\n    >= 1000 => "Legend",\n    >= 500  => "Master",\n    >= 100  => "Veteran",\n    _       => "Rookie"\n};\n\n// DO: Order from specific to general\nstring Classify(object obj) => obj switch\n{\n    int n when n > 100 => "big number",   // most specific\n    int n when n > 0   => "small number", // less specific\n    int _              => "non-positive", // all other ints\n    string s           => $"text: {s}",   // different type\n    _                  => "unknown"       // catch-all last\n};\n\n// DO: Use property patterns for null-safe access\nstring GetCity(Order? order) => order switch\n{\n    { Customer: { Address: { City: var c } } } => c,\n    _ => "Unknown"\n};\n\n// DON'T: Pattern match when simple if is clearer\n// if (x > 5) is simpler than: if (x is > 5)\n// Use patterns when you have multiple branches\n\n// DO: Handle all cases explicitly\n// The compiler will warn about unhandled enum values\n// Always include _ as a safety net for future additions`,
    breakdown: `\u2022 score switch { >= 1000 => ... } \u2014 Clean mapping from score ranges to rank strings. One expression, immediately understandable.\n\n\u2022 Most specific patterns first \u2014 'int n when n > 100' before 'int n when n > 0' because n=150 matches both, and first match wins.\n\n\u2022 Property patterns for null-safe navigation \u2014 If order or any nested property is null, the pattern simply doesn't match. No NullReferenceException.\n\n\u2022 Don't over-pattern \u2014 'if (x > 5)' is clearer than 'if (x is > 5)' for single conditions. Patterns shine with multiple branches.\n\n\u2022 Handle all cases \u2014 The _ discard ensures no runtime MatchFailureException. Compiler warnings catch missing enum cases.`,
    summary: "Order patterns specific-to-general. Use exhaustive switches for enums and sealed types. Don't over-pattern simple conditions. Always handle the default case. Property patterns replace null-checking chains."
  }
];
