// Pre-written spec lessons for: Game Loops & Architecture

export const gdGameLoopsLessons = [
  {
    title: "The Game Loop Pattern",
    definition: "The game loop is the central heartbeat of every video game — a continuous cycle that processes input, updates game state, and renders the frame, repeating dozens of times per second to create the illusion of interactive, real-time motion.",
    explanation: `Every interactive game, from Pong to modern AAA titles, relies on a game loop at its core. Unlike traditional applications that wait for user input before responding, games must continuously update their world regardless of whether the player is pressing buttons. Enemies move, physics simulate, animations play, and the screen redraws — all driven by this loop.\n\nThe classic game loop follows a three-phase structure: first, it processes player input (keyboard, mouse, gamepad); second, it updates the game world (moves objects, checks collisions, runs AI); third, it renders the current state to the screen. This cycle repeats as fast as the hardware allows, or is capped to a target frame rate like 60 FPS.\n\nIn web-based games using JavaScript, the browser provides requestAnimationFrame() as the mechanism for running a game loop. This API synchronizes your loop with the display's refresh rate (typically 60Hz), ensuring smooth rendering without wasted CPU cycles. The browser calls your function once per frame, and you perform all your update and draw logic within that callback.\n\nProfessional game engines abstract the loop away, but understanding it is critical for debugging performance issues, implementing custom timing logic, and knowing why your game might stutter or run differently on various hardware.`,
    code: `// A basic game loop using requestAnimationFrame\n\nclass Game {\n  private isRunning: boolean = false;\n  private lastTimestamp: number = 0;\n\n  start(): void {\n    this.isRunning = true;\n    this.lastTimestamp = performance.now();\n    requestAnimationFrame(this.loop.bind(this));\n  }\n\n  stop(): void {\n    this.isRunning = false;\n  }\n\n  private loop(timestamp: number): void {\n    if (!this.isRunning) return;\n\n    // Calculate time elapsed since last frame\n    const deltaTime = (timestamp - this.lastTimestamp) / 1000;\n    this.lastTimestamp = timestamp;\n\n    // The three core phases of the game loop\n    this.processInput();\n    this.update(deltaTime);\n    this.render();\n\n    // Schedule the next frame\n    requestAnimationFrame(this.loop.bind(this));\n  }\n\n  private processInput(): void {\n    // Read keyboard, mouse, gamepad state\n  }\n\n  private update(dt: number): void {\n    // Update positions, physics, AI, animations\n  }\n\n  private render(): void {\n    // Clear screen and draw all game objects\n  }\n}\n\nconst game = new Game();\ngame.start();`,
    breakdown: `• requestAnimationFrame — The browser API that drives the loop, calling your function roughly 60 times per second in sync with the monitor's refresh rate, pausing when the tab is hidden to save resources.\n\n• Three-phase structure — Input → Update → Render is the canonical order; processing input first ensures the game reacts to the latest player actions, update applies game logic, and render displays the final state.\n\n• Delta time calculation — By tracking the time between frames (timestamp - lastTimestamp), we know exactly how much real time has elapsed, which is essential for frame-rate independent movement.\n\n• Continuous execution — Unlike event-driven GUI apps, the game loop runs continuously; even if no input occurs, the game world keeps simulating (enemies move, particles decay, timers tick down).\n\n• Performance awareness — Each iteration of the loop must complete within ~16.67ms (at 60 FPS); if your update or render takes longer, you drop frames and the game appears to stutter.`,
    summary: "The game loop is the fundamental pattern that drives all real-time games, cycling through input processing, state updates, and rendering every frame. Mastering this pattern is the foundation for understanding frame timing, performance optimization, and game architecture."
  },
  {
    title: "Fixed vs Variable Timestep",
    definition: "Fixed timestep updates game logic at a constant rate (e.g., exactly 60 times per second) regardless of rendering speed, while variable timestep updates once per frame using the actual elapsed time, each approach offering different tradeoffs for consistency, determinism, and smoothness.",
    explanation: `One of the most important architectural decisions in game development is how you handle the passage of time. With a variable (or unfixed) timestep, your game updates once per frame and multiplies all movement by deltaTime — the actual time since the last frame. This is simple to implement but can cause physics instabilities and non-deterministic behavior when frame rates fluctuate.\n\nA fixed timestep, on the other hand, always advances the simulation by the same amount (e.g., 1/60th of a second). If the real elapsed time is larger than one tick, you run multiple simulation steps. If it's smaller, you wait. This guarantees that physics and game logic behave identically regardless of frame rate, which is critical for multiplayer games, replays, and precise physics simulations.\n\nThe gold standard approach combines both: a fixed timestep for game logic with an uncapped (or vsync'd) render rate. You accumulate real elapsed time into a bucket, and drain it in fixed-size simulation steps. Any leftover time is used to interpolate between the last two states for buttery-smooth rendering. This is the pattern used by most professional game engines.\n\nThe key insight is separating simulation rate from display rate. Your physics might tick at 50Hz (20ms per step) while your renderer draws at 144Hz on a high-refresh monitor. The interpolation step bridges the gap, preventing visual stuttering without compromising simulation stability.`,
    code: `// Fixed timestep with interpolation - the professional approach\n\nclass GameLoop {\n  private readonly TICK_RATE = 1 / 60; // 60 updates per second\n  private accumulator: number = 0;\n  private previousState: GameState;\n  private currentState: GameState;\n  private lastTime: number = 0;\n\n  loop(timestamp: number): void {\n    const frameTime = Math.min(\n      (timestamp - this.lastTime) / 1000,\n      0.25 // Cap to prevent spiral of death\n    );\n    this.lastTime = timestamp;\n\n    this.accumulator += frameTime;\n\n    // Process input once per frame\n    this.processInput();\n\n    // Run fixed simulation steps\n    while (this.accumulator >= this.TICK_RATE) {\n      this.previousState = this.currentState.clone();\n      this.update(this.TICK_RATE);\n      this.accumulator -= this.TICK_RATE;\n    }\n\n    // Calculate interpolation factor (0.0 to 1.0)\n    const alpha = this.accumulator / this.TICK_RATE;\n\n    // Render interpolated state for smooth visuals\n    this.render(this.previousState, this.currentState, alpha);\n\n    requestAnimationFrame(this.loop.bind(this));\n  }\n\n  private render(\n    prev: GameState,\n    curr: GameState,\n    alpha: number\n  ): void {\n    // Interpolate positions for smooth display\n    for (const entity of curr.entities) {\n      const prevEntity = prev.getEntity(entity.id);\n      const displayX = prevEntity.x * (1 - alpha) + entity.x * alpha;\n      const displayY = prevEntity.y * (1 - alpha) + entity.y * alpha;\n      this.drawEntity(entity, displayX, displayY);\n    }\n  }\n}`,
    breakdown: `• Fixed tick rate — By defining a constant TICK_RATE (e.g., 1/60 = 16.67ms), the simulation always advances in identical steps, ensuring deterministic behavior across different hardware.\n\n• Accumulator pattern — Real elapsed time is added to an accumulator; when enough time has built up for one or more simulation ticks, those ticks execute, draining the accumulator by TICK_RATE each time.\n\n• Spiral of death prevention — Capping frameTime (to 0.25s here) prevents a devastating feedback loop where slow frames cause more simulation steps, which cause even slower frames.\n\n• Interpolation (alpha blending) — The leftover time in the accumulator becomes a fraction (alpha) used to blend between the previous and current states, producing smooth visuals even when render rate differs from simulation rate.\n\n• Determinism guarantee — Because update() always receives the exact same dt value, the simulation produces identical results given the same inputs — essential for lockstep multiplayer, replays, and automated testing.`,
    summary: "Fixed timestep provides deterministic, stable simulation by updating at a constant rate, while interpolation ensures smooth rendering at any display refresh rate. This separation of simulation and rendering is the industry-standard approach used by professional game engines."
  },
  {
    title: "Delta Time & Frame Independence",
    definition: "Delta time (dt) is the elapsed time between the current frame and the previous frame, used to scale all time-dependent calculations so that game behavior remains consistent regardless of whether the game runs at 30, 60, or 144 frames per second.",
    explanation: `Without delta time, a game running at 60 FPS would appear to run twice as fast as one running at 30 FPS, because movement and animations would execute twice as many times per second. Delta time solves this by making all movement proportional to the actual time elapsed rather than the number of frames.\n\nThe concept is straightforward: instead of moving an object by a fixed amount each frame (e.g., x += 5), you move it by a rate multiplied by elapsed time (e.g., x += speed * dt). If dt is large (slow frame rate), the object moves further in that frame to compensate. If dt is small (fast frame rate), it moves a tiny amount. Over any given real-time period, the total distance traveled is the same.\n\nDelta time applies to virtually everything time-dependent: movement, animations, particle effects, cooldown timers, spawn intervals, camera transitions, and audio fading. Anything that should happen "over time" rather than "per frame" needs to be multiplied by dt.\n\nHowever, naive delta time usage has pitfalls. Very large dt values (from frame spikes) can cause objects to teleport through walls or physics to explode. Very small dt values can introduce floating-point precision issues. This is why professional games combine delta time with fixed timestep simulation, and why physics engines use substeps rather than one giant integration step.`,
    code: `// Frame-independent movement using delta time\n\nclass Player {\n  x: number = 0;\n  y: number = 0;\n  speed: number = 200; // pixels per SECOND, not per frame\n  jumpVelocity: number = 0;\n  gravity: number = 980; // pixels/sec^2\n\n  update(dt: number): void {\n    // Horizontal movement - consistent at any frame rate\n    if (Input.isKeyDown('ArrowRight')) {\n      this.x += this.speed * dt;\n    }\n    if (Input.isKeyDown('ArrowLeft')) {\n      this.x -= this.speed * dt;\n    }\n\n    // Gravity applies acceleration over time\n    this.jumpVelocity += this.gravity * dt;\n    this.y += this.jumpVelocity * dt;\n  }\n}\n\n// Timer that counts down in real seconds\nclass CooldownTimer {\n  private remaining: number = 0;\n\n  start(duration: number): void {\n    this.remaining = duration; // e.g., 2.5 seconds\n  }\n\n  update(dt: number): boolean {\n    if (this.remaining <= 0) return true; // already done\n    this.remaining -= dt;\n    return this.remaining <= 0; // returns true when expired\n  }\n\n  getProgress(): number {\n    return Math.max(0, this.remaining);\n  }\n}\n\n// Smooth interpolation (lerp) using delta time\nfunction smoothFollow(\n  current: number,\n  target: number,\n  smoothing: number,\n  dt: number\n): number {\n  // Frame-rate independent exponential smoothing\n  const factor = 1 - Math.pow(smoothing, dt);\n  return current + (target - current) * factor;\n}`,
    breakdown: `• Speed in units per second — By defining speed as 200 pixels per second (not per frame), multiplying by dt naturally produces the correct distance for any frame duration: at 60 FPS, dt ≈ 0.0167s, so movement is ~3.3px per frame.\n\n• Gravity as acceleration — Physics quantities like gravity (pixels/sec²) are integrated correctly by multiplying by dt: velocity changes by gravity * dt each frame, and position changes by velocity * dt.\n\n• Timer countdown — Real-time timers subtract dt from their remaining time, making cooldowns and spawn timers work correctly regardless of frame rate fluctuations.\n\n• Frame-rate independent smoothing — Naive lerp (current + (target - current) * 0.1) is frame-rate dependent; using Math.pow(smoothing, dt) produces consistent smoothing at any FPS.\n\n• Units matter — When using delta time, always think in "per second" units: speed is pixels/second, rotation is radians/second, fade rate is opacity/second. This makes code self-documenting and physically meaningful.`,
    summary: "Delta time ensures your game behaves consistently at any frame rate by scaling all time-dependent calculations by the actual elapsed time between frames. Always define rates in per-second units and multiply by dt to achieve frame independence."
  },
  {
    title: "Input Processing in the Loop",
    definition: "Input processing is the first phase of each game loop iteration, responsible for polling hardware state, translating raw device events into game actions, and buffering inputs so that game logic receives a clean, consistent snapshot of player intent.",
    explanation: `Input handling in games differs fundamentally from input in traditional applications. A web form waits for a submit event; a game needs to know every single frame whether the 'W' key is currently held down. This distinction between event-driven and polling-based input is central to game architecture.\n\nModern game input systems maintain two parallel structures: an event queue that captures discrete actions (key pressed, key released, mouse clicked) and a state table that tracks the current status of every button. The event queue handles one-shot actions like jumping or shooting, while the state table handles continuous actions like running or aiming. Both are updated at the start of each frame before game logic runs.\n\nThe reason input is processed first in the loop is causality: the player's actions this frame should immediately affect the game state computed in the update phase, and the results should be visible in the render phase of the same frame. Processing input after update would introduce one frame of latency, making controls feel sluggish.\n\nInput buffering is also critical for responsive game feel. If a player presses jump 2 frames before landing, a naive system would ignore it (they were airborne when they pressed). A buffered system remembers the press for a short window and triggers the jump upon landing, creating snappier controls that forgive imprecise timing.`,
    code: `// Comprehensive input system for a game loop\n\nclass InputManager {\n  // Current frame state\n  private keysDown: Set<string> = new Set();\n  private keysPressed: Set<string> = new Set(); // just this frame\n  private keysReleased: Set<string> = new Set(); // just this frame\n\n  // Previous frame state for edge detection\n  private previousKeys: Set<string> = new Set();\n\n  // Input buffer for forgiving timing\n  private inputBuffer: Array<{\n    action: string;\n    timestamp: number;\n  }> = [];\n  private readonly BUFFER_WINDOW = 0.1; // 100ms buffer\n\n  constructor() {\n    // Register raw browser events\n    window.addEventListener('keydown', (e) => {\n      this.keysDown.add(e.code);\n    });\n    window.addEventListener('keyup', (e) => {\n      this.keysDown.delete(e.code);\n    });\n  }\n\n  // Called at the START of each game loop iteration\n  update(currentTime: number): void {\n    // Detect edges: pressed this frame, released this frame\n    this.keysPressed.clear();\n    this.keysReleased.clear();\n\n    for (const key of this.keysDown) {\n      if (!this.previousKeys.has(key)) {\n        this.keysPressed.add(key);\n        this.inputBuffer.push({\n          action: key,\n          timestamp: currentTime\n        });\n      }\n    }\n    for (const key of this.previousKeys) {\n      if (!this.keysDown.has(key)) {\n        this.keysReleased.add(key);\n      }\n    }\n\n    // Store current state as previous for next frame\n    this.previousKeys = new Set(this.keysDown);\n\n    // Expire old buffer entries\n    this.inputBuffer = this.inputBuffer.filter(\n      (entry) => currentTime - entry.timestamp < this.BUFFER_WINDOW\n    );\n  }\n\n  // Continuous hold check (for movement)\n  isHeld(key: string): boolean {\n    return this.keysDown.has(key);\n  }\n\n  // One-shot press check (for jump, attack)\n  wasPressed(key: string): boolean {\n    return this.keysPressed.has(key);\n  }\n\n  // Buffered check - consumes the input\n  consumeBuffered(key: string): boolean {\n    const index = this.inputBuffer.findIndex(\n      (entry) => entry.action === key\n    );\n    if (index !== -1) {\n      this.inputBuffer.splice(index, 1);\n      return true;\n    }\n    return false;\n  }\n}`,
    breakdown: `• Polling vs events — Raw browser events (keydown/keyup) feed into a state set, which is then polled each frame; this hybrid approach gives you both instant event detection and per-frame state queries.\n\n• Edge detection — By comparing current keys against previous frame keys, we detect "just pressed" and "just released" transitions, essential for one-shot actions like jumping or firing that should trigger once per press.\n\n• Input buffering — Recent inputs are stored with timestamps; the game can check if a relevant input occurred within a window (100ms), enabling forgiving mechanics like coyote time or pre-jump buffering.\n\n• Consume pattern — consumeBuffered() removes the entry after reading it, preventing a single button press from triggering an action multiple times across frames.\n\n• Frame-start processing — Running input update at the start of the loop ensures all game logic in the same frame sees a consistent input snapshot, avoiding bugs where different systems see different input states.`,
    summary: "Input processing combines event capture with per-frame polling to provide both edge-triggered and continuous input queries. Buffering inputs with short timing windows creates responsive, forgiving controls that enhance game feel."
  },
  {
    title: "Rendering Pipeline Basics",
    definition: "The rendering pipeline is the final phase of the game loop, responsible for translating the current game state into visual output by clearing the previous frame, sorting and drawing game objects in the correct order, and presenting the final image to the display.",
    explanation: `Rendering is where your game becomes visible. After input has been processed and game state updated, the render phase reads the current state and draws everything to the screen. In 2D canvas-based games, this means clearing the canvas, drawing backgrounds, then sprites and effects in back-to-front order. In 3D games with WebGL, it involves setting up cameras, transforming geometry, applying shaders, and compositing multiple render passes.\n\nA critical principle is that rendering should be a pure read operation on game state — it should never modify positions, health values, or any gameplay data. This separation ensures that rendering can be skipped (for headless servers), run at a different rate than simulation, or be swapped out entirely (e.g., switching from Canvas2D to WebGL) without affecting game logic.\n\nFor 2D games, draw order determines visual layering. Objects are typically sorted by a z-index or layer system: backgrounds draw first, then terrain, then game entities, then particles, then UI. Within a layer, objects might be sorted by y-position (for top-down games creating depth illusion) or by a custom priority value.\n\nThe camera system transforms world coordinates to screen coordinates before drawing. If a player is at world position (5000, 3000), the camera calculates what portion of the world is visible and offsets all draw calls accordingly. This abstraction lets game logic work in consistent world units while the renderer handles viewport translation.`,
    code: `// 2D rendering pipeline with layers and camera\n\ninterface Renderable {\n  x: number;\n  y: number;\n  layer: number;\n  render(ctx: CanvasRenderingContext2D, camera: Camera): void;\n}\n\nclass Camera {\n  x: number = 0;\n  y: number = 0;\n  zoom: number = 1;\n  private width: number;\n  private height: number;\n\n  constructor(width: number, height: number) {\n    this.width = width;\n    this.height = height;\n  }\n\n  // Convert world position to screen position\n  worldToScreen(worldX: number, worldY: number): { x: number; y: number } {\n    return {\n      x: (worldX - this.x) * this.zoom + this.width / 2,\n      y: (worldY - this.y) * this.zoom + this.height / 2\n    };\n  }\n\n  // Check if a world position is visible on screen\n  isVisible(worldX: number, worldY: number, margin: number = 64): boolean {\n    const screen = this.worldToScreen(worldX, worldY);\n    return (\n      screen.x > -margin &&\n      screen.x < this.width + margin &&\n      screen.y > -margin &&\n      screen.y < this.height + margin\n    );\n  }\n}\n\nclass Renderer {\n  private ctx: CanvasRenderingContext2D;\n  private camera: Camera;\n  private renderables: Renderable[] = [];\n\n  render(): void {\n    // Step 1: Clear the entire canvas\n    this.ctx.clearRect(0, 0, this.ctx.canvas.width, this.ctx.canvas.height);\n\n    // Step 2: Sort by layer, then by y-position within layer\n    this.renderables.sort((a, b) => {\n      if (a.layer !== b.layer) return a.layer - b.layer;\n      return a.y - b.y;\n    });\n\n    // Step 3: Apply camera transform\n    this.ctx.save();\n    this.ctx.translate(\n      this.ctx.canvas.width / 2 - this.camera.x * this.camera.zoom,\n      this.ctx.canvas.height / 2 - this.camera.y * this.camera.zoom\n    );\n    this.ctx.scale(this.camera.zoom, this.camera.zoom);\n\n    // Step 4: Draw only visible objects (frustum culling)\n    for (const obj of this.renderables) {\n      if (this.camera.isVisible(obj.x, obj.y)) {\n        obj.render(this.ctx, this.camera);\n      }\n    }\n\n    // Step 5: Restore transform for UI (drawn in screen space)\n    this.ctx.restore();\n    this.renderUI();\n  }\n\n  private renderUI(): void {\n    // UI elements are drawn in screen coordinates, not world\n    // Health bars, score, minimap, etc.\n  }\n}`,
    breakdown: `• Clear-Sort-Draw cycle — Every frame starts fresh: clear the canvas, determine draw order, then render each object. This prevents ghost images and ensures correct layering.\n\n• Layer system — Objects are assigned numeric layers (0 = background, 1 = terrain, 2 = entities, 3 = effects, 4 = UI), providing coarse depth sorting before fine-grained y-sorting within each layer.\n\n• Camera transformation — Rather than moving every object, the camera applies a single canvas transform (translate + scale) that shifts the entire world view, making scrolling and zooming trivial.\n\n• Frustum culling — By checking isVisible() before drawing, we skip objects entirely off-screen, which can dramatically improve performance in large worlds with hundreds of entities.\n\n• UI in screen space — After restoring the camera transform, UI elements are drawn in fixed screen positions (health bar in top-left, score in top-right) independent of where the camera is looking in the world.`,
    summary: "The rendering pipeline clears the screen, sorts objects by layer and depth, applies camera transformations to convert world coordinates to screen space, culls invisible objects, and draws everything in order. Separating rendering from game logic enables flexible display strategies without affecting gameplay."
  }
];
