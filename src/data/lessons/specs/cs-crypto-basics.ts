// Pre-written spec lessons for: Cryptography Basics

export const csCryptoBasicsLessons = [
  {
    title: "Symmetric vs Asymmetric Encryption",
    definition: "Symmetric encryption uses a single shared key for both encryption and decryption, while asymmetric encryption uses a mathematically linked key pair — a public key for encryption and a private key for decryption. Understanding the tradeoffs between these two approaches is fundamental to all modern cryptographic systems.",

    explanation: `Symmetric encryption is the oldest and most straightforward form of cryptography. Both the sender and receiver share an identical secret key. The sender encrypts plaintext using this key, and the receiver decrypts the ciphertext with the same key. Examples include AES (Advanced Encryption Standard), DES, and ChaCha20. Symmetric encryption is extremely fast and efficient, making it ideal for encrypting large volumes of data like disk encryption or VPN tunnels.\n\nAsymmetric encryption (also called public-key cryptography) solves the key distribution problem inherent in symmetric systems. Each party generates a key pair: a public key they share openly, and a private key they keep secret. Data encrypted with someone's public key can only be decrypted with their corresponding private key. RSA, ECC (Elliptic Curve Cryptography), and ElGamal are common asymmetric algorithms.\n\nThe fundamental tradeoff is performance vs. convenience. Symmetric encryption is roughly 1000x faster than asymmetric encryption, but requires a secure channel to exchange keys. Asymmetric encryption eliminates the key exchange problem but is computationally expensive. In practice, modern protocols like TLS use a hybrid approach: asymmetric encryption to securely exchange a symmetric session key, then symmetric encryption for the actual data transfer.\n\nKey sizes differ dramatically between the two. A 256-bit AES symmetric key provides equivalent security to a 3072-bit RSA key or a 256-bit ECC key. This is because breaking symmetric encryption requires brute force (2^n operations for an n-bit key), while breaking asymmetric encryption involves solving mathematical problems like integer factorization (RSA) or the discrete logarithm problem (ECC).`,

    code: `# Symmetric vs Asymmetric Encryption in Python\nfrom cryptography.fernet import Fernet\nfrom cryptography.hazmat.primitives.asymmetric import rsa, padding\nfrom cryptography.hazmat.primitives import hashes, serialization\n\n# === SYMMETRIC ENCRYPTION (AES via Fernet) ===\n# Generate a random symmetric key\nsymmetric_key = Fernet.generate_key()\ncipher = Fernet(symmetric_key)\n\n# Encrypt a message\nplaintext = b"Secret message for symmetric encryption"\nciphertext = cipher.encrypt(plaintext)\nprint(f"Encrypted: {ciphertext[:50]}...")\n\n# Decrypt with the SAME key\ndecrypted = cipher.decrypt(ciphertext)\nprint(f"Decrypted: {decrypted.decode()}")\n\n# === ASYMMETRIC ENCRYPTION (RSA) ===\n# Generate RSA key pair\nprivate_key = rsa.generate_private_key(\n    public_exponent=65537,\n    key_size=2048\n)\npublic_key = private_key.public_key()\n\n# Encrypt with PUBLIC key (anyone can do this)\nmessage = b"Secret message for asymmetric encryption"\nciphertext_rsa = public_key.encrypt(\n    message,\n    padding.OAEP(\n        mgf=padding.MGF1(algorithm=hashes.SHA256()),\n        algorithm=hashes.SHA256(),\n        label=None\n    )\n)\n\n# Decrypt with PRIVATE key (only key owner)\ndecrypted_rsa = private_key.decrypt(\n    ciphertext_rsa,\n    padding.OAEP(\n        mgf=padding.MGF1(algorithm=hashes.SHA256()),\n        algorithm=hashes.SHA256(),\n        label=None\n    )\n)\nprint(f"RSA Decrypted: {decrypted_rsa.decode()}")`,

    breakdown: `• Fernet (symmetric) — Uses AES-128-CBC under the hood with HMAC for authentication. A single key encrypts and decrypts. Key must be shared securely between parties.\n\n• RSA (asymmetric) — Generates a 2048-bit key pair. The public key encrypts, the private key decrypts. No need to share secret keys over insecure channels.\n\n• OAEP Padding — Optimal Asymmetric Encryption Padding adds randomness to RSA encryption, preventing attacks where identical plaintexts produce identical ciphertexts.\n\n• Performance tradeoff — Symmetric encryption handles gigabytes per second; RSA encryption is limited to small data blocks (max ~190 bytes for 2048-bit key with OAEP).\n\n• Hybrid approach — Real systems combine both: use RSA to exchange an AES key, then AES for bulk data encryption. This is exactly how TLS/HTTPS works.`,
    summary: "Symmetric encryption uses one shared key and is fast but has key distribution challenges. Asymmetric encryption uses key pairs solving distribution but is slower. Modern systems combine both in hybrid schemes where asymmetric encryption protects symmetric key exchange."
  },

  {
    title: "Hashing Algorithms (MD5, SHA)",
    definition: "Hash functions are one-way mathematical algorithms that transform arbitrary-length input data into a fixed-length output (called a digest or hash). Unlike encryption, hashing is irreversible by design — you cannot recover the original data from a hash. Hash functions are used for data integrity verification, password storage, digital signatures, and blockchain technology.",

    explanation: `A cryptographic hash function must satisfy several properties: determinism (same input always produces same output), pre-image resistance (given a hash, it's computationally infeasible to find the input), second pre-image resistance (given an input, it's infeasible to find a different input with the same hash), and collision resistance (infeasible to find any two different inputs with the same hash).\n\nMD5 (Message Digest 5) produces a 128-bit hash. It was designed in 1991 by Ronald Rivest and was widely used for file integrity checks. However, MD5 is now considered cryptographically broken — researchers demonstrated practical collision attacks in 2004, meaning two different files can be crafted to produce identical MD5 hashes. MD5 should never be used for security purposes but is still used for non-security checksums.\n\nThe SHA (Secure Hash Algorithm) family was designed by the NSA. SHA-1 produces a 160-bit hash but was deprecated after collision attacks in 2017. SHA-2 includes SHA-256 (256-bit) and SHA-512 (512-bit) and remains secure. SHA-3 (Keccak) was standardized in 2015 as an alternative with a completely different internal structure, providing defense-in-depth if SHA-2 is ever broken.\n\nFor password hashing specifically, general-purpose hash functions like SHA-256 are too fast — an attacker with a GPU can compute billions of SHA-256 hashes per second. Purpose-built password hashing functions like bcrypt, scrypt, and Argon2 intentionally slow computation and require significant memory, making brute-force attacks impractical even with specialized hardware.`,

    code: `# Hashing Algorithms Comparison in Python\nimport hashlib\nimport bcrypt\nimport time\n\nmessage = b"The quick brown fox jumps over the lazy dog"\n\n# === MD5 (BROKEN - do not use for security) ===\nmd5_hash = hashlib.md5(message).hexdigest()\nprint(f"MD5 (128-bit):  {md5_hash}")\nprint(f"MD5 length: {len(md5_hash) * 4} bits")\n\n# === SHA-256 (Secure, widely used) ===\nsha256_hash = hashlib.sha256(message).hexdigest()\nprint(f"SHA-256:        {sha256_hash}")\nprint(f"SHA-256 length: {len(sha256_hash) * 4} bits")\n\n# === SHA-512 (Larger output, same family) ===\nsha512_hash = hashlib.sha512(message).hexdigest()\nprint(f"SHA-512:        {sha512_hash[:64]}...")\n\n# === Avalanche effect demonstration ===\n# Changing one character completely changes the hash\nmessage2 = b"The quick brown fox jumps over the lazy dog."\nsha256_original = hashlib.sha256(message).hexdigest()\nsha256_modified = hashlib.sha256(message2).hexdigest()\nprint(f"\\nAvalanche Effect:")\nprint(f"Original: {sha256_original[:32]}...")\nprint(f"Modified: {sha256_modified[:32]}...")\n\n# === Password hashing with bcrypt (RECOMMENDED) ===\npassword = b"my_secure_password"\n\n# bcrypt automatically generates salt and is intentionally slow\nstart = time.time()\nhashed = bcrypt.hashpw(password, bcrypt.gensalt(rounds=12))\nprint(f"\\nbcrypt time: {time.time() - start:.3f}s")\nprint(f"bcrypt hash: {hashed.decode()}")\n\n# Verification\nassert bcrypt.checkpw(password, hashed)  # Returns True`,

    breakdown: `• MD5 produces 128-bit output — Fast but cryptographically broken since 2004. Collisions can be generated in seconds. Only acceptable for non-security checksums like verifying file downloads.\n\n• SHA-256 produces 256-bit output — Currently secure with no known practical attacks. Used in TLS certificates, Bitcoin mining, file integrity, and digital signatures. Part of the SHA-2 family.\n\n• Avalanche effect — Changing even a single bit of input completely changes the output hash. This property ensures that similar inputs produce vastly different hashes, preventing pattern analysis.\n\n• bcrypt for passwords — General-purpose hashes are too fast for password storage. bcrypt uses a configurable work factor (rounds=12 means 2^12 iterations), making each hash take ~250ms — negligible for legitimate login but devastating for brute-force attacks.\n\n• Salt prevents rainbow tables — bcrypt automatically prepends a random salt to each password before hashing. This means identical passwords produce different hashes, defeating pre-computed lookup tables.`,
    summary: "Hash functions produce fixed-length irreversible digests used for integrity and authentication. MD5 is broken; SHA-256 remains secure for general use. For password storage, use purpose-built slow hashes like bcrypt or Argon2 that resist GPU-accelerated brute-force attacks."
  },

  {
    title: "Digital Signatures & Certificates",
    definition: "A digital signature is a cryptographic mechanism that proves the authenticity and integrity of a message or document. It uses asymmetric cryptography in reverse: the sender signs with their private key, and anyone can verify with the sender's public key. Digital certificates bind a public key to an identity through a trusted Certificate Authority (CA).",

    explanation: `Digital signatures provide three crucial security properties: authentication (proves who sent the message), integrity (proves the message wasn't altered), and non-repudiation (the sender cannot deny having sent it). The process works by first hashing the message, then encrypting that hash with the sender's private key. The recipient decrypts with the sender's public key and compares hashes.\n\nThe signing process: (1) Compute a hash of the document (e.g., SHA-256), (2) Encrypt the hash using the signer's private key — this encrypted hash IS the signature, (3) Attach the signature to the document. Verification: (1) Decrypt the signature using the signer's public key to recover the hash, (2) Independently hash the document, (3) Compare — if they match, the signature is valid.\n\nDigital certificates solve the trust problem: how do you know a public key actually belongs to who it claims? A Certificate Authority (CA) verifies the identity of the key owner and issues a certificate — essentially the CA's digital signature on the binding between a public key and an identity. This creates a chain of trust: you trust the CA, the CA vouches for the certificate holder.\n\nThe X.509 certificate standard defines the format used in TLS/SSL. A certificate contains: subject name (domain or organization), public key, issuer (CA), validity period, serial number, and the CA's signature. Certificate chains work hierarchically: Root CAs sign intermediate CAs, which sign end-entity certificates. Browsers ship with ~150 trusted root CA certificates pre-installed.`,

    code: `# Digital Signatures in Python\nfrom cryptography.hazmat.primitives.asymmetric import rsa, padding, utils\nfrom cryptography.hazmat.primitives import hashes, serialization\nfrom cryptography.x509 import CertificateBuilder, Name, NameAttribute\nfrom cryptography.x509.oid import NameOID\nimport datetime\n\n# === DIGITAL SIGNATURE ===\n# Step 1: Generate key pair for the signer\nprivate_key = rsa.generate_private_key(\n    public_exponent=65537,\n    key_size=2048\n)\npublic_key = private_key.public_key()\n\n# Step 2: Sign a message (hash + encrypt with private key)\nmessage = b"I authorize the transfer of 1000 USD to Account #12345"\n\nsignature = private_key.sign(\n    message,\n    padding.PSS(\n        mgf=padding.MGF1(hashes.SHA256()),\n        salt_length=padding.PSS.MAX_LENGTH\n    ),\n    hashes.SHA256()\n)\nprint(f"Signature (hex): {signature.hex()[:64]}...")\n\n# Step 3: Verify signature (anyone with public key can do this)\ntry:\n    public_key.verify(\n        signature,\n        message,\n        padding.PSS(\n            mgf=padding.MGF1(hashes.SHA256()),\n            salt_length=padding.PSS.MAX_LENGTH\n        ),\n        hashes.SHA256()\n    )\n    print("Signature is VALID - message is authentic")\nexcept Exception:\n    print("Signature is INVALID - message was tampered")\n\n# Tampered message fails verification\ntampered = b"I authorize the transfer of 9999 USD to Account #99999"\ntry:\n    public_key.verify(signature, tampered,\n        padding.PSS(mgf=padding.MGF1(hashes.SHA256()),\n                    salt_length=padding.PSS.MAX_LENGTH),\n        hashes.SHA256())\nexcept Exception as e:\n    print(f"Tampered message caught: {type(e).__name__}")`,

    breakdown: `• PSS Padding (Probabilistic Signature Scheme) — Adds randomness to signature generation so signing the same message twice produces different signatures, preventing signature analysis attacks.\n\n• Sign then Verify pattern — The private key creates the signature (only the owner can sign), while the public key verifies it (anyone can verify). This is the reverse of encryption where public encrypts and private decrypts.\n\n• Tamper detection — Even a single character change in the message invalidates the signature. The verification process independently hashes the received message and compares it to the hash recovered from the signature.\n\n• Certificate Authority trust model — CAs are organizations trusted by browsers/OS vendors. When you visit https://example.com, the server presents its certificate, your browser checks the CA signature chain up to a pre-trusted root CA.\n\n• Non-repudiation — Unlike symmetric MACs where both parties share the key, only the private key holder can create a valid signature. This provides legal-grade proof of authorship that the signer cannot deny.`,
    summary: "Digital signatures use private keys to sign and public keys to verify, providing authentication, integrity, and non-repudiation. Certificates bind public keys to identities through trusted CAs, forming the trust infrastructure behind HTTPS and secure communications."
  },

  {
    title: "Key Exchange (Diffie-Hellman)",
    definition: "The Diffie-Hellman key exchange protocol allows two parties to establish a shared secret over an insecure communication channel without ever transmitting the secret itself. This solved one of cryptography's fundamental challenges: how can two strangers agree on a shared encryption key when an eavesdropper can observe all their communications?",

    explanation: `The Diffie-Hellman protocol, published in 1976 by Whitfield Diffie and Martin Hellman, was revolutionary because it proved that two parties could agree on a secret without a pre-existing secure channel. The security relies on the mathematical difficulty of the discrete logarithm problem: given g^a mod p, it's computationally infeasible to determine 'a' for large primes.\n\nThe protocol works as follows: Alice and Bob publicly agree on a large prime p and a generator g. Alice picks a secret number 'a' and sends Bob g^a mod p. Bob picks a secret number 'b' and sends Alice g^b mod p. Alice computes (g^b)^a mod p. Bob computes (g^a)^b mod p. Both arrive at the same value g^(ab) mod p — the shared secret. An eavesdropper sees g^a mod p and g^b mod p but cannot efficiently compute g^(ab) mod p.\n\nModern implementations use Elliptic Curve Diffie-Hellman (ECDH) which provides equivalent security with much smaller key sizes. A 256-bit ECDH key provides the same security as a 3072-bit classic DH key. ECDH is used in TLS 1.3, Signal Protocol, and most modern key exchange scenarios.\n\nDiffie-Hellman is vulnerable to man-in-the-middle attacks if used without authentication. An attacker could intercept communications and perform separate DH exchanges with each party. This is why DH is always combined with authentication (digital signatures or pre-shared keys) in real protocols. Ephemeral DH (DHE/ECDHE) generates new key pairs for each session, providing forward secrecy — if long-term keys are compromised, past sessions remain secure.`,

    code: `# Diffie-Hellman Key Exchange in Python\nfrom cryptography.hazmat.primitives.asymmetric import dh, ec\nfrom cryptography.hazmat.primitives import hashes\nfrom cryptography.hazmat.primitives.kdf.hkdf import HKDF\nimport os\n\n# === CLASSIC DIFFIE-HELLMAN ===\n# Step 1: Generate shared parameters (public: p and g)\nparameters = dh.generate_parameters(generator=2, key_size=2048)\n\n# Step 2: Alice generates her private key and public value\nalice_private = parameters.generate_private_key()\nalice_public = alice_private.public_key()\n\n# Step 3: Bob generates his private key and public value\nbob_private = parameters.generate_private_key()\nbob_public = bob_private.public_key()\n\n# Step 4: Exchange public values and compute shared secret\n# Alice computes: bob_public ^ alice_private mod p\nalice_shared = alice_private.exchange(bob_public)\n# Bob computes: alice_public ^ bob_private mod p\nbob_shared = bob_private.exchange(alice_public)\n\n# Both arrive at the same shared secret!\nassert alice_shared == bob_shared\nprint(f"Shared secret (first 32 bytes): {alice_shared[:32].hex()}")\n\n# Step 5: Derive usable encryption key from shared secret\nderived_key = HKDF(\n    algorithm=hashes.SHA256(),\n    length=32,\n    salt=None,\n    info=b"handshake data"\n).derive(alice_shared)\nprint(f"Derived AES key: {derived_key.hex()}")\n\n# === ELLIPTIC CURVE DH (Modern, preferred) ===\nalice_ec_private = ec.generate_private_key(ec.SECP256R1())\nbob_ec_private = ec.generate_private_key(ec.SECP256R1())\n\nalice_ec_shared = alice_ec_private.exchange(\n    ec.ECDH(), bob_ec_private.public_key())\nbob_ec_shared = bob_ec_private.exchange(\n    ec.ECDH(), alice_ec_private.public_key())\n\nassert alice_ec_shared == bob_ec_shared\nprint(f"ECDH shared secret: {alice_ec_shared.hex()[:64]}...")`,

    breakdown: `• Parameter generation — Both parties agree on a large prime p and generator g. These are public and can be reused. The security comes from the private exponents a and b, not from p and g being secret.\n\n• Key exchange math — Alice sends g^a mod p, Bob sends g^b mod p. Both compute g^(ab) mod p independently. An eavesdropper cannot compute g^(ab) from g^a and g^b without solving the discrete logarithm problem.\n\n• HKDF key derivation — The raw shared secret should not be used directly as an encryption key. HKDF (HMAC-based Key Derivation Function) extracts entropy and expands it into a cryptographically strong key of the desired length.\n\n• ECDH advantage — Elliptic Curve DH provides equivalent security with ~10x smaller keys and faster computation. SECP256R1 (P-256) is the most common curve, providing 128-bit security level.\n\n• Forward secrecy — Using ephemeral (one-time) DH keys means that even if a server's long-term private key is compromised later, all past session keys remain secure. This is why TLS 1.3 requires ephemeral key exchange.`,
    summary: "Diffie-Hellman enables two parties to establish a shared secret over public channels using the mathematical hardness of the discrete logarithm problem. Modern systems use Elliptic Curve DH (ECDH) for efficiency and ephemeral keys for forward secrecy."
  },

  {
    title: "Encryption in Practice (AES, RSA)",
    definition: "AES (Advanced Encryption Standard) and RSA are the two most widely deployed encryption algorithms in the world. AES is a symmetric block cipher that encrypts data in 128-bit blocks using 128, 192, or 256-bit keys. RSA is an asymmetric algorithm based on the difficulty of factoring the product of two large primes. Together they secure virtually all internet communications.",

    explanation: `AES operates in different modes that dramatically affect its security properties. ECB (Electronic Codebook) encrypts each block independently — identical plaintext blocks produce identical ciphertext blocks, leaking patterns. CBC (Cipher Block Chaining) XORs each plaintext block with the previous ciphertext block, hiding patterns but requiring serial processing. CTR (Counter) mode turns AES into a stream cipher, enabling parallel encryption. GCM (Galois/Counter Mode) provides both encryption and authentication (AEAD — Authenticated Encryption with Associated Data), and is the preferred mode for modern applications.\n\nRSA key generation: (1) Choose two large random primes p and q, (2) Compute n = p * q (the modulus), (3) Compute the totient phi(n) = (p-1)(q-1), (4) Choose public exponent e (typically 65537), (5) Compute private exponent d where e*d mod phi(n) = 1. The public key is (n, e), the private key is (n, d). Security relies on the fact that factoring n back into p and q is computationally infeasible for large primes (2048+ bits).\n\nIn practice, RSA is limited by message size (max plaintext = key_size - padding_overhead). For 2048-bit RSA with OAEP padding, maximum plaintext is about 190 bytes. This is why RSA is used to encrypt small items like symmetric keys or hashes, never bulk data directly.\n\nReal-world deployment: HTTPS uses ECDHE for key exchange, RSA or ECDSA for authentication (digital signatures in certificates), and AES-256-GCM for bulk data encryption. SSH uses similar combinations. File encryption tools like GPG use RSA to encrypt a random AES session key, then AES to encrypt the actual file. Full-disk encryption (BitLocker, LUKS) uses AES-XTS mode optimized for random-access storage.`,

    code: `# AES and RSA Encryption in Practice\nfrom cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes\nfrom cryptography.hazmat.primitives.asymmetric import rsa, padding\nfrom cryptography.hazmat.primitives import hashes, padding as sym_padding\nimport os\n\n# === AES-256-GCM (Authenticated Encryption) ===\n# GCM provides both confidentiality AND integrity\ndef aes_gcm_encrypt(plaintext: bytes, key: bytes):\n    """Encrypt with AES-256-GCM (recommended mode)"""\n    iv = os.urandom(12)  # 96-bit IV for GCM\n    cipher = Cipher(algorithms.AES(key), modes.GCM(iv))\n    encryptor = cipher.encryptor()\n    \n    # Associated data (authenticated but not encrypted)\n    encryptor.authenticate_additional_data(b"metadata")\n    \n    ciphertext = encryptor.update(plaintext) + encryptor.finalize()\n    # Tag proves data wasn't tampered with\n    return iv, ciphertext, encryptor.tag\n\ndef aes_gcm_decrypt(iv, ciphertext, tag, key):\n    """Decrypt and verify integrity"""\n    cipher = Cipher(algorithms.AES(key), modes.GCM(iv, tag))\n    decryptor = cipher.decryptor()\n    decryptor.authenticate_additional_data(b"metadata")\n    return decryptor.update(ciphertext) + decryptor.finalize()\n\n# Generate random 256-bit key\naes_key = os.urandom(32)\nmessage = b"Sensitive financial data: Account balance $50,000"\n\niv, ct, tag = aes_gcm_encrypt(message, aes_key)\nrecovered = aes_gcm_decrypt(iv, ct, tag, aes_key)\nprint(f"Decrypted: {recovered.decode()}")\n\n# === HYBRID ENCRYPTION (RSA + AES) ===\n# Real-world pattern: encrypt AES key with RSA\nrsa_private = rsa.generate_private_key(public_exponent=65537, key_size=2048)\nrsa_public = rsa_private.public_key()\n\n# Encrypt the AES key with recipient's RSA public key\nencrypted_aes_key = rsa_public.encrypt(\n    aes_key,\n    padding.OAEP(\n        mgf=padding.MGF1(algorithm=hashes.SHA256()),\n        algorithm=hashes.SHA256(), label=None\n    )\n)\nprint(f"Encrypted AES key size: {len(encrypted_aes_key)} bytes")\n\n# Recipient decrypts AES key with their private key\nrecovered_key = rsa_private.decrypt(\n    encrypted_aes_key,\n    padding.OAEP(\n        mgf=padding.MGF1(algorithm=hashes.SHA256()),\n        algorithm=hashes.SHA256(), label=None\n    )\n)\nassert recovered_key == aes_key\nprint("Hybrid encryption successful!")`,

    breakdown: `• AES-GCM mode — Provides authenticated encryption: if even one bit of ciphertext is modified, decryption fails. The authentication tag (16 bytes) acts as a cryptographic checksum. This prevents bit-flipping attacks possible with CTR mode alone.\n\n• 96-bit IV (Initialization Vector) — Must be unique for every encryption with the same key. Reusing an IV with GCM completely breaks security. For random IVs, 96 bits provides sufficient collision resistance for practical use.\n\n• Hybrid encryption pattern — RSA encrypts the small AES key (32 bytes), AES encrypts the large payload. This combines RSA's key distribution advantage with AES's speed. Used in PGP/GPG, TLS, and S/MIME email encryption.\n\n• OAEP padding for RSA — Adds randomized padding to prevent deterministic encryption attacks. Without padding, RSA is malleable (ciphertexts can be mathematically manipulated to produce predictable plaintext changes).\n\n• Key management is critical — The best algorithm is worthless if keys are stored insecurely. Use hardware security modules (HSMs) or key management services (AWS KMS, HashiCorp Vault) in production. Never hardcode keys in source code.`,
    summary: "AES-256-GCM is the gold standard for symmetric encryption, providing both confidentiality and integrity. RSA enables secure key distribution. Real systems use hybrid encryption combining RSA for key exchange with AES for bulk data, secured by proper key management practices."
  }
];
