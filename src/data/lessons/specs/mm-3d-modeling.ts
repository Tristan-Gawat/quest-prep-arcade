// Pre-written spec lessons for: 3D Modeling

export const mm3dModelingLessons = [
  {
    title: "3D Coordinate Systems & Transforms",
    definition: "3D coordinate systems define spatial positions using three perpendicular axes (X, Y, Z), while transforms — translation, rotation, and scaling — manipulate objects within that space using matrix mathematics to position, orient, and size geometry.",
    explanation: `Every 3D application operates within a coordinate system that establishes how positions are described in space. The three axes are typically: X (horizontal/left-right), Y (vertical/up-down), and Z (depth/forward-back). However, different software uses different conventions: Y-up (Blender default, OpenGL, Three.js) or Z-up (3ds Max, Unreal Engine). This mismatch is a common source of confusion when moving assets between applications.\n\n3D transforms are operations that modify an object's position, orientation, or size. Translation moves an object along axes, rotation spins it around axes, and scaling changes its size along axes. These operations are represented as 4x4 matrices, and combining multiple transforms means multiplying their matrices together. The order of multiplication matters — rotating then translating gives different results than translating then rotating.\n\nEvery object in a 3D scene has both local space (relative to itself) and world space (relative to the scene origin). A character's hand has a local position relative to the arm bone, but a world position that accounts for the arm's rotation, the body's position, and the character's location in the scene. This hierarchy of coordinate spaces is fundamental to scene graphs and skeletal animation.\n\nUnderstanding pivot points (transform origins) is crucial. When you rotate an object, it rotates around its pivot point. A door's pivot is at its hinge, a wheel's pivot is at its center. Misplaced pivots create unexpected rotation behavior — one of the most common beginner mistakes in 3D modeling.`,
    code: `// 3D Transform concepts in JavaScript (Three.js style)\n\n// Position: Translation along X, Y, Z axes\nconst mesh = new THREE.Mesh(geometry, material);\nmesh.position.set(5, 2, -3); // X=5, Y=2, Z=-3\n\n// Rotation: Around each axis (in radians)\nmesh.rotation.set(\n    Math.PI / 4,  // 45 degrees around X\n    0,             // no Y rotation\n    Math.PI / 6   // 30 degrees around Z\n);\n\n// Scale: Multiplier on each axis\nmesh.scale.set(2, 1, 1); // Stretched 2x on X axis\n\n// Transform Matrix (4x4)\n// Combines translation, rotation, and scale\n// | Rx Ry Rz Tx |   R = rotation/scale\n// | Rx Ry Rz Ty |   T = translation\n// | Rx Ry Rz Tz |\n// |  0  0  0  1 |\n\n// Parent-child hierarchy (scene graph)\nconst car = new THREE.Group();\nconst body = new THREE.Mesh(bodyGeom, bodyMat);\nconst wheel = new THREE.Mesh(wheelGeom, wheelMat);\n\nwheel.position.set(1.5, -0.5, 1); // Local to car\ncar.add(body);\ncar.add(wheel);\n\n// Moving the car moves all children\ncar.position.x += 1; // Body AND wheel move together\n// Rotating wheel locally doesn't affect car\nwheel.rotation.x += 0.1; // Wheel spins independently\n\n// Converting between coordinate spaces\n// Local position to world position\nconst worldPos = new THREE.Vector3();\nwheel.getWorldPosition(worldPos);\n// worldPos = car.position + wheel.localPosition (transformed)\n\n// Pivot point demonstration\n// Door hinge: offset pivot\nconst door = new THREE.Mesh(doorGeom, doorMat);\ndoor.geometry.translate(0.5, 0, 0); // Shift geometry so\n// pivot (origin) is at the left edge (hinge point)\ndoor.rotation.y = Math.PI / 3; // Rotates around hinge`,
    breakdown: `• Coordinate handedness — Left-handed (DirectX, Unity) vs right-handed (OpenGL, Blender) systems differ in Z direction. When exporting between tools, check if Z points into or out of the screen to avoid mirrored models.\n\n• Transform order (SRT) — Standard order is Scale, then Rotate, then Translate. In matrix math this is written T * R * S (applied right-to-left). Changing the order produces completely different results.\n\n• Local vs World space — Local space is relative to the parent object. An object at local position (0,0,0) sits at its parent's origin. World space is the absolute scene position after all parent transforms are applied.\n\n• Gimbal lock — When using Euler angles (X,Y,Z rotations), two axes can align and you lose a degree of freedom. Quaternions solve this but are less intuitive. Most 3D software uses Euler for display but quaternions internally.\n\n• Scene graph — A tree structure where each node's transform is relative to its parent. Moving a root node moves the entire subtree. This hierarchy enables complex assembled objects (robot arms, vehicles, skeletal characters).`,
    summary: "3D coordinate systems establish spatial reference using three axes with different conventions across tools. Transforms (translate, rotate, scale) are represented as 4x4 matrices applied in specific order, operating within hierarchical parent-child scene graphs that define local and world space relationships."
  },
  {
    title: "Mesh Geometry (Vertices, Edges, Faces)",
    definition: "A mesh is the fundamental 3D model representation consisting of vertices (points in space), edges (connections between vertices), and faces (filled polygonal surfaces) that together define the shape of a 3D object through interconnected triangles or polygons.",
    explanation: `All 3D models you see in games, films, and web experiences are constructed from meshes — collections of flat polygonal faces that approximate curved surfaces. A vertex is a single point in 3D space defined by its XYZ coordinates. An edge connects two vertices. A face is a filled surface bounded by three or more edges. Most real-time applications use triangular faces exclusively because triangles are always planar and every GPU is optimized to render them.\n\nMesh topology refers to how vertices and edges flow across a surface. Good topology has evenly-spaced quads (four-sided polygons) that follow the natural contours of the shape, enabling clean deformation for animation and efficient subdivision. Bad topology — triangles, n-gons (5+ sided polygons), extremely thin faces, or uneven density — causes shading artifacts, poor deformation, and wastes polygons.\n\nPolygon count (polycount) directly affects performance. A mobile game character might use 5,000-15,000 triangles, while a console character uses 30,000-100,000+. Hard-surface objects (buildings, vehicles) can get away with lower polycounts because they don't need to deform. Organic shapes (characters, creatures) need higher density at joints and facial features for smooth animation.\n\nNormals are vectors perpendicular to each face (or vertex) that tell the renderer which direction the surface points. They determine how light bounces off the surface. Smooth shading interpolates normals across faces for a curved appearance. Flat shading uses face normals directly, giving a faceted look. Normal maps can fake the appearance of high-detail geometry on low-poly meshes.`,
    code: `// Mesh geometry concepts\n\n// A simple cube defined by vertices and faces\n// 8 vertices, 12 triangular faces (2 per side)\nconst cubeVertices = [\n    // Front face\n    { x: -1, y: -1, z:  1 },  // 0: bottom-left-front\n    { x:  1, y: -1, z:  1 },  // 1: bottom-right-front\n    { x:  1, y:  1, z:  1 },  // 2: top-right-front\n    { x: -1, y:  1, z:  1 },  // 3: top-left-front\n    // Back face\n    { x: -1, y: -1, z: -1 },  // 4: bottom-left-back\n    { x:  1, y: -1, z: -1 },  // 5: bottom-right-back\n    { x:  1, y:  1, z: -1 },  // 6: top-right-back\n    { x: -1, y:  1, z: -1 },  // 7: top-left-back\n];\n\n// Faces defined as triangle indices (winding order matters)\nconst cubeFaces = [\n    // Front face (2 triangles)\n    [0, 1, 2], [0, 2, 3],\n    // Back face\n    [5, 4, 7], [5, 7, 6],\n    // Top face\n    [3, 2, 6], [3, 6, 7],\n    // Bottom face\n    [4, 5, 1], [4, 1, 0],\n    // Right face\n    [1, 5, 6], [1, 6, 2],\n    // Left face\n    [4, 0, 3], [4, 3, 7]\n];\n\n// Calculate face normal (cross product of two edges)\nfunction calculateNormal(v0, v1, v2) {\n    const edge1 = {\n        x: v1.x - v0.x,\n        y: v1.y - v0.y,\n        z: v1.z - v0.z\n    };\n    const edge2 = {\n        x: v2.x - v0.x,\n        y: v2.y - v0.y,\n        z: v2.z - v0.z\n    };\n    // Cross product gives perpendicular vector\n    const normal = {\n        x: edge1.y * edge2.z - edge1.z * edge2.y,\n        y: edge1.z * edge2.x - edge1.x * edge2.z,\n        z: edge1.x * edge2.y - edge1.y * edge2.x\n    };\n    // Normalize to unit length\n    const len = Math.sqrt(\n        normal.x ** 2 + normal.y ** 2 + normal.z ** 2\n    );\n    return { x: normal.x/len, y: normal.y/len, z: normal.z/len };\n}\n\n// Three.js BufferGeometry (how meshes are stored for GPU)\nconst geometry = new THREE.BufferGeometry();\nconst vertices = new Float32Array([\n    -1, -1, 1,  // vertex 0\n     1, -1, 1,  // vertex 1\n     1,  1, 1   // vertex 2\n]);\ngeometry.setAttribute('position',\n    new THREE.BufferAttribute(vertices, 3) // 3 values per vertex\n);`,
    breakdown: `• Vertices — Points with XYZ position (plus optional UV coordinates for texturing, normals for lighting, and vertex colors). The vertex buffer is sent to the GPU once and reused across frames.\n\n• Index buffers — Instead of repeating shared vertices for adjacent faces, an index buffer references vertices by number. A cube has 8 unique vertices but 36 indices (12 triangles × 3 vertices each), saving memory.\n\n• Winding order — Counter-clockwise (CCW) vertex order defines the front of a face. Clockwise winding = back face, which gets culled (not drawn) for performance. Flipped normals mean inside-out rendering.\n\n• Edge loops — Continuous rings of edges around a model. Strategic edge loops at joints, eyelids, and mouths provide geometry for smooth deformation. More loops = smoother bending but higher polycount.\n\n• Subdivision surfaces — Algorithms like Catmull-Clark subdivide each quad into four, smoothing the surface. Artists model low-poly 'control cages' that subdivide into smooth final meshes, giving both control and detail.`,
    summary: "Meshes represent 3D shapes through vertices (points), edges (connections), and faces (surfaces). Proper topology with clean quad-based edge flow enables smooth animation deformation, while understanding normals, winding order, and polycount budgets is essential for rendering performance."
  },
  {
    title: "Materials & Texturing",
    definition: "Materials define how a 3D surface responds to light (color, reflectivity, roughness, transparency), while textures are 2D images mapped onto mesh surfaces using UV coordinates to add color detail, surface variation, and visual complexity without additional geometry.",
    explanation: `A material is a complete description of a surface's visual properties. Modern materials use Physically Based Rendering (PBR), which simulates real-world light interaction with physically accurate parameters. The standard PBR workflow uses: albedo/base color (the diffuse color without lighting), metallic (0 = dielectric, 1 = metal), roughness (0 = mirror-smooth, 1 = fully rough), and normal maps (fake surface bumps).\n\nUV mapping is the process of flattening a 3D mesh into 2D space so textures can be applied. Imagine cutting a cardboard box and unfolding it flat — those flattened pieces are 'UV islands.' The U and V axes are the 2D equivalents of X and Y. Every vertex stores UV coordinates that tell the renderer where on the texture image that point samples from. Good UV unwrapping minimizes stretching and maximizes texture resolution usage.\n\nTexture maps serve different purposes: the albedo map provides base color, the normal map adds surface detail (bumps, scratches) without geometry cost, the roughness map varies surface smoothness (glossy vs matte areas), the metallic map defines which areas are metallic, and ambient occlusion (AO) maps darken crevices for depth. Together, these create convincing materials from relatively few polygons.\n\nTexture resolution is measured in pixels (1024x1024, 2048x2048, 4096x4096). Higher resolution means more detail but more memory. Games carefully budget texture memory — characters might get 2K-4K textures while background props get 256-512px. Texture atlases combine multiple objects' textures into one image to reduce draw calls.`,
    code: `// PBR Material setup (Three.js)\nconst material = new THREE.MeshStandardMaterial({\n    // Base color (albedo)\n    map: textureLoader.load('albedo.png'),\n    color: 0xffffff,  // Tint multiplier\n\n    // Metalness: 0 = plastic/wood, 1 = metal\n    metalness: 0.0,\n    metalnessMap: textureLoader.load('metalness.png'),\n\n    // Roughness: 0 = mirror, 1 = chalk\n    roughness: 0.5,\n    roughnessMap: textureLoader.load('roughness.png'),\n\n    // Normal map: Fakes surface detail\n    normalMap: textureLoader.load('normal.png'),\n    normalScale: new THREE.Vector2(1, 1),\n\n    // Ambient Occlusion: Darkens crevices\n    aoMap: textureLoader.load('ao.png'),\n    aoMapIntensity: 1.0,\n\n    // Emissive: Self-illuminating areas\n    emissiveMap: textureLoader.load('emissive.png'),\n    emissive: 0xffffff,\n    emissiveIntensity: 2.0\n});\n\n// UV Mapping concept\n// Each vertex has a UV coordinate (0-1 range)\nconst uvs = new Float32Array([\n    0.0, 0.0,  // vertex 0: bottom-left of texture\n    1.0, 0.0,  // vertex 1: bottom-right of texture\n    1.0, 1.0,  // vertex 2: top-right of texture\n    0.0, 1.0   // vertex 3: top-left of texture\n]);\ngeometry.setAttribute('uv',\n    new THREE.BufferAttribute(uvs, 2) // 2 values per vertex\n);\n\n// Shader pseudocode for PBR calculation\n/*\nfragmentShader:\n    albedo = texture(albedoMap, uv).rgb\n    metallic = texture(metallicMap, uv).r\n    roughness = texture(roughnessMap, uv).r\n    normal = perturbNormal(texture(normalMap, uv))\n\n    // For each light:\n    diffuse = albedo * (1 - metallic) * lambertian(normal, lightDir)\n    specular = fresnel(metallic, albedo) *\n               distribution(roughness, normal, halfway) *\n               geometry(roughness, normal, viewDir, lightDir)\n\n    finalColor = diffuse + specular + ambient * ao\n*/`,
    breakdown: `• PBR workflow — Physically Based Rendering uses energy-conserving shading: as a surface becomes more reflective (lower roughness), it reflects more light specularly and less diffusely. Metals tint their reflections with their albedo color; dielectrics reflect white.\n\n• UV coordinates — Range from 0 to 1 across a texture. Values outside 0-1 tile the texture (repeat wrapping) or clamp to edge pixels. UV seams (where islands meet) should be hidden along natural edges or behind geometry.\n\n• Normal maps — Store surface angle perturbations in RGB channels (tangent-space: R=X deviation, G=Y deviation, B=Z/up). A flat surface is (128, 128, 255) in the normal map — purple/blue color. They simulate bumps without adding polygons.\n\n• Texture optimization — Use mipmaps (pre-computed smaller versions) to prevent aliasing at distance. Compress textures (BCn/DXT formats) for GPU memory savings. Channel-pack greyscale maps (metallic in R, roughness in G, AO in B of one texture).\n\n• Tiling materials — Seamless textures that repeat across surfaces. Add variation with detail maps, decals, and vertex color blending. Procedural materials (Substance Designer) generate tileable textures from node graphs.`,
    summary: "Materials define surface light response using PBR parameters (albedo, metallic, roughness, normal), while UV mapping projects 2D textures onto 3D geometry. Multiple texture maps work together to create convincing surfaces, with careful resolution budgeting and optimization for real-time performance."
  },
  {
    title: "Lighting & Rendering Basics",
    definition: "Lighting illuminates 3D scenes using simulated light sources that interact with material properties to produce realistic shading, shadows, and reflections, while rendering is the computational process of converting 3D scene data into final 2D images.",
    explanation: `Lighting in 3D is built on physics-based models of how photons interact with surfaces. Direct lighting comes from explicit light sources: directional lights (sun — parallel rays), point lights (light bulbs — radiate in all directions), spot lights (flashlights — cone-shaped), and area lights (windows — emit from a surface). Each light has intensity, color, and falloff characteristics.\n\nIndirect lighting (global illumination) accounts for light bouncing off surfaces to illuminate other surfaces. A red wall subtly tints nearby objects with red light. This multi-bounce behavior is computationally expensive — real-time engines use approximations like baked lightmaps, screen-space reflections, light probes, and irradiance volumes. Offline renderers (used in film) trace millions of light paths for photographic accuracy.\n\nShadows add crucial depth cues. Shadow maps (the most common real-time technique) render the scene from the light's perspective, storing depth values. When rendering the camera view, each pixel checks if it's farther from the light than the shadow map records — if yes, it's in shadow. Cascaded shadow maps use multiple resolutions for different distances to balance quality and performance.\n\nRendering pipelines fall into two categories: rasterization (real-time, used in games and WebGL) projects triangles onto the screen pixel-by-pixel, while ray tracing shoots rays from the camera through each pixel into the scene, naturally handling reflections, refractions, and soft shadows. Modern GPUs support both, with hybrid approaches becoming the standard.`,
    code: `// Three.js Lighting Setup\n\n// Ambient light: uniform base illumination\nconst ambient = new THREE.AmbientLight(0x404040, 0.5);\nscene.add(ambient);\n\n// Directional light: simulates sun (parallel rays)\nconst sun = new THREE.DirectionalLight(0xffffff, 1.0);\nsun.position.set(50, 100, 50);\nsun.castShadow = true;\n// Shadow map configuration\nsun.shadow.mapSize.width = 2048;\nsun.shadow.mapSize.height = 2048;\nsun.shadow.camera.near = 0.5;\nsun.shadow.camera.far = 500;\nscene.add(sun);\n\n// Point light: radiates in all directions\nconst bulb = new THREE.PointLight(0xff9900, 2.0, 10);\nbulb.position.set(0, 3, 0);\nbulb.castShadow = true;\nscene.add(bulb);\n\n// Spot light: cone-shaped beam\nconst spot = new THREE.SpotLight(0xffffff, 1.5);\nspot.position.set(0, 10, 0);\nspot.angle = Math.PI / 6;  // 30-degree cone\nspot.penumbra = 0.3;       // Soft edge falloff\nspot.decay = 2;            // Physical falloff\nspot.castShadow = true;\nscene.add(spot);\n\n// Environment map for reflections/ambient\nconst envMap = new THREE.CubeTextureLoader().load([\n    'px.jpg', 'nx.jpg', 'py.jpg',\n    'ny.jpg', 'pz.jpg', 'nz.jpg'\n]);\nscene.environment = envMap; // PBR reflection source\n\n// Renderer configuration\nconst renderer = new THREE.WebGLRenderer({\n    antialias: true\n});\nrenderer.shadowMap.enabled = true;\nrenderer.shadowMap.type = THREE.PCFSoftShadowMap;\nrenderer.toneMapping = THREE.ACESFilmicToneMapping;\nrenderer.toneMappingExposure = 1.0;\nrenderer.outputEncoding = THREE.sRGBEncoding;\n\n// Render loop\nfunction render() {\n    requestAnimationFrame(render);\n    renderer.render(scene, camera);\n}`,
    breakdown: `• Light types — Directional (sun: parallel, no falloff), Point (bulb: omnidirectional, inverse-square falloff), Spot (flashlight: cone-shaped with penumbra), Area (window: soft shadows, expensive). Combine for realistic multi-source lighting.\n\n• Shadow mapping — The renderer draws the scene from the light's viewpoint, storing depth in a texture. Higher shadow map resolution (2048+) reduces blocky shadow edges. PCF (Percentage Closer Filtering) smooths shadow boundaries.\n\n• Tone mapping — HDR lighting values (brightness > 1.0) must be mapped to displayable 0-1 range. ACES filmic curve preserves detail in highlights and shadows, mimicking film response. Exposure controls overall brightness.\n\n• Environment maps — Cubemap or equirectangular images representing surrounding environment. Used for reflections on shiny surfaces and as ambient light source (Image-Based Lighting/IBL). Captures real-world lighting for photorealistic results.\n\n• Rendering pipeline — Vertex shader transforms geometry to screen space, fragment shader calculates pixel color from lighting/materials, post-processing adds bloom, SSAO, depth of field. Each step runs on the GPU in parallel.`,
    summary: "Lighting uses physically-based light sources (directional, point, spot, area) with shadow mapping and environment maps to illuminate scenes realistically. Rendering converts 3D data to 2D images through rasterization pipelines with tone mapping, anti-aliasing, and post-processing for final image quality."
  },
  {
    title: "Exporting Models for Games/Web",
    definition: "Exporting 3D models involves converting authoring-format geometry, materials, and animations into optimized runtime formats (glTF, FBX, OBJ) with appropriate polygon counts, texture compression, and metadata for efficient loading and rendering in game engines or web browsers.",
    explanation: `The format you model in (Blender's .blend, Maya's .ma, ZBrush's .ztl) is not the format you deliver in. Export formats must be efficiently parseable by the target runtime, store all necessary data (geometry, materials, animations, skeleton), and minimize file size for distribution. The choice of format depends on your target platform and what data needs to travel with the model.\n\nglTF (GL Transmission Format) has become the standard for web 3D and increasingly for games. Called the 'JPEG of 3D,' it's designed for efficient delivery and loading. glTF files store geometry in binary buffers (ready for direct GPU upload), support PBR materials, skeletal animation, morph targets, and scene hierarchy. The binary version (.glb) packages everything in a single file, ideal for web delivery.\n\nFBX remains dominant in game development pipelines as the interchange format between 3D authoring tools and game engines (Unity, Unreal). It supports complex animation data including blend shapes, constraints, and camera animations. OBJ is a legacy format that only stores static geometry and basic materials — useful for simple props but inadequate for animated or PBR content.\n\nOptimization during export is critical. Game-ready models need LOD (Level of Detail) variants — a character might have 30,000 triangles up close but only 500 at distance. Textures must be power-of-two dimensions (512, 1024, 2048) for GPU mipmapping. Material setups must match the target engine's shader system. Proper naming conventions and clean hierarchy enable automation in production pipelines.`,
    code: `// glTF loading in Three.js\nimport { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader';\nimport { DRACOLoader } from 'three/examples/jsm/loaders/DRACOLoader';\n\n// Setup Draco decoder for compressed meshes\nconst dracoLoader = new DRACOLoader();\ndracoLoader.setDecoderPath('/draco/');\n\nconst gltfLoader = new GLTFLoader();\ngltfLoader.setDRACOLoader(dracoLoader);\n\n// Load a glTF model\ngltfLoader.load('model.glb', (gltf) => {\n    const model = gltf.scene;\n\n    // Access animations\n    const mixer = new THREE.AnimationMixer(model);\n    gltf.animations.forEach((clip) => {\n        mixer.clipAction(clip).play();\n    });\n\n    scene.add(model);\n});\n\n// Export optimization checklist (pseudocode)\nconst exportChecklist = {\n    geometry: {\n        triangulate: true,      // Convert quads/ngons to triangles\n        removeDoubles: true,    // Merge overlapping vertices\n        maxTriangles: 30000,    // Polycount budget\n        applyModifiers: true,   // Bake subdivision, mirror, etc.\n    },\n    textures: {\n        maxResolution: 2048,    // Power of two\n        format: 'PNG or JPEG',  // PNG for alpha, JPEG for opaque\n        compression: 'KTX2/Basis', // GPU-compressed for web\n        channelPack: true       // Combine R/M/AO into one texture\n    },\n    animation: {\n        bakeConstraints: true,  // Convert IK to FK keyframes\n        sampleRate: 30,         // Keyframes per second\n        removeDuplicates: true  // Delete redundant keyframes\n    },\n    scene: {\n        applyTransforms: true,  // Apply scale/rotation\n        clearUnused: true,      // Remove unused materials/bones\n        centerOrigin: true,     // Model at world origin\n        yUpAxis: true           // Match target engine convention\n    }\n};\n\n// LOD (Level of Detail) setup\n// High: 30,000 tris - within 10 meters\n// Medium: 10,000 tris - 10-30 meters\n// Low: 2,000 tris - 30-100 meters\n// Billboard: 2 tris - beyond 100 meters\nconst lod = new THREE.LOD();\nlod.addLevel(highDetailMesh, 0);    // distance 0+\nlod.addLevel(mediumDetailMesh, 10); // distance 10+\nlod.addLevel(lowDetailMesh, 30);    // distance 30+\nscene.add(lod);`,
    breakdown: `• glTF format — The open standard for 3D delivery. Binary buffers upload directly to GPU without parsing. Supports PBR materials, animations, morph targets, and extensions. .glb bundles everything; .gltf references external binary/texture files.\n\n• Draco compression — Google's geometry compression reduces mesh file size by 90%+ by quantizing vertex positions and compressing topology. Requires decoder library client-side but dramatically reduces download time.\n\n• LOD system — Multiple mesh resolutions swap based on camera distance. Dramatically improves performance for large scenes. Automatic LOD generators (Simplygon, InstaLOD, mesh decimation) reduce manual work.\n\n• Texture optimization — Power-of-two dimensions enable mipmapping. KTX2/Basis Universal provides GPU-compressed textures that decompress on the GPU, reducing both download size and VRAM usage. Channel packing combines greyscale maps into RGB channels of one texture.\n\n• Pipeline automation — Production studios use scripts to batch-export, validate (check naming, polycount, UV overlaps), generate LODs, compress textures, and deploy. Proper naming conventions (character_body_albedo_2k.png) enable this automation.`,
    summary: "Exporting 3D models requires converting authoring formats to runtime-optimized formats like glTF (.glb) with proper triangulation, texture compression (Draco, KTX2), LOD variants, and applied transforms. Understanding target platform requirements ensures models load efficiently and render correctly in games and web browsers."
  }
];
