// Pre-written spec lessons for: Shaders & Visual Effects

export const gdShadersLessons = [
  {
    title: "What are Shaders?",
    definition: "Shaders are small programs that run on the GPU (Graphics Processing Unit) rather than the CPU, executing in parallel for every vertex or pixel simultaneously to transform geometry, calculate colors, and produce the final visual output you see on screen.",
    explanation: `Shaders fundamentally changed computer graphics by moving visual calculations from the CPU to the GPU. A GPU can execute thousands of simple operations simultaneously (one for each pixel), while a CPU processes things sequentially. For a 1920x1080 display, that's over 2 million pixels that need colors calculated every frame — a task perfectly suited to the GPU's massively parallel architecture.\n\nThe term "shader" originally meant programs that calculated shading (light and shadow), but modern shaders handle all GPU-side graphics computation: transforming 3D coordinates to screen positions, determining pixel colors, applying textures, simulating lighting, creating special effects, and post-processing the final image.\n\nIn the WebGL/OpenGL pipeline, shaders are written in GLSL (OpenGL Shading Language), a C-like language designed for graphics math. You write the shader code as strings in your JavaScript, compile it on the GPU at runtime, and then the GPU executes that program for every vertex and pixel every frame. The key mental shift is that your code doesn't loop over pixels — it describes what happens to ONE pixel, and the GPU runs it millions of times in parallel.\n\nThere are two main shader types in basic graphics: vertex shaders (transform each vertex's position from 3D world space to 2D screen space) and fragment shaders (determine the final color of each pixel). These two stages work as a pipeline: vertex shader positions the geometry, then the fragment shader fills in the pixels between those vertices.`,
    code: `// WebGL shader basics - a minimal shader program\n\n// Vertex Shader: runs once per vertex\n// Transforms 3D position to 2D screen position\nconst vertexShaderSource = \`\n  // Input: vertex position from mesh data\n  attribute vec3 aPosition;\n  attribute vec2 aTexCoord;\n\n  // Output: passed to fragment shader\n  varying vec2 vTexCoord;\n\n  // Transformation matrices\n  uniform mat4 uModelMatrix;      // object transform\n  uniform mat4 uViewMatrix;       // camera transform\n  uniform mat4 uProjectionMatrix; // perspective projection\n\n  void main() {\n    // Transform vertex from local space to clip space\n    gl_Position = uProjectionMatrix * uViewMatrix\n                  * uModelMatrix * vec4(aPosition, 1.0);\n\n    // Pass texture coordinate to fragment shader\n    vTexCoord = aTexCoord;\n  }\n\`;\n\n// Fragment Shader: runs once per pixel (fragment)\n// Determines the color of each pixel\nconst fragmentShaderSource = \`\n  precision mediump float;\n\n  // Received from vertex shader (interpolated)\n  varying vec2 vTexCoord;\n\n  // Texture to sample\n  uniform sampler2D uTexture;\n  uniform float uTime; // for animation\n\n  void main() {\n    // Sample the texture at this pixel's UV coordinate\n    vec4 texColor = texture2D(uTexture, vTexCoord);\n\n    // Simple pulsing brightness effect\n    float brightness = 0.8 + 0.2 * sin(uTime * 3.0);\n    gl_FragColor = texColor * brightness;\n  }\n\`;\n\n// JavaScript: compiling and using shaders with WebGL\nfunction createShaderProgram(\n  gl: WebGLRenderingContext,\n  vertSrc: string,\n  fragSrc: string\n): WebGLProgram {\n  const vertShader = gl.createShader(gl.VERTEX_SHADER)!;\n  gl.shaderSource(vertShader, vertSrc);\n  gl.compileShader(vertShader);\n\n  if (!gl.getShaderParameter(vertShader, gl.COMPILE_STATUS)) {\n    console.error('Vertex shader error:',\n      gl.getShaderInfoLog(vertShader));\n  }\n\n  const fragShader = gl.createShader(gl.FRAGMENT_SHADER)!;\n  gl.shaderSource(fragShader, fragSrc);\n  gl.compileShader(fragShader);\n\n  if (!gl.getShaderParameter(fragShader, gl.COMPILE_STATUS)) {\n    console.error('Fragment shader error:',\n      gl.getShaderInfoLog(fragShader));\n  }\n\n  const program = gl.createProgram()!;\n  gl.attachShader(program, vertShader);\n  gl.attachShader(program, fragShader);\n  gl.linkProgram(program);\n\n  return program;\n}`,

    breakdown: `• GPU parallelism — While the CPU might take 30ms to loop through 2 million pixels sequentially, the GPU processes them all simultaneously in under 1ms because it has thousands of simple cores designed exactly for this kind of identical-operation-on-different-data workload.\n\n• Vertex vs Fragment — The pipeline has two programmable stages: vertex shaders position geometry (run once per triangle corner), then fragment shaders color pixels (run once per pixel covered by triangles). Data flows from vertex to fragment via 'varying' variables.\n\n• Uniforms vs Attributes — Uniforms are values constant for all vertices/pixels in a draw call (camera matrix, time, light position), while attributes are per-vertex data (position, normal, texture coordinate) that differ for each vertex.\n\n• GLSL language — Shader code uses C-like syntax with built-in vector math types (vec2, vec3, vec4, mat4) and functions (sin, dot, normalize, mix) optimized for graphics calculations on GPU hardware.\n\n• Compile at runtime — Shader source code is compiled by the GPU driver at runtime (not at build time), meaning compilation errors only appear when the program runs; always check gl.getShaderParameter for COMPILE_STATUS.`,
    summary: "Shaders are GPU programs that run in parallel for every vertex and pixel, transforming geometry and computing colors simultaneously. Written in GLSL and compiled at runtime, they form the programmable stages of the graphics pipeline that produce all visual output."
  },
  {
    title: "Vertex & Fragment Shaders",
    definition: "Vertex shaders transform each vertex's 3D position through model, view, and projection matrices to produce screen coordinates, while fragment shaders calculate the final color for every pixel between those vertices using interpolated data, lighting, and textures.",
    explanation: `The vertex shader is your first chance to manipulate geometry. Every vertex of every triangle in your scene passes through it. The shader receives the vertex's local-space position (relative to the object's origin) and must output a clip-space position (where it appears on screen). This transformation typically multiplies by three matrices: model (object's world position/rotation/scale), view (camera's position/rotation inverted), and projection (perspective or orthographic mapping).\n\nBut vertex shaders can do much more than just transform positions. They can animate vertices (waving grass, ocean waves, cloth simulation), calculate per-vertex lighting, pass data to the fragment shader (normals, texture coordinates, world positions), and even implement simple physics on the GPU (particle systems where each particle is a vertex).\n\nThe fragment shader (also called pixel shader in DirectX) runs after rasterization — the process of determining which pixels a triangle covers on screen. For each covered pixel, the fragment shader receives interpolated values from the triangle's three vertices and must output a final color (RGBA). The interpolation is automatic: if one vertex has UV (0,0) and another has UV (1,1), a pixel halfway between them receives UV (0.5, 0.5).\n\nFragment shaders handle the most visually impactful work: texture sampling (reading pixel colors from images), lighting calculations (how bright is this pixel based on light sources?), shadow mapping, reflections, transparency, and virtually all visual effects. Because they run for every pixel (potentially millions per frame), fragment shader performance is critical — complex fragment shaders are the most common cause of GPU-bound frame rate drops.`,
    code: `// Advanced vertex and fragment shaders\n\n// Vertex shader with animation and lighting prep\nconst advancedVertexShader = \`\n  attribute vec3 aPosition;\n  attribute vec3 aNormal;\n  attribute vec2 aTexCoord;\n\n  uniform mat4 uModel;\n  uniform mat4 uView;\n  uniform mat4 uProjection;\n  uniform mat3 uNormalMatrix; // inverse transpose of model\n  uniform float uTime;\n\n  varying vec3 vWorldPos;\n  varying vec3 vNormal;\n  varying vec2 vTexCoord;\n\n  void main() {\n    // Animate vertices: wave effect on Y axis\n    vec3 pos = aPosition;\n    pos.y += sin(pos.x * 2.0 + uTime * 3.0) * 0.1;\n\n    // Calculate world-space position (for lighting)\n    vec4 worldPos = uModel * vec4(pos, 1.0);\n    vWorldPos = worldPos.xyz;\n\n    // Transform normal to world space\n    vNormal = normalize(uNormalMatrix * aNormal);\n\n    // Pass UV coordinates\n    vTexCoord = aTexCoord;\n\n    // Final clip-space position\n    gl_Position = uProjection * uView * worldPos;\n  }\n\`;\n\n// Fragment shader with texture + per-pixel lighting\nconst advancedFragmentShader = \`\n  precision highp float;\n\n  varying vec3 vWorldPos;\n  varying vec3 vNormal;\n  varying vec2 vTexCoord;\n\n  uniform sampler2D uDiffuseMap;\n  uniform vec3 uLightPos;\n  uniform vec3 uLightColor;\n  uniform vec3 uCameraPos;\n  uniform float uShininess;\n\n  void main() {\n    // Sample texture\n    vec4 texColor = texture2D(uDiffuseMap, vTexCoord);\n\n    // Normalize interpolated normal\n    vec3 normal = normalize(vNormal);\n\n    // Light direction\n    vec3 lightDir = normalize(uLightPos - vWorldPos);\n\n    // Diffuse lighting (Lambert)\n    float diffuse = max(dot(normal, lightDir), 0.0);\n\n    // Specular lighting (Blinn-Phong)\n    vec3 viewDir = normalize(uCameraPos - vWorldPos);\n    vec3 halfDir = normalize(lightDir + viewDir);\n    float specular = pow(\n      max(dot(normal, halfDir), 0.0),\n      uShininess\n    );\n\n    // Combine: ambient + diffuse + specular\n    vec3 ambient = texColor.rgb * 0.15;\n    vec3 diff = texColor.rgb * diffuse * uLightColor;\n    vec3 spec = uLightColor * specular * 0.5;\n\n    gl_FragColor = vec4(ambient + diff + spec, texColor.a);\n  }\n\`;\n\n// Setting up uniforms from JavaScript\nfunction renderMesh(\n  gl: WebGLRenderingContext,\n  program: WebGLProgram,\n  mesh: Mesh,\n  camera: Camera3D,\n  light: PointLight\n): void {\n  gl.useProgram(program);\n\n  // Set matrix uniforms\n  const modelLoc = gl.getUniformLocation(program, 'uModel');\n  gl.uniformMatrix4fv(modelLoc, false, mesh.modelMatrix);\n\n  const viewLoc = gl.getUniformLocation(program, 'uView');\n  gl.uniformMatrix4fv(viewLoc, false, camera.viewMatrix);\n\n  const projLoc = gl.getUniformLocation(program, 'uProjection');\n  gl.uniformMatrix4fv(projLoc, false, camera.projectionMatrix);\n\n  // Set light uniforms\n  const lightPosLoc = gl.getUniformLocation(program, 'uLightPos');\n  gl.uniform3fv(lightPosLoc, light.position);\n\n  // Draw the mesh\n  gl.drawElements(gl.TRIANGLES, mesh.indexCount, gl.UNSIGNED_SHORT, 0);\n}`,
    breakdown: `• Matrix pipeline — Model matrix places the object in the world, view matrix represents the camera's perspective, and projection matrix creates depth (perspective) or flat (orthographic) projection; multiplied together they map any 3D point to screen position.\n\n• Normal matrix — The inverse transpose of the model matrix correctly transforms surface normals under non-uniform scaling; using the model matrix directly would distort normals when objects are stretched.\n\n• Varying interpolation — Values output from vertex shaders (vWorldPos, vNormal, vTexCoord) are automatically interpolated across the triangle surface for each pixel, providing smooth gradients of position, normal, and UV data.\n\n• Per-pixel vs per-vertex lighting — Computing lighting in the fragment shader (per-pixel) produces smooth, accurate lighting even on low-poly geometry, unlike per-vertex lighting which can show triangle edges.\n\n• Vertex animation — Modifying vertex positions in the shader (like the wave effect) runs entirely on the GPU, enabling thousands of animated vertices without any CPU cost; perfect for grass, water, and cloth.`,
    summary: "Vertex shaders transform geometry from 3D world space to 2D screen space and can animate vertices on the GPU. Fragment shaders compute per-pixel colors using interpolated data from vertices, enabling smooth lighting, texture mapping, and visual effects at full resolution."
  },

  {
    title: "UV Mapping & Textures",
    definition: "UV mapping assigns 2D texture coordinates (U horizontal, V vertical) to each vertex of a 3D mesh, defining how a flat image wraps around the geometry so that fragment shaders can sample the correct color from the texture at each pixel.",
    explanation: `Textures are the images applied to 3D surfaces to give them visual detail — wood grain on a crate, bricks on a wall, skin on a character. But the GPU needs to know which part of the image corresponds to which part of the geometry. This is the job of UV coordinates: a 2D coordinate system (U = horizontal, V = vertical, ranging from 0 to 1) that maps points on the texture to points on the mesh.\n\nEach vertex in a mesh has a UV coordinate pair alongside its position and normal. When the fragment shader runs for a pixel between three vertices, the UV coordinates are interpolated, giving each pixel a unique texture lookup position. The shader then calls texture2D(sampler, uv) to read the color from the texture at that interpolated coordinate.\n\nTexture sampling involves important GPU settings: filtering (how to interpolate between texture pixels when zoomed in or out), wrapping mode (what happens when UVs go below 0 or above 1 — repeat, clamp, or mirror), and mipmapping (pre-generated smaller versions of the texture used when viewed from far away, preventing aliasing and improving performance).\n\nIn practice, textures serve many purposes beyond just color. Normal maps store surface detail that affects lighting without adding geometry. Specular/roughness maps control how shiny each pixel is. Emissive maps define self-glowing areas. All of these are just images sampled with the same UV coordinates but interpreted differently by the shader — their RGB values represent normals, roughness, or glow intensity rather than visible color.`,
    code: `// UV mapping and texture sampling in shaders\n\n// Fragment shader with multiple texture maps\nconst texturedFragShader = \`\n  precision highp float;\n\n  varying vec2 vTexCoord;\n  varying vec3 vNormal;\n  varying vec3 vWorldPos;\n  varying vec3 vTangent;\n\n  // Multiple texture maps\n  uniform sampler2D uDiffuseMap;   // base color\n  uniform sampler2D uNormalMap;    // surface detail normals\n  uniform sampler2D uRoughnessMap; // shininess variation\n  uniform sampler2D uEmissiveMap;  // self-illumination\n\n  uniform vec3 uLightPos;\n  uniform vec3 uCameraPos;\n\n  void main() {\n    // Sample all texture maps at same UV\n    vec4 diffuse = texture2D(uDiffuseMap, vTexCoord);\n    vec3 normalMap = texture2D(uNormalMap, vTexCoord).rgb;\n    float roughness = texture2D(uRoughnessMap, vTexCoord).r;\n    vec3 emissive = texture2D(uEmissiveMap, vTexCoord).rgb;\n\n    // Convert normal map from [0,1] to [-1,1] range\n    vec3 mappedNormal = normalMap * 2.0 - 1.0;\n\n    // Transform normal map to world space (TBN matrix)\n    vec3 N = normalize(vNormal);\n    vec3 T = normalize(vTangent);\n    vec3 B = cross(N, T);\n    mat3 TBN = mat3(T, B, N);\n    vec3 worldNormal = normalize(TBN * mappedNormal);\n\n    // Lighting with normal-mapped surface\n    vec3 lightDir = normalize(uLightPos - vWorldPos);\n    float diff = max(dot(worldNormal, lightDir), 0.0);\n\n    // Roughness affects specular sharpness\n    float shininess = mix(4.0, 128.0, 1.0 - roughness);\n    vec3 viewDir = normalize(uCameraPos - vWorldPos);\n    vec3 halfVec = normalize(lightDir + viewDir);\n    float spec = pow(max(dot(worldNormal, halfVec), 0.0), shininess);\n\n    // Final color: diffuse + specular + emissive glow\n    vec3 color = diffuse.rgb * (0.1 + diff * 0.9)\n               + vec3(1.0) * spec * (1.0 - roughness)\n               + emissive * 2.0;\n\n    gl_FragColor = vec4(color, diffuse.a);\n  }\n\`;\n\n// JavaScript: loading and configuring textures for WebGL\nasync function loadTexture(\n  gl: WebGLRenderingContext,\n  url: string,\n  options: {\n    wrapS?: number; // gl.REPEAT, gl.CLAMP_TO_EDGE\n    wrapT?: number;\n    filter?: number; // gl.LINEAR, gl.NEAREST\n    mipmap?: boolean;\n  } = {}\n): Promise<WebGLTexture> {\n  const texture = gl.createTexture()!;\n  const image = await loadImage(url);\n\n  gl.bindTexture(gl.TEXTURE_2D, texture);\n  gl.texImage2D(\n    gl.TEXTURE_2D, 0, gl.RGBA, gl.RGBA, gl.UNSIGNED_BYTE, image\n  );\n\n  // Wrapping: what happens outside UV range [0,1]\n  gl.texParameteri(\n    gl.TEXTURE_2D, gl.TEXTURE_WRAP_S,\n    options.wrapS ?? gl.REPEAT\n  );\n  gl.texParameteri(\n    gl.TEXTURE_2D, gl.TEXTURE_WRAP_T,\n    options.wrapT ?? gl.REPEAT\n  );\n\n  // Filtering: how pixels are interpolated\n  gl.texParameteri(\n    gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER,\n    options.mipmap ? gl.LINEAR_MIPMAP_LINEAR : gl.LINEAR\n  );\n  gl.texParameteri(\n    gl.TEXTURE_2D, gl.TEXTURE_MAG_FILTER,\n    options.filter ?? gl.LINEAR\n  );\n\n  // Generate mipmaps for efficient distant rendering\n  if (options.mipmap) {\n    gl.generateMipmap(gl.TEXTURE_2D);\n  }\n\n  return texture;\n}\n\n// Tiling UVs for repeated patterns (brick wall, grass)\n// In the vertex shader or CPU, multiply UVs:\n// vTexCoord = aTexCoord * vec2(4.0, 4.0); // repeat 4x4`,
    breakdown: `• UV coordinate range — UVs typically range from 0 to 1, mapping to the full texture image; values outside this range trigger the wrap mode (repeat creates tiling, clamp stretches edge pixels).\n\n• Normal mapping — Rather than storing surface colors, normal maps store per-pixel surface direction (encoded as RGB where XYZ maps to 0-1 range), faking detailed geometry that catches light realistically without extra vertices.\n\n• TBN matrix — The Tangent-Bitangent-Normal matrix transforms normal map vectors from tangent space (relative to the texture) to world space (relative to the scene), enabling correct lighting on rotated or curved surfaces.\n\n• Mipmaps — Pre-computed half-size versions of textures (512→256→128→64→...) are automatically selected based on pixel-to-texel ratio, eliminating shimmering artifacts on distant surfaces and improving cache performance.\n\n• Multi-texture workflow — Modern materials use 4+ texture maps per surface (diffuse, normal, roughness, metallic, emissive, occlusion), all sampled at the same UV coordinate but interpreted as different physical properties by the shader.`,
    summary: "UV mapping defines how 2D textures wrap around 3D geometry by assigning texture coordinates to each vertex. Fragment shaders sample multiple texture maps (color, normal, roughness) at interpolated UVs to produce detailed, realistic surfaces from simple geometry."
  },
  {
    title: "Lighting Models (Phong, PBR)",
    definition: "Lighting models are mathematical formulas that shaders use to calculate how light interacts with surfaces, ranging from the simple empirical Phong model (ambient + diffuse + specular) to physically-based rendering (PBR) that simulates real-world light behavior using energy conservation and microfacet theory.",
    explanation: `The Phong lighting model, introduced in 1975, decomposes light-surface interaction into three components: ambient (constant base illumination so nothing is pure black), diffuse (light scattered equally in all directions, based on surface angle to light via dot product), and specular (bright highlight reflection, based on view angle relative to reflection direction). While not physically accurate, Phong is intuitive, cheap to compute, and still widely used in stylized games.\n\nBlinn-Phong is a popular modification that replaces the reflection vector calculation with a half-vector (halfway between light and view direction). This is cheaper to compute and produces more realistic highlights at grazing angles. Most "Phong" implementations in games actually use Blinn-Phong.\n\nPhysically-Based Rendering (PBR) replaced Phong as the industry standard for realistic graphics around 2013 (Unreal Engine 4, Frostbite). PBR is built on two principles: energy conservation (a surface cannot reflect more light than it receives) and microfacet theory (rough surfaces are modeled as collections of tiny perfect mirrors oriented at various angles).\n\nPBR uses two main parameters: metalness (is the surface a metal or non-metal, affecting whether it tints reflections) and roughness (how blurred/sharp reflections appear). The Cook-Torrance specular model combines a distribution function (how microfacets are oriented), a geometry function (how microfacets shadow each other), and a Fresnel term (surfaces reflect more at grazing angles — like water appearing mirror-like at shallow angles). PBR looks correct under any lighting because it's based on physics, not artistic approximation.`,
    code: `// Lighting models: Phong and PBR fragment shaders\n\n// BLINN-PHONG lighting model\nconst phongShader = \`\n  precision highp float;\n\n  varying vec3 vNormal;\n  varying vec3 vWorldPos;\n\n  uniform vec3 uLightPos;\n  uniform vec3 uLightColor;\n  uniform vec3 uCameraPos;\n  uniform vec3 uAmbientColor;\n  uniform float uShininess;    // 16 = rough, 256 = mirror-like\n  uniform vec3 uDiffuseColor;\n  uniform vec3 uSpecularColor;\n\n  void main() {\n    vec3 N = normalize(vNormal);\n    vec3 L = normalize(uLightPos - vWorldPos);\n    vec3 V = normalize(uCameraPos - vWorldPos);\n    vec3 H = normalize(L + V); // half vector (Blinn-Phong)\n\n    // Ambient: constant minimum light\n    vec3 ambient = uAmbientColor * uDiffuseColor;\n\n    // Diffuse: Lambert cosine law\n    float NdotL = max(dot(N, L), 0.0);\n    vec3 diffuse = uDiffuseColor * uLightColor * NdotL;\n\n    // Specular: Blinn-Phong highlight\n    float NdotH = max(dot(N, H), 0.0);\n    float spec = pow(NdotH, uShininess);\n    vec3 specular = uSpecularColor * uLightColor * spec;\n\n    gl_FragColor = vec4(ambient + diffuse + specular, 1.0);\n  }\n\`;\n\n// PHYSICALLY-BASED RENDERING (Cook-Torrance)\nconst pbrShader = \`\n  precision highp float;\n\n  varying vec3 vNormal;\n  varying vec3 vWorldPos;\n  varying vec2 vTexCoord;\n\n  uniform vec3 uLightPos;\n  uniform vec3 uLightColor;\n  uniform vec3 uCameraPos;\n  uniform sampler2D uAlbedoMap;\n  uniform float uMetalness;\n  uniform float uRoughness;\n\n  const float PI = 3.14159265359;\n\n  // Normal Distribution Function (GGX/Trowbridge-Reitz)\n  float distributionGGX(vec3 N, vec3 H, float roughness) {\n    float a = roughness * roughness;\n    float a2 = a * a;\n    float NdotH = max(dot(N, H), 0.0);\n    float NdotH2 = NdotH * NdotH;\n    float denom = NdotH2 * (a2 - 1.0) + 1.0;\n    return a2 / (PI * denom * denom);\n  }\n\n  // Geometry function (Smith's method with Schlick-GGX)\n  float geometrySmith(vec3 N, vec3 V, vec3 L, float roughness) {\n    float r = roughness + 1.0;\n    float k = (r * r) / 8.0;\n    float NdotV = max(dot(N, V), 0.0);\n    float NdotL = max(dot(N, L), 0.0);\n    float ggx1 = NdotV / (NdotV * (1.0 - k) + k);\n    float ggx2 = NdotL / (NdotL * (1.0 - k) + k);\n    return ggx1 * ggx2;\n  }\n\n  // Fresnel-Schlick approximation\n  vec3 fresnelSchlick(float cosTheta, vec3 F0) {\n    return F0 + (1.0 - F0) * pow(1.0 - cosTheta, 5.0);\n  }\n\n  void main() {\n    vec3 albedo = texture2D(uAlbedoMap, vTexCoord).rgb;\n    vec3 N = normalize(vNormal);\n    vec3 V = normalize(uCameraPos - vWorldPos);\n    vec3 L = normalize(uLightPos - vWorldPos);\n    vec3 H = normalize(V + L);\n\n    // Base reflectivity (metals tint reflections with albedo)\n    vec3 F0 = mix(vec3(0.04), albedo, uMetalness);\n\n    // Cook-Torrance specular BRDF\n    float D = distributionGGX(N, H, uRoughness);\n    float G = geometrySmith(N, V, L, uRoughness);\n    vec3 F = fresnelSchlick(max(dot(H, V), 0.0), F0);\n\n    vec3 numerator = D * G * F;\n    float denominator = 4.0 * max(dot(N, V), 0.0)\n                            * max(dot(N, L), 0.0) + 0.0001;\n    vec3 specular = numerator / denominator;\n\n    // Energy conservation: what isn't reflected is diffused\n    vec3 kD = (vec3(1.0) - F) * (1.0 - uMetalness);\n\n    // Final: diffuse + specular\n    float NdotL = max(dot(N, L), 0.0);\n    vec3 color = (kD * albedo / PI + specular) * uLightColor * NdotL;\n\n    // Add ambient (simplified; real PBR uses IBL)\n    color += albedo * 0.03;\n\n    // Tone mapping (HDR to LDR)\n    color = color / (color + vec3(1.0));\n\n    gl_FragColor = vec4(color, 1.0);\n  }\n\`;\n\n// In JavaScript, set PBR material parameters:\n// gl.uniform1f(roughnessLoc, 0.3);  // smooth surface\n// gl.uniform1f(metalnessLoc, 0.0);  // non-metal (plastic, wood)\n// gl.uniform1f(metalnessLoc, 1.0);  // metal (gold, steel)`,
    breakdown: `• Phong simplicity — Three intuitive components (ambient + diffuse + specular) with a single shininess exponent make Phong easy to implement and tune; ideal for stylized games or mobile where performance matters more than realism.\n\n• Energy conservation in PBR — The line (1.0 - F) ensures that light energy reflected as specular isn't also counted as diffuse; total outgoing light never exceeds incoming light, which is physically correct.\n\n• GGX distribution — Models how microfacets are oriented; high roughness means microfacets point in many directions (blurred highlight), low roughness means they're mostly aligned (sharp highlight). This produces more realistic highlights than Phong's power function.\n\n• Fresnel effect — fresnelSchlick models the observation that surfaces reflect more light at grazing angles; even rough non-metals become mirror-like at extreme angles (look at a table surface at eye level).\n\n• Metalness workflow — Non-metals (F0=0.04) have white specular and colored diffuse; metals (F0=albedo color) have colored specular and no diffuse. This binary distinction simplifies material authoring enormously.`,
    summary: "Phong lighting combines ambient, diffuse, and specular components using simple dot products. PBR replaces this with physically-accurate microfacet models (GGX distribution, Smith geometry, Fresnel reflection) that conserve energy and produce realistic results under any lighting condition."
  },

  {
    title: "Post-Processing Effects",
    definition: "Post-processing effects are shader passes applied to the entire rendered frame after all 3D geometry has been drawn, treating the finished image as a texture and applying screen-space transformations like bloom, color grading, depth of field, vignette, and motion blur.",
    explanation: `Post-processing transforms a game's visual quality from "basic 3D render" to "cinematic." After the scene is rendered normally to a framebuffer (an off-screen texture), one or more full-screen shader passes read that texture and output a modified version. Each pass is just a fragment shader that processes every pixel of the screen-sized texture.\n\nThe process works like photo filters: the 3D scene is your "photograph," and post-processing applies Instagram-like effects to the whole image. Unlike per-object shaders that need 3D geometry information, post-processing operates purely in 2D screen space. This makes effects like bloom, blur, and color grading independent of scene complexity — they cost the same whether your scene has 10 or 10,000 objects.\n\nCommon post-processing effects include: bloom (bright areas bleed light into surrounding pixels, simulating camera overexposure), depth of field (near/far objects blur like a camera lens), motion blur (fast-moving objects smear in their motion direction), color grading (shift the entire color palette for artistic mood), vignette (darken screen edges), chromatic aberration (color channel separation at edges), and film grain (subtle noise overlay).\n\nPost-processing typically chains multiple passes: render scene to framebuffer A, apply bloom to A writing to B, apply color grading to B writing to A, apply vignette to A writing to the screen. Each pass reads one texture and writes another. For effects like bloom that need blurring, additional passes at half-resolution or quarter-resolution improve performance (blur is expensive and benefits from working on fewer pixels).`,
    code: `// Post-processing pipeline with bloom and color grading\n\n// Step 1: Render scene to framebuffer (not screen)\nfunction setupFramebuffer(\n  gl: WebGLRenderingContext,\n  width: number,\n  height: number\n): { fbo: WebGLFramebuffer; texture: WebGLTexture } {\n  const fbo = gl.createFramebuffer()!;\n  const texture = gl.createTexture()!;\n\n  gl.bindTexture(gl.TEXTURE_2D, texture);\n  gl.texImage2D(\n    gl.TEXTURE_2D, 0, gl.RGBA,\n    width, height, 0, gl.RGBA, gl.UNSIGNED_BYTE, null\n  );\n  gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, gl.LINEAR);\n  gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);\n  gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);\n\n  gl.bindFramebuffer(gl.FRAMEBUFFER, fbo);\n  gl.framebufferTexture2D(\n    gl.FRAMEBUFFER, gl.COLOR_ATTACHMENT0,\n    gl.TEXTURE_2D, texture, 0\n  );\n\n  return { fbo, texture };\n}\n\n// Bloom: extract bright pixels, blur them, add back\nconst bloomBrightPassShader = \`\n  precision highp float;\n  varying vec2 vTexCoord;\n  uniform sampler2D uScene;\n  uniform float uThreshold;\n\n  void main() {\n    vec4 color = texture2D(uScene, vTexCoord);\n    // Calculate luminance\n    float brightness = dot(color.rgb, vec3(0.2126, 0.7152, 0.0722));\n    // Only keep pixels brighter than threshold\n    if (brightness > uThreshold) {\n      gl_FragColor = color;\n    } else {\n      gl_FragColor = vec4(0.0);\n    }\n  }\n\`;\n\nconst gaussianBlurShader = \`\n  precision highp float;\n  varying vec2 vTexCoord;\n  uniform sampler2D uImage;\n  uniform vec2 uDirection; // (1,0) for horizontal, (0,1) for vertical\n  uniform vec2 uResolution;\n\n  void main() {\n    vec2 texelSize = 1.0 / uResolution;\n    vec4 result = vec4(0.0);\n\n    // 9-tap Gaussian kernel\n    float weights[5];\n    weights[0] = 0.227027;\n    weights[1] = 0.1945946;\n    weights[2] = 0.1216216;\n    weights[3] = 0.054054;\n    weights[4] = 0.016216;\n\n    result += texture2D(uImage, vTexCoord) * weights[0];\n\n    for (int i = 1; i < 5; i++) {\n      vec2 offset = uDirection * texelSize * float(i) * 2.0;\n      result += texture2D(uImage, vTexCoord + offset) * weights[i];\n      result += texture2D(uImage, vTexCoord - offset) * weights[i];\n    }\n\n    gl_FragColor = result;\n  }\n\`;\n\n// Color grading and tone mapping\nconst colorGradingShader = \`\n  precision highp float;\n  varying vec2 vTexCoord;\n  uniform sampler2D uScene;\n  uniform sampler2D uBloom;\n  uniform float uBloomIntensity;\n  uniform float uExposure;\n  uniform float uContrast;\n  uniform float uSaturation;\n  uniform vec3 uColorTint;\n  uniform float uVignetteStrength;\n\n  vec3 adjustSaturation(vec3 color, float saturation) {\n    float grey = dot(color, vec3(0.2126, 0.7152, 0.0722));\n    return mix(vec3(grey), color, saturation);\n  }\n\n  void main() {\n    // Combine scene with bloom\n    vec3 scene = texture2D(uScene, vTexCoord).rgb;\n    vec3 bloom = texture2D(uBloom, vTexCoord).rgb;\n    vec3 color = scene + bloom * uBloomIntensity;\n\n    // Exposure (simulate camera exposure)\n    color *= uExposure;\n\n    // Reinhard tone mapping (HDR to LDR)\n    color = color / (color + vec3(1.0));\n\n    // Contrast adjustment\n    color = (color - 0.5) * uContrast + 0.5;\n\n    // Saturation\n    color = adjustSaturation(color, uSaturation);\n\n    // Color tint\n    color *= uColorTint;\n\n    // Vignette (darken edges)\n    vec2 center = vTexCoord - 0.5;\n    float dist = length(center);\n    float vignette = 1.0 - dist * dist * uVignetteStrength;\n    color *= vignette;\n\n    // Gamma correction (linear to sRGB)\n    color = pow(color, vec3(1.0 / 2.2));\n\n    gl_FragColor = vec4(color, 1.0);\n  }\n\`;\n\n// Post-processing render pass execution order\nclass PostProcessPipeline {\n  render(gl: WebGLRenderingContext): void {\n    // 1. Scene already rendered to sceneFramebuffer\n    // 2. Extract bright pixels -> bloomBrightBuffer\n    this.renderPass(gl, this.brightPassProgram, this.sceneTex);\n    // 3. Horizontal blur at half resolution\n    this.renderPass(gl, this.blurProgram, this.brightTex, { dir: [1, 0] });\n    // 4. Vertical blur\n    this.renderPass(gl, this.blurProgram, this.blurHTex, { dir: [0, 1] });\n    // 5. Combine + color grade + output to screen\n    this.renderFinal(gl, this.colorGradeProgram, this.sceneTex, this.bloomTex);\n  }\n}`,
    breakdown: `• Framebuffer objects (FBOs) — Instead of rendering directly to the screen, the scene is drawn to an off-screen texture; this texture then becomes input for post-processing shaders, enabling the "render, then modify" pipeline.\n\n• Bloom pipeline — Three passes: extract bright pixels (luminance threshold), blur the result with Gaussian (two separate horizontal+vertical passes for efficiency), then additively blend back with the original scene.\n\n• Separable blur — A 2D Gaussian blur is separated into two 1D passes (horizontal then vertical), reducing texture samples from N² to 2N and making large blur radii affordable.\n\n• Tone mapping — HDR (high dynamic range) color values above 1.0 are compressed into displayable [0,1] range using Reinhard (x/(x+1)); this prevents bright areas from clipping to white and preserves detail.\n\n• Effect stacking — Post-processing passes chain sequentially (ping-ponging between two framebuffers), with each pass reading the previous result; the order matters — bloom before color grading produces different results than the reverse.`,
    summary: "Post-processing applies full-screen shader effects to the finished rendered image, enabling cinematic effects like bloom, color grading, blur, and vignette. The framebuffer ping-pong pipeline chains multiple passes efficiently, transforming basic 3D renders into polished visual experiences."
  }
];
