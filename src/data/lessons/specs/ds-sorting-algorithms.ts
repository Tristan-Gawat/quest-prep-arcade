// Pre-written spec lessons for: Sorting Algorithms

export const dsSortingAlgorithmsLessons = [
  {
    title: "Bubble Sort & Selection Sort",
    definition: "Bubble sort and selection sort are simple comparison-based sorting algorithms with O(n²) average and worst-case time complexity. Bubble sort repeatedly swaps adjacent elements that are out of order until the array is sorted. Selection sort repeatedly finds the minimum element from the unsorted portion and places it at the beginning.",

    explanation: `Bubble sort works by making multiple passes through the array. On each pass, it compares adjacent elements and swaps them if they're in the wrong order. After the first pass, the largest element "bubbles up" to its correct position at the end. After the second pass, the second-largest is in place, and so on. The algorithm terminates when a complete pass makes no swaps.\n\nBubble sort's time complexity is O(n²) in the average and worst cases because it makes roughly n passes, each examining up to n elements. However, it has a best case of O(n) when the array is already sorted (with the optimization of tracking whether any swaps occurred). Space complexity is O(1) — it sorts in place.\n\nSelection sort divides the array into sorted and unsorted portions. It repeatedly scans the unsorted portion to find the minimum element, then swaps it with the first unsorted element. After i iterations, the first i elements are in their final sorted position.\n\nSelection sort always makes exactly n(n-1)/2 comparisons regardless of input order, giving O(n²) in all cases (best, average, worst). However, it makes at most n-1 swaps, which is optimal. This makes selection sort preferable when swap cost is high (e.g., large records with cheap comparison keys).\n\nBoth algorithms are primarily educational — they're simple to understand and implement but inefficient for large datasets. In practice, they're only useful for very small arrays (< 20 elements) where their simplicity and low overhead can outweigh the asymptotic disadvantage.`,

    code: `// Bubble Sort with optimization\nfunction bubbleSort(arr) {\n    const n = arr.length;\n    for (let i = 0; i < n - 1; i++) {\n        let swapped = false;\n        // Each pass bubbles the next largest to the end\n        for (let j = 0; j < n - 1 - i; j++) {\n            if (arr[j] > arr[j + 1]) {\n                [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];\n                swapped = true;\n            }\n        }\n        // If no swaps occurred, array is sorted\n        if (!swapped) break;\n    }\n    return arr;\n}\n\n// Selection Sort\nfunction selectionSort(arr) {\n    const n = arr.length;\n    for (let i = 0; i < n - 1; i++) {\n        // Find minimum in unsorted portion\n        let minIdx = i;\n        for (let j = i + 1; j < n; j++) {\n            if (arr[j] < arr[minIdx]) {\n                minIdx = j;\n            }\n        }\n        // Swap minimum with first unsorted element\n        if (minIdx !== i) {\n            [arr[i], arr[minIdx]] = [arr[minIdx], arr[i]];\n        }\n    }\n    return arr;\n}\n\n// Example:\nconsole.log(bubbleSort([64, 34, 25, 12, 22, 11, 90]));\n// Output: [11, 12, 22, 25, 34, 64, 90]\n\nconsole.log(selectionSort([64, 34, 25, 12, 22, 11, 90]));\n// Output: [11, 12, 22, 25, 34, 64, 90]`,

    breakdown: `• Bubble sort mechanism — Compares adjacent pairs and swaps if out of order. Each pass guarantees the next-largest element reaches its final position. The "swapped" flag optimization gives O(n) best case for already-sorted input.\n\n• Selection sort mechanism — Finds the minimum element in the unsorted portion and swaps it into position. Always makes O(n²) comparisons but only O(n) swaps, making it efficient when writes are expensive.\n\n• Stability — Bubble sort is stable (equal elements maintain their relative order). Selection sort is NOT stable because swapping can move equal elements past each other. Stability matters when sorting by multiple keys.\n\n• Space complexity — Both are in-place algorithms using O(1) extra space. This is their primary advantage — no additional memory allocation needed regardless of input size.\n\n• When to use — Only for very small arrays (n < 20), educational purposes, or when code simplicity is paramount. For any real workload, use O(n log n) algorithms like merge sort or quick sort. Insertion sort is generally preferred over both for small arrays due to fewer comparisons in practice.`,
    summary: "Bubble sort and selection sort are O(n²) algorithms useful for learning fundamental sorting concepts. Bubble sort swaps adjacent elements and can detect pre-sorted input; selection sort minimizes swaps by finding the minimum each pass. Both are impractical for large datasets but illustrate key sorting principles like stability, in-place operation, and optimization."
  },

  {
    title: "Merge Sort (Divide & Conquer)",
    definition: "Merge sort is an efficient, stable, comparison-based sorting algorithm that uses the divide-and-conquer strategy. It recursively divides the array into halves until each sub-array has one element, then merges the sorted halves back together. It guarantees O(n log n) time complexity in all cases.",
    explanation: `Merge sort embodies the divide-and-conquer paradigm perfectly. The algorithm has three steps: (1) Divide the array into two halves. (2) Recursively sort each half. (3) Merge the two sorted halves into a single sorted array.\n\nThe key insight is that merging two already-sorted arrays is efficient — it takes O(n) time. You simply compare the front elements of both arrays and take the smaller one, advancing that array's pointer. When one array is exhausted, append the remainder of the other.\n\nThe recursion creates a tree of depth log₂(n), since we halve the array at each level. At each level of the tree, the total work for merging is O(n) (each element is compared and moved exactly once per level). Therefore, total time = O(n) × O(log n) = O(n log n).\n\nMerge sort guarantees O(n log n) in ALL cases — best, average, and worst. Unlike quick sort, its performance doesn't depend on the input distribution or pivot selection. This predictability makes it ideal for external sorting (sorting data that doesn't fit in memory) and linked list sorting.\n\nThe main drawback is O(n) space complexity — merge sort needs a temporary array for merging. This makes it less cache-friendly than in-place algorithms and unsuitable for memory-constrained environments. However, for linked lists, merge sort is ideal because merging linked lists requires no extra space (just pointer manipulation).`,

    code: `// Merge Sort implementation\nfunction mergeSort(arr) {\n    // Base case: array of 0 or 1 elements is already sorted\n    if (arr.length <= 1) return arr;\n\n    // Divide: split array into two halves\n    const mid = Math.floor(arr.length / 2);\n    const left = mergeSort(arr.slice(0, mid));\n    const right = mergeSort(arr.slice(mid));\n\n    // Conquer: merge the sorted halves\n    return merge(left, right);\n}\n\n// Merge two sorted arrays into one sorted array\nfunction merge(left, right) {\n    const result = [];\n    let i = 0, j = 0;\n\n    // Compare front elements, take the smaller one\n    while (i < left.length && j < right.length) {\n        if (left[i] <= right[j]) {\n            result.push(left[i]);\n            i++;\n        } else {\n            result.push(right[j]);\n            j++;\n        }\n    }\n\n    // Append remaining elements (one array is exhausted)\n    while (i < left.length) result.push(left[i++]);\n    while (j < right.length) result.push(right[j++]);\n\n    return result;\n}\n\n// Example trace for [38, 27, 43, 3]:\n// Split: [38, 27] and [43, 3]\n// Split: [38], [27] and [43], [3]\n// Merge: [27, 38] and [3, 43]\n// Merge: [3, 27, 38, 43]\n\nconsole.log(mergeSort([38, 27, 43, 3, 9, 82, 10]));\n// Output: [3, 9, 10, 27, 38, 43, 82]`,

    breakdown: `• Divide and conquer — The array is recursively halved (O(log n) levels), and at each level, O(n) total work is done for merging. This gives O(n log n) time regardless of input order — the algorithm always divides and always merges.\n\n• The merge operation — Merging two sorted arrays takes O(n) time and O(n) space. It uses two pointers comparing front elements and building the result array. The merge preserves stability since equal elements from the left array are taken first.\n\n• Space complexity — Merge sort requires O(n) auxiliary space for the temporary merge arrays. The recursion stack adds O(log n) space. Total: O(n). This is the main disadvantage compared to in-place algorithms like quick sort.\n\n• Stability — Merge sort is stable: equal elements maintain their original relative order. This is guaranteed because during merging, when elements are equal, we take from the left array first. Critical for multi-key sorting.\n\n• Practical applications — Merge sort is used for external sorting (sorting files larger than RAM, since sequential access patterns suit disk I/O), sorting linked lists (no random access needed, O(1) extra space for lists), and as the basis for Timsort (Python/Java's default sort algorithm).`,
    summary: "Merge sort is a stable, O(n log n) guaranteed algorithm that divides the array into halves, recursively sorts each half, and merges them. It excels at sorting linked lists and external data, with the tradeoff of O(n) extra space. Its predictable performance makes it a cornerstone algorithm in computer science."
  },

  {
    title: "Quick Sort & Partitioning",
    definition: "Quick sort is a highly efficient, in-place, comparison-based sorting algorithm that uses the divide-and-conquer strategy. It works by selecting a 'pivot' element, partitioning the array so all elements less than the pivot come before it and all greater elements come after, then recursively sorting the partitions.",
    explanation: `Quick sort's core operation is partitioning. Choose a pivot element, then rearrange the array so that elements smaller than the pivot are on the left and elements larger are on the right. The pivot is now in its final sorted position. Then recursively quick sort the left and right partitions.\n\nThe Lomuto partition scheme is simpler to understand: use the last element as pivot, maintain a boundary index i for elements ≤ pivot, scan through the array swapping elements ≤ pivot to the left side, then place the pivot at the boundary. The Hoare partition scheme is more efficient in practice, using two pointers moving toward each other.\n\nQuick sort's average-case time complexity is O(n log n). Each partition places one element in its final position and divides the array into two roughly equal halves. With log n levels of recursion and O(n) work per level, we get O(n log n).\n\nThe worst case is O(n²), occurring when the pivot is always the smallest or largest element (e.g., sorted input with last-element pivot). This creates maximally unbalanced partitions of size 0 and n-1. To avoid this, use randomized pivot selection, median-of-three pivot choice, or Dutch National Flag partitioning for arrays with many duplicates.\n\nDespite the theoretical worst case, quick sort is often the fastest sorting algorithm in practice. Its in-place nature (O(log n) stack space) and excellent cache locality (sequential memory access during partitioning) give it smaller constant factors than merge sort. Most standard library sort functions use quick sort or hybrid variants (like introsort).`,

    code: `// Quick Sort with Lomuto partition scheme\nfunction quickSort(arr, low = 0, high = arr.length - 1) {\n    if (low < high) {\n        // Partition and get pivot index\n        const pivotIndex = partition(arr, low, high);\n        // Recursively sort elements before and after pivot\n        quickSort(arr, low, pivotIndex - 1);\n        quickSort(arr, pivotIndex + 1, high);\n    }\n    return arr;\n}\n\nfunction partition(arr, low, high) {\n    // Choose random pivot to avoid worst case\n    const randomIdx = Math.floor(Math.random() * (high - low + 1)) + low;\n    [arr[randomIdx], arr[high]] = [arr[high], arr[randomIdx]];\n\n    const pivot = arr[high];\n    let i = low - 1; // boundary of elements <= pivot\n\n    for (let j = low; j < high; j++) {\n        if (arr[j] <= pivot) {\n            i++;\n            [arr[i], arr[j]] = [arr[j], arr[i]];\n        }\n    }\n    // Place pivot in its correct position\n    [arr[i + 1], arr[high]] = [arr[high], arr[i + 1]];\n    return i + 1;\n}\n\n// Hoare partition (more efficient, fewer swaps)\nfunction hoarePartition(arr, low, high) {\n    const pivot = arr[Math.floor((low + high) / 2)];\n    let i = low - 1;\n    let j = high + 1;\n    while (true) {\n        do { i++; } while (arr[i] < pivot);\n        do { j--; } while (arr[j] > pivot);\n        if (i >= j) return j;\n        [arr[i], arr[j]] = [arr[j], arr[i]];\n    }\n}\n\nconsole.log(quickSort([10, 7, 8, 9, 1, 5]));\n// Output: [1, 5, 7, 8, 9, 10]`,

    breakdown: `• Partition operation — The heart of quick sort. Rearranges elements around a pivot in O(n) time using O(1) space. After partitioning, the pivot is in its final sorted position. Elements left of it are smaller, elements right are larger.\n\n• Pivot selection — Poor pivot choices (always min/max) cause O(n²). Random pivot gives O(n log n) expected time. Median-of-three (first, middle, last) works well in practice. For arrays with many duplicates, three-way partitioning (Dutch National Flag) prevents O(n²).\n\n• Average vs worst case — Average: O(n log n) with balanced partitions. Worst: O(n²) with maximally unbalanced partitions. Randomized pivot makes worst case extremely unlikely (probability 1/n! for any specific bad ordering).\n\n• In-place and cache-friendly — Quick sort uses only O(log n) stack space for recursion (or O(n) worst case without tail-call optimization). Partitioning accesses memory sequentially, which is cache-friendly. This gives quick sort excellent real-world performance despite merge sort's theoretical guarantees.\n\n• Not stable — Quick sort is not stable; partitioning can change the relative order of equal elements. When stability is required, use merge sort or a stable variant of quick sort (which sacrifices some performance or space efficiency).`,
    summary: "Quick sort selects a pivot, partitions the array around it, and recursively sorts the partitions. With O(n log n) average case, O(1) extra space (in-place), and excellent cache performance, it's often the fastest practical sorting algorithm. Randomized pivot selection avoids the O(n²) worst case."
  },

  {
    title: "Counting Sort & Radix Sort",
    definition: "Counting sort and radix sort are non-comparison-based sorting algorithms that can achieve linear O(n) time complexity under certain conditions. Counting sort works by counting occurrences of each value, while radix sort processes individual digits/characters from least significant to most significant, using a stable sort (like counting sort) at each position.",
    explanation: `Comparison-based sorts have a theoretical lower bound of O(n log n) — you cannot do better when your only operation is comparing two elements. However, if you know something about the values being sorted (like their range), non-comparison sorts can beat this bound.\n\nCounting sort works when values are integers in a known range [0, k]. It creates a count array of size k+1, counts how many times each value appears, computes cumulative counts (prefix sums), then places each element in its correct position. Time: O(n + k). Space: O(n + k). If k = O(n), this is linear time.\n\nCounting sort is stable — elements with equal keys maintain their original order. This stability is critical for radix sort to work correctly.\n\nRadix sort handles larger numbers by processing one digit at a time. It sorts numbers by least significant digit first, then second-least significant, and so on. At each digit position, it uses a stable sort (counting sort) as a subroutine. After processing all d digits, the array is fully sorted.\n\nRadix sort time complexity is O(d × (n + k)) where d is the number of digits and k is the base (10 for decimal, 256 for bytes). For fixed-length integers with constant number of digits, this simplifies to O(n). Radix sort is used in practice for sorting strings (MSD radix sort), integers, and even floating-point numbers with bit manipulation.`,

    code: `// Counting Sort\nfunction countingSort(arr, maxVal) {\n    const count = new Array(maxVal + 1).fill(0);\n    const output = new Array(arr.length);\n\n    // Count occurrences of each value\n    for (const num of arr) {\n        count[num]++;\n    }\n\n    // Compute cumulative counts (prefix sums)\n    // count[i] now contains the position where i should end up\n    for (let i = 1; i <= maxVal; i++) {\n        count[i] += count[i - 1];\n    }\n\n    // Build output array (iterate backwards for stability)\n    for (let i = arr.length - 1; i >= 0; i--) {\n        output[count[arr[i]] - 1] = arr[i];\n        count[arr[i]]--;\n    }\n\n    return output;\n}\n\n// Radix Sort (LSD - Least Significant Digit first)\nfunction radixSort(arr) {\n    const max = Math.max(...arr);\n\n    // Process each digit position\n    for (let exp = 1; Math.floor(max / exp) > 0; exp *= 10) {\n        countingSortByDigit(arr, exp);\n    }\n    return arr;\n}\n\nfunction countingSortByDigit(arr, exp) {\n    const n = arr.length;\n    const output = new Array(n);\n    const count = new Array(10).fill(0);\n\n    // Count occurrences of each digit\n    for (let i = 0; i < n; i++) {\n        const digit = Math.floor(arr[i] / exp) % 10;\n        count[digit]++;\n    }\n\n    // Cumulative count\n    for (let i = 1; i < 10; i++) {\n        count[i] += count[i - 1];\n    }\n\n    // Build output (backwards for stability)\n    for (let i = n - 1; i >= 0; i--) {\n        const digit = Math.floor(arr[i] / exp) % 10;\n        output[count[digit] - 1] = arr[i];\n        count[digit]--;\n    }\n\n    // Copy back to original array\n    for (let i = 0; i < n; i++) {\n        arr[i] = output[i];\n    }\n}\n\nconsole.log(radixSort([170, 45, 75, 90, 802, 24, 2, 66]));\n// Output: [2, 24, 45, 66, 75, 90, 170, 802]`,

    breakdown: `• Breaking the O(n log n) barrier — Comparison sorts cannot be faster than O(n log n) (proven via decision tree argument). Counting sort and radix sort bypass this by not comparing elements directly — they use element values as indices.\n\n• Counting sort mechanics — Count occurrences, compute prefix sums (which give final positions), then place elements. The backwards traversal in the placement step ensures stability. Requires O(n + k) time and space where k is the value range.\n\n• Radix sort mechanics — Process digits from least significant to most significant. Stability of the digit-level sort is ESSENTIAL — without it, previously sorted positions would be disrupted. Each pass is O(n + k), and there are d passes, giving O(d(n + k)).\n\n• When to use — Counting sort: when the range k is O(n) (e.g., sorting exam scores 0-100 for millions of students). Radix sort: when numbers have a fixed number of digits or when sorting strings. Not suitable when k >> n (wastes space) or for floating-point numbers without conversion.\n\n• LSD vs MSD radix sort — LSD (Least Significant Digit) processes right-to-left and is simpler/non-recursive. MSD (Most Significant Digit) processes left-to-right, can short-circuit (skip if bucket has one element), and is better for variable-length strings.`,
    summary: "Counting sort and radix sort achieve linear O(n) time by exploiting knowledge of value ranges rather than comparing elements. Counting sort counts occurrences and uses prefix sums for placement. Radix sort processes digit-by-digit using stable counting sort. Both require specific conditions (bounded range, fixed-length keys) but are extremely fast when applicable."
  },

  {
    title: "Comparing Sort Algorithms (Big O)",
    definition: "Sorting algorithm comparison involves analyzing time complexity (best, average, worst case), space complexity, stability, and adaptivity across different algorithms to determine which is most appropriate for a given use case. The fundamental lower bound for comparison-based sorting is Ω(n log n).",
    explanation: `The comparison-based sorting lower bound of Ω(n log n) is proven using decision trees. Any comparison sort can be modeled as a binary decision tree where each internal node represents a comparison. For n elements, there are n! possible permutations (leaf nodes). A binary tree with n! leaves has height at least log₂(n!) = Ω(n log n). Therefore, any comparison sort must make at least Ω(n log n) comparisons in the worst case.\n\nTime complexity comparison: Bubble sort, selection sort, and insertion sort are all O(n²) average/worst case. Merge sort is O(n log n) in ALL cases. Quick sort is O(n log n) average but O(n²) worst case. Heap sort is O(n log n) in all cases. Counting sort is O(n+k), radix sort is O(d(n+k)).\n\nSpace complexity matters in practice: Merge sort uses O(n) extra space. Quick sort uses O(log n) stack space. Heap sort, bubble sort, selection sort, and insertion sort are all O(1) extra space (in-place). Counting/radix sort use O(n+k) space.\n\nStability (preserving relative order of equal elements): Merge sort, insertion sort, bubble sort, counting sort, and radix sort are stable. Quick sort, heap sort, and selection sort are NOT stable. Stability is crucial when sorting objects by multiple keys (e.g., sort by name, then by age — age sort must be stable to preserve name ordering).\n\nAdaptivity (faster on partially sorted input): Insertion sort is adaptive — O(n) for nearly sorted data. Timsort (Python/Java default) is designed to exploit existing runs (sorted sequences) in the input, achieving O(n) on already-sorted data while maintaining O(n log n) worst case.`,

    code: `// Comparison of sort algorithms - summary table as code\n\n/*\n+------------------+----------+----------+----------+-------+--------+\n| Algorithm        | Best     | Average  | Worst    | Space | Stable |\n+------------------+----------+----------+----------+-------+--------+\n| Bubble Sort      | O(n)     | O(n\u00b2)    | O(n\u00b2)    | O(1)  | Yes    |\n| Selection Sort   | O(n\u00b2)    | O(n\u00b2)    | O(n\u00b2)    | O(1)  | No     |\n| Insertion Sort   | O(n)     | O(n\u00b2)    | O(n\u00b2)    | O(1)  | Yes    |\n| Merge Sort       | O(nlogn) | O(nlogn) | O(nlogn) | O(n)  | Yes    |\n| Quick Sort       | O(nlogn) | O(nlogn) | O(n\u00b2)    | O(lgn)| No     |\n| Heap Sort        | O(nlogn) | O(nlogn) | O(nlogn) | O(1)  | No     |\n| Counting Sort    | O(n+k)   | O(n+k)   | O(n+k)   | O(n+k)| Yes    |\n| Radix Sort       | O(dn)    | O(dn)    | O(dn)    | O(n+k)| Yes    |\n+------------------+----------+----------+----------+-------+--------+\n*/\n\n// Choosing the right algorithm:\nfunction chooseSort(arraySize, dataType, constraints) {\n    // Small array (n < 20): insertion sort\n    if (arraySize < 20) return "Insertion Sort";\n\n    // Need guaranteed O(n log n) + stability: merge sort\n    if (constraints.needsStable && constraints.needsGuaranteed)\n        return "Merge Sort";\n\n    // Memory constrained + guaranteed O(n log n): heap sort\n    if (constraints.memoryLimited && constraints.needsGuaranteed)\n        return "Heap Sort";\n\n    // Integers in small range: counting sort\n    if (dataType === 'integer' && constraints.maxValue <= arraySize * 10)\n        return "Counting Sort";\n\n    // General purpose, best average performance: quick sort\n    return "Quick Sort (randomized)";\n}\n\n// Insertion sort: best for small or nearly-sorted arrays\nfunction insertionSort(arr) {\n    for (let i = 1; i < arr.length; i++) {\n        const key = arr[i];\n        let j = i - 1;\n        while (j >= 0 && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            j--;\n        }\n        arr[j + 1] = key;\n    }\n    return arr;\n}`,

    breakdown: `• Lower bound theorem — No comparison sort can beat O(n log n) worst case. This is proven mathematically via decision trees: n! permutations require log₂(n!) = Ω(n log n) comparisons to distinguish. Non-comparison sorts bypass this by not using pairwise comparisons.\n\n• Quick sort dominance in practice — Despite O(n²) worst case, quick sort's cache-friendly sequential access pattern, small constant factors, and in-place nature make it 2-3x faster than merge sort in practice for random data. Randomized pivot makes worst case negligibly rare.\n\n• Hybrid algorithms — Real-world sort implementations combine algorithms: Timsort (Python, Java) uses merge sort with insertion sort for small runs. Introsort (C++ std::sort) starts with quick sort but switches to heap sort if recursion depth exceeds log n (preventing O(n²) worst case).\n\n• Stability importance — When sorting database records by multiple fields, stable sort preserves the order of previous sorts. Example: sort employees by name, then stable sort by department → employees are sorted by department with names in order within each department.\n\n• Choosing wisely — No single algorithm is best for all cases. Consider: data size, memory constraints, stability requirements, whether data is partially sorted, key type (integers vs strings vs objects), and whether you need guaranteed worst-case or just good average-case performance.`,
    summary: "Different sorting algorithms have different strengths: merge sort guarantees O(n log n) and stability; quick sort offers best average performance in-place; heap sort provides O(n log n) with O(1) space; counting/radix achieve O(n) for constrained inputs. Real implementations use hybrid approaches combining multiple algorithms based on input characteristics."
  }
];
