// Pre-written spec lessons for: Graph Algorithms

export const dsGraphAlgorithmsLessons = [
  {
    title: "Graph Representations (Adjacency List/Matrix)",
    definition: "A graph is a data structure consisting of vertices (nodes) connected by edges. The two primary ways to represent a graph in memory are adjacency matrices (a 2D array where entry [i][j] indicates an edge between vertices i and j) and adjacency lists (each vertex stores a list of its neighbors). The choice of representation significantly impacts algorithm performance.",

    explanation: `Graphs model relationships between entities: social networks (people connected by friendships), road networks (intersections connected by roads), web pages (connected by hyperlinks), dependency systems (packages depending on other packages). Understanding graph representations is the foundation for all graph algorithms.\n\nAn adjacency matrix is a V×V 2D array where V is the number of vertices. Entry matrix[i][j] = 1 (or the edge weight) if there's an edge from vertex i to vertex j, and 0 otherwise. For undirected graphs, the matrix is symmetric. Checking if an edge exists is O(1), but storing the matrix requires O(V²) space regardless of how many edges exist.\n\nAn adjacency list stores, for each vertex, a list of its neighbors (and optionally edge weights). This uses O(V + E) space where E is the number of edges. For sparse graphs (E << V²), this is far more memory-efficient. Iterating over all neighbors of a vertex is O(degree), while checking if a specific edge exists requires searching the neighbor list.\n\nThe choice depends on graph density: for dense graphs (E close to V²), adjacency matrices are appropriate — constant-time edge lookup and the O(V²) space is comparable to the edge count. For sparse graphs (most real-world graphs like social networks, road maps), adjacency lists are strongly preferred — they save space and make neighbor iteration faster.\n\nGraphs can be directed (edges have a direction, like one-way streets) or undirected (edges go both ways). They can be weighted (edges have costs/distances) or unweighted. They can contain cycles or be acyclic (DAGs — directed acyclic graphs). These properties determine which algorithms apply.`,

    code: `// Adjacency List representation (most common)\nclass Graph {\n    constructor(directed = false) {\n        this.adjacencyList = new Map();\n        this.directed = directed;\n    }\n\n    addVertex(vertex) {\n        if (!this.adjacencyList.has(vertex)) {\n            this.adjacencyList.set(vertex, []);\n        }\n    }\n\n    addEdge(v1, v2, weight = 1) {\n        this.addVertex(v1);\n        this.addVertex(v2);\n        this.adjacencyList.get(v1).push({ node: v2, weight });\n        if (!this.directed) {\n            this.adjacencyList.get(v2).push({ node: v1, weight });\n        }\n    }\n\n    getNeighbors(vertex) {\n        return this.adjacencyList.get(vertex) || [];\n    }\n\n    getVertices() {\n        return [...this.adjacencyList.keys()];\n    }\n}\n\n// Adjacency Matrix representation\nclass GraphMatrix {\n    constructor(numVertices) {\n        this.V = numVertices;\n        this.matrix = Array.from({length: numVertices},\n            () => new Array(numVertices).fill(0));\n    }\n\n    addEdge(v1, v2, weight = 1) {\n        this.matrix[v1][v2] = weight;\n        this.matrix[v2][v1] = weight; // Remove for directed\n    }\n\n    hasEdge(v1, v2) {\n        return this.matrix[v1][v2] !== 0; // O(1) lookup!\n    }\n\n    getNeighbors(vertex) {\n        const neighbors = [];\n        for (let i = 0; i < this.V; i++) {\n            if (this.matrix[vertex][i] !== 0) {\n                neighbors.push({ node: i, weight: this.matrix[vertex][i] });\n            }\n        }\n        return neighbors; // O(V) - must scan entire row\n    }\n}\n\n// Example: Build a road network\nconst cityGraph = new Graph(false);\ncityGraph.addEdge("NYC", "Boston", 215);\ncityGraph.addEdge("NYC", "Philadelphia", 97);\ncityGraph.addEdge("Boston", "Portland", 108);\ncityGraph.addEdge("Philadelphia", "Baltimore", 101);`,

    breakdown: `• Space complexity — Adjacency matrix: O(V²) always. Adjacency list: O(V + E). For sparse graphs (E << V²), lists save enormous space. A social network with 1 billion users and 150 average friends: list uses ~150B entries vs matrix using 10^18 entries.\n\n• Edge lookup time — Matrix: O(1) — just check matrix[i][j]. List: O(degree) — must search the neighbor list. For algorithms that frequently check "does this edge exist?", matrices can be faster (but at great space cost).\n\n• Neighbor iteration — List: O(degree) — directly access the neighbor list. Matrix: O(V) — must scan the entire row checking for non-zero entries. Most graph algorithms (BFS, DFS, Dijkstra) iterate over neighbors, making lists generally faster.\n\n• Graph types — Undirected: edges are bidirectional (friendship). Directed: edges have direction (following on Twitter). Weighted: edges have costs (road distances). Unweighted: all edges equal (maze passages). DAG: directed + no cycles (task dependencies, course prerequisites).\n\n• Practical choice — Use adjacency lists for almost all real-world graphs (social networks, web graphs, road networks — all sparse). Use adjacency matrices only for small dense graphs, or when O(1) edge existence checking is critical (like Floyd-Warshall algorithm).`,
    summary: "Adjacency lists (O(V+E) space, efficient neighbor iteration) and adjacency matrices (O(V²) space, O(1) edge lookup) are the two fundamental graph representations. Most real-world graphs are sparse, making adjacency lists the preferred choice. Understanding the tradeoffs is essential for implementing efficient graph algorithms."
  },

  {
    title: "BFS & DFS Traversal",
    definition: "Breadth-First Search (BFS) and Depth-First Search (DFS) are the two fundamental graph traversal algorithms. BFS explores all neighbors at the current depth before moving deeper (level by level, using a queue). DFS explores as far as possible along each branch before backtracking (using a stack or recursion). Together they form the basis for most graph algorithms.",
    explanation: `BFS uses a queue to explore vertices level by level. Starting from a source vertex, it visits all vertices at distance 1, then distance 2, then distance 3, and so on. This guarantees that BFS finds the shortest path (in terms of number of edges) from the source to any reachable vertex in an unweighted graph.\n\nBFS algorithm: (1) Enqueue the start vertex and mark it visited. (2) While queue is not empty: dequeue a vertex, process it, then enqueue all unvisited neighbors (marking them visited). Time complexity: O(V + E) since each vertex and edge is processed exactly once.\n\nDFS uses a stack (or recursion) to explore as deeply as possible before backtracking. From the starting vertex, it follows one path until it reaches a dead end (no unvisited neighbors), then backtracks to explore other branches. DFS is simpler to implement recursively and uses less memory for wide graphs.\n\nDFS algorithm: (1) Mark current vertex as visited and process it. (2) For each unvisited neighbor, recursively perform DFS. The implicit call stack handles backtracking. Iterative DFS uses an explicit stack instead. Time complexity: O(V + E), same as BFS.\n\nKey differences: BFS finds shortest paths in unweighted graphs; DFS does not. BFS uses more memory for wide graphs (queue stores entire frontier); DFS uses more memory for deep graphs (stack stores entire path). BFS is preferred for shortest path, level-order problems. DFS is preferred for connectivity, cycle detection, topological sort, and problems requiring complete path exploration (maze solving, backtracking).`,

    code: `// BFS - Breadth-First Search\nfunction bfs(graph, start) {\n    const visited = new Set();\n    const queue = [start];\n    const result = [];\n    visited.add(start);\n\n    while (queue.length > 0) {\n        const vertex = queue.shift(); // Dequeue front\n        result.push(vertex);\n\n        for (const neighbor of graph.getNeighbors(vertex)) {\n            if (!visited.has(neighbor.node)) {\n                visited.add(neighbor.node);\n                queue.push(neighbor.node);\n            }\n        }\n    }\n    return result;\n}\n\n// BFS Shortest Path (unweighted graph)\nfunction bfsShortestPath(graph, start, end) {\n    const visited = new Set([start]);\n    const queue = [[start, [start]]]; // [vertex, path]\n\n    while (queue.length > 0) {\n        const [vertex, path] = queue.shift();\n        if (vertex === end) return path;\n\n        for (const neighbor of graph.getNeighbors(vertex)) {\n            if (!visited.has(neighbor.node)) {\n                visited.add(neighbor.node);\n                queue.push([neighbor.node, [...path, neighbor.node]]);\n            }\n        }\n    }\n    return null; // No path exists\n}\n\n// DFS - Depth-First Search (recursive)\nfunction dfs(graph, start, visited = new Set()) {\n    visited.add(start);\n    const result = [start];\n\n    for (const neighbor of graph.getNeighbors(start)) {\n        if (!visited.has(neighbor.node)) {\n            result.push(...dfs(graph, neighbor.node, visited));\n        }\n    }\n    return result;\n}\n\n// DFS - Iterative (using explicit stack)\nfunction dfsIterative(graph, start) {\n    const visited = new Set();\n    const stack = [start];\n    const result = [];\n\n    while (stack.length > 0) {\n        const vertex = stack.pop(); // Pop from top (LIFO)\n        if (visited.has(vertex)) continue;\n        visited.add(vertex);\n        result.push(vertex);\n\n        // Push neighbors (reverse for same order as recursive)\n        const neighbors = graph.getNeighbors(vertex);\n        for (let i = neighbors.length - 1; i >= 0; i--) {\n            if (!visited.has(neighbors[i].node)) {\n                stack.push(neighbors[i].node);\n            }\n        }\n    }\n    return result;\n}\n\n// Cycle Detection using DFS\nfunction hasCycle(graph) {\n    const visited = new Set();\n    const recursionStack = new Set(); // Track current path\n\n    function dfsCheck(vertex) {\n        visited.add(vertex);\n        recursionStack.add(vertex);\n\n        for (const neighbor of graph.getNeighbors(vertex)) {\n            if (!visited.has(neighbor.node)) {\n                if (dfsCheck(neighbor.node)) return true;\n            } else if (recursionStack.has(neighbor.node)) {\n                return true; // Back edge = cycle!\n            }\n        }\n\n        recursionStack.delete(vertex); // Backtrack\n        return false;\n    }\n\n    for (const vertex of graph.getVertices()) {\n        if (!visited.has(vertex) && dfsCheck(vertex)) return true;\n    }\n    return false;\n}`,

    breakdown: `• BFS guarantees shortest path — In unweighted graphs, BFS finds the minimum number of edges from source to any vertex. This is because it explores all vertices at distance d before any at distance d+1. Essential for social network "degrees of separation" and maze shortest paths.\n\n• DFS for connectivity and cycles — DFS naturally explores connected components and detects cycles (back edges in the DFS tree). It's the basis for topological sort, strongly connected components (Tarjan's, Kosaraju's), and articulation point/bridge detection.\n\n• Time/Space complexity — Both are O(V + E) time (visit each vertex and edge once). BFS space: O(V) for the queue (worst case: entire level). DFS space: O(V) for the recursion stack (worst case: longest path). BFS is worse for wide graphs; DFS is worse for deep graphs.\n\n• Queue vs Stack — The ONLY structural difference between BFS and DFS is the data structure: BFS uses a queue (FIFO — process oldest first, exploring breadth), DFS uses a stack (LIFO — process newest first, exploring depth). Swapping the data structure converts one into the other.\n\n• Applications — BFS: shortest path in unweighted graphs, level-order tree traversal, finding connected components, web crawling. DFS: topological sorting, cycle detection, maze generation, solving puzzles with backtracking, finding strongly connected components.`,
    summary: "BFS (queue-based, level by level) finds shortest paths in unweighted graphs. DFS (stack/recursion-based, depth first) is used for cycle detection, topological sort, and connectivity. Both run in O(V+E) time. The choice depends on the problem: shortest paths need BFS; exhaustive exploration and backtracking need DFS."
  },

  {
    title: "Dijkstra's Shortest Path",
    definition: "Dijkstra's algorithm finds the shortest path from a single source vertex to all other vertices in a weighted graph with non-negative edge weights. It uses a greedy approach: always process the unvisited vertex with the smallest known distance, updating its neighbors' distances. With a priority queue (min-heap), it runs in O((V + E) log V) time.",
    explanation: `Dijkstra's algorithm generalizes BFS to weighted graphs. While BFS works for unweighted graphs (where shortest path = fewest edges), Dijkstra handles edges with different costs. The key insight is greedy: the vertex with the smallest tentative distance is guaranteed to have its shortest path finalized — no future path through other vertices could be shorter (because all edges are non-negative).\n\nAlgorithm steps: (1) Set distance to source = 0, all others = infinity. (2) Add source to priority queue. (3) While queue not empty: extract vertex u with minimum distance. For each neighbor v of u: if dist[u] + weight(u,v) < dist[v], update dist[v] and add v to the priority queue. (4) When a vertex is extracted, its shortest distance is finalized.\n\nThe priority queue is critical for efficiency. Without it (scanning all vertices for the minimum), time is O(V²). With a binary heap priority queue, extraction is O(log V) and we do at most V extractions + E decrease-key operations, giving O((V + E) log V). For dense graphs, O(V²) may actually be faster.\n\nDijkstra's FAILS with negative edge weights. Consider: A→B (weight 1), A→C (weight 5), C→B (weight -10). Dijkstra finalizes B's distance as 1 (via A→B), but the actual shortest path is A→C→B = 5 + (-10) = -5. For negative weights, use Bellman-Ford algorithm (O(V*E)) which relaxes all edges V-1 times.\n\nApplications are everywhere: GPS navigation (shortest driving route), network routing protocols (OSPF uses Dijkstra), game pathfinding (with A* heuristic extension), and any scenario requiring cheapest paths through weighted graphs.`,

    code: `// Dijkstra's Algorithm with Priority Queue (Min-Heap)\nfunction dijkstra(graph, start) {\n    const distances = new Map();\n    const previous = new Map(); // For path reconstruction\n    const pq = new MinPriorityQueue(); // Min-heap\n\n    // Initialize all distances to Infinity\n    for (const vertex of graph.getVertices()) {\n        distances.set(vertex, Infinity);\n        previous.set(vertex, null);\n    }\n    distances.set(start, 0);\n    pq.enqueue(start, 0);\n\n    while (!pq.isEmpty()) {\n        const { element: u, priority: dist } = pq.dequeue();\n\n        // Skip if we already found a better path\n        if (dist > distances.get(u)) continue;\n\n        // Explore neighbors\n        for (const { node: v, weight } of graph.getNeighbors(u)) {\n            const newDist = distances.get(u) + weight;\n\n            // Relaxation: found a shorter path to v\n            if (newDist < distances.get(v)) {\n                distances.set(v, newDist);\n                previous.set(v, u);\n                pq.enqueue(v, newDist);\n            }\n        }\n    }\n\n    return { distances, previous };\n}\n\n// Reconstruct shortest path from start to end\nfunction getPath(previous, start, end) {\n    const path = [];\n    let current = end;\n    while (current !== null) {\n        path.unshift(current);\n        current = previous.get(current);\n    }\n    return path[0] === start ? path : []; // No path if doesn't start at source\n}\n\n// Simple Priority Queue implementation\nclass MinPriorityQueue {\n    constructor() { this.items = []; }\n    enqueue(element, priority) {\n        this.items.push({ element, priority });\n        this.items.sort((a, b) => a.priority - b.priority);\n    }\n    dequeue() { return this.items.shift(); }\n    isEmpty() { return this.items.length === 0; }\n}\n\n// Example usage\nconst g = new Graph(false);\ng.addEdge("A", "B", 4);\ng.addEdge("A", "C", 2);\ng.addEdge("B", "E", 3);\ng.addEdge("C", "D", 2);\ng.addEdge("C", "F", 4);\ng.addEdge("D", "E", 3);\ng.addEdge("D", "F", 1);\ng.addEdge("E", "F", 1);\n\nconst { distances, previous } = dijkstra(g, "A");\n// A->B: 4, A->C: 2, A->D: 4, A->E: 7, A->F: 5\nconsole.log(getPath(previous, "A", "E")); // ["A","C","D","E"]`,

    breakdown: `• Greedy correctness — When a vertex is extracted from the priority queue, its distance is final. Proof: any other path to this vertex must go through an unprocessed vertex, which has a larger tentative distance (since we always extract the minimum). This only holds with non-negative weights.\n\n• Relaxation — The core operation: if dist[u] + weight(u,v) < dist[v], update dist[v]. This "relaxes" the distance estimate for v. Each edge is relaxed at most once when its source vertex is processed. The term comes from gradually tightening upper bounds on distances.\n\n• Priority queue choice — Binary heap: O((V+E) log V). Fibonacci heap: O(V log V + E), optimal for dense graphs. For sparse graphs (E ≈ V), binary heap is excellent. In practice, binary heaps are preferred due to simplicity and low constant factors.\n\n• Negative edge limitation — Dijkstra assumes extracting the minimum-distance vertex finalizes it. Negative edges invalidate this: a longer path could become shorter via negative edges later. Use Bellman-Ford (O(VE)) for negative weights, or Johnson's algorithm for all-pairs with negative edges.\n\n• A* extension — A* is Dijkstra with a heuristic: instead of always processing the closest vertex to the source, it processes the vertex estimated to be closest to the GOAL (using priority = distance + heuristic). This focuses the search toward the destination, dramatically reducing explored vertices in practice.`,
    summary: "Dijkstra's algorithm finds shortest paths in non-negative weighted graphs using a greedy approach with a priority queue. It processes vertices in order of increasing distance, relaxing edges to discover shorter paths. Running in O((V+E) log V) with a binary heap, it's the foundation for GPS navigation, network routing, and is extended by A* for heuristic-guided pathfinding."
  },

  {
    title: "Minimum Spanning Tree (Kruskal, Prim)",
    definition: "A Minimum Spanning Tree (MST) of a connected, weighted, undirected graph is a subset of edges that connects all vertices with the minimum total edge weight, forming a tree (no cycles). Kruskal's algorithm builds the MST by adding the cheapest edge that doesn't create a cycle. Prim's algorithm grows the MST from a starting vertex by always adding the cheapest edge connecting the tree to a non-tree vertex.",
    explanation: `A spanning tree of a graph connects all V vertices using exactly V-1 edges with no cycles. Among all possible spanning trees, the Minimum Spanning Tree has the least total edge weight. MSTs have applications in network design (connecting cities with minimum cable), approximation algorithms (TSP approximation), and clustering.\n\nKruskal's algorithm is edge-centric: (1) Sort all edges by weight. (2) Process edges from lightest to heaviest. (3) For each edge, if it connects two different components (doesn't create a cycle), add it to the MST. (4) Stop when V-1 edges are added. The Union-Find (Disjoint Set) data structure efficiently checks connectivity and merges components.\n\nKruskal's time complexity is O(E log E) for sorting edges, plus O(E × α(V)) for Union-Find operations (where α is the inverse Ackermann function, practically constant). Total: O(E log E) = O(E log V) since E ≤ V².\n\nPrim's algorithm is vertex-centric: (1) Start with any vertex in the MST. (2) Repeatedly add the cheapest edge from any MST vertex to any non-MST vertex. (3) Continue until all vertices are in the MST. This uses a priority queue to efficiently find the cheapest crossing edge.\n\nPrim's with a binary heap runs in O((V + E) log V), similar to Dijkstra. With a Fibonacci heap: O(E + V log V). Prim's is better for dense graphs (fewer edges to sort than Kruskal), while Kruskal's is better for sparse graphs and easier to parallelize. Both produce the same MST (or one of equal weight if multiple MSTs exist).`,

    code: `// Kruskal's Algorithm with Union-Find\nclass UnionFind {\n    constructor(n) {\n        this.parent = Array.from({length: n}, (_, i) => i);\n        this.rank = new Array(n).fill(0);\n    }\n\n    find(x) {\n        if (this.parent[x] !== x) {\n            this.parent[x] = this.find(this.parent[x]); // Path compression\n        }\n        return this.parent[x];\n    }\n\n    union(x, y) {\n        const px = this.find(x), py = this.find(y);\n        if (px === py) return false; // Already connected\n        // Union by rank\n        if (this.rank[px] < this.rank[py]) this.parent[px] = py;\n        else if (this.rank[px] > this.rank[py]) this.parent[py] = px;\n        else { this.parent[py] = px; this.rank[px]++; }\n        return true;\n    }\n}\n\nfunction kruskal(numVertices, edges) {\n    // edges: [{from, to, weight}, ...]\n    edges.sort((a, b) => a.weight - b.weight); // Sort by weight\n    const uf = new UnionFind(numVertices);\n    const mst = [];\n    let totalWeight = 0;\n\n    for (const edge of edges) {\n        // Add edge if it doesn't create a cycle\n        if (uf.union(edge.from, edge.to)) {\n            mst.push(edge);\n            totalWeight += edge.weight;\n            if (mst.length === numVertices - 1) break; // MST complete\n        }\n    }\n\n    return { mst, totalWeight };\n}\n\n// Prim's Algorithm with Priority Queue\nfunction prim(graph, start) {\n    const inMST = new Set();\n    const mst = [];\n    const pq = new MinPriorityQueue();\n    let totalWeight = 0;\n\n    inMST.add(start);\n    // Add all edges from start to priority queue\n    for (const { node, weight } of graph.getNeighbors(start)) {\n        pq.enqueue({ from: start, to: node, weight }, weight);\n    }\n\n    while (!pq.isEmpty() && inMST.size < graph.getVertices().length) {\n        const { element: edge } = pq.dequeue();\n\n        if (inMST.has(edge.to)) continue; // Skip if already in MST\n\n        // Add vertex to MST\n        inMST.add(edge.to);\n        mst.push(edge);\n        totalWeight += edge.weight;\n\n        // Add new edges from newly added vertex\n        for (const { node, weight } of graph.getNeighbors(edge.to)) {\n            if (!inMST.has(node)) {\n                pq.enqueue({ from: edge.to, to: node, weight }, weight);\n            }\n        }\n    }\n\n    return { mst, totalWeight };\n}\n\n// Example\nconst edges = [\n    {from: 0, to: 1, weight: 4}, {from: 0, to: 2, weight: 3},\n    {from: 1, to: 2, weight: 1}, {from: 1, to: 3, weight: 2},\n    {from: 2, to: 3, weight: 4}, {from: 3, to: 4, weight: 2},\n    {from: 2, to: 4, weight: 5}\n];\nconsole.log(kruskal(5, edges)); // totalWeight: 8`,

    breakdown: `• Cut property — The theoretical foundation: for any cut (partition of vertices into two sets), the minimum-weight edge crossing the cut must be in some MST. Both Kruskal's and Prim's exploit this property — they always add a safe (minimum crossing) edge.\n\n• Union-Find for Kruskal's — Union-Find efficiently tracks connected components. find() with path compression and union() with rank ensure nearly O(1) per operation. This makes cycle detection trivial: an edge creates a cycle if and only if both endpoints are in the same component.\n\n• Kruskal vs Prim — Kruskal's: sort edges globally O(E log E), better for sparse graphs. Prim's: grow tree locally O((V+E) log V), better for dense graphs. Kruskal's is easier to implement and parallelize. Prim's resembles Dijkstra in structure.\n\n• MST properties — An MST has exactly V-1 edges. Adding any edge creates exactly one cycle. The maximum edge on the path between any two vertices in the MST is the minimum over all possible spanning trees (minimax path property).\n\n• Applications — Network design (minimum cost to connect all locations), approximation algorithms (2-approximation for Traveling Salesman on metric graphs), clustering (remove heaviest MST edges to form k clusters), and image segmentation (group similar pixels).`,
    summary: "MST algorithms find the cheapest way to connect all vertices without cycles. Kruskal's sorts edges globally and uses Union-Find to avoid cycles (O(E log E), best for sparse graphs). Prim's grows the tree greedily using a priority queue (O((V+E) log V), best for dense graphs). Both are foundational for network design and approximation algorithms."
  },

  {
    title: "Topological Sort & DAGs",
    definition: "Topological sort produces a linear ordering of vertices in a Directed Acyclic Graph (DAG) such that for every directed edge (u, v), vertex u appears before vertex v in the ordering. It is only possible on DAGs — graphs with directed edges and no cycles. Topological sort is essential for dependency resolution, task scheduling, and build systems.",
    explanation: `A Directed Acyclic Graph (DAG) represents dependencies: edges point from prerequisites to dependents. Course prerequisites (take CS101 before CS201), build dependencies (compile library before main program), task scheduling (prepare ingredients before cooking). Topological sort provides a valid execution order that respects all dependencies.\n\nThere are two standard algorithms for topological sort: Kahn's algorithm (BFS-based using in-degrees) and DFS-based (using finish times). Both produce valid orderings in O(V + E) time. Multiple valid orderings may exist for any given DAG.\n\nKahn's algorithm: (1) Compute in-degree of each vertex (number of incoming edges). (2) Add all vertices with in-degree 0 to a queue (they have no dependencies). (3) While queue not empty: remove a vertex, add it to the result, and decrement the in-degree of all its neighbors. (4) If any neighbor's in-degree becomes 0, add it to the queue. If the result doesn't contain all vertices, the graph has a cycle.\n\nDFS-based approach: Run DFS on the graph. When DFS finishes processing a vertex (all descendants explored), push it to a stack. The stack's top-to-bottom order gives the topological ordering. This works because a vertex's DFS finishes only after all its reachable descendants have finished.\n\nTopological sort also detects cycles: if Kahn's algorithm can't process all vertices (some always have in-degree > 0), or if DFS encounters a back edge (an edge to a vertex currently on the recursion stack), the graph contains a cycle and no topological ordering exists.`,

    code: `// Topological Sort - Kahn's Algorithm (BFS-based)\nfunction topologicalSortKahn(graph) {\n    const inDegree = new Map();\n    const result = [];\n\n    // Initialize in-degrees\n    for (const vertex of graph.getVertices()) {\n        inDegree.set(vertex, 0);\n    }\n    for (const vertex of graph.getVertices()) {\n        for (const { node } of graph.getNeighbors(vertex)) {\n            inDegree.set(node, (inDegree.get(node) || 0) + 1);\n        }\n    }\n\n    // Start with vertices that have no dependencies\n    const queue = [];\n    for (const [vertex, degree] of inDegree) {\n        if (degree === 0) queue.push(vertex);\n    }\n\n    while (queue.length > 0) {\n        const vertex = queue.shift();\n        result.push(vertex);\n\n        for (const { node } of graph.getNeighbors(vertex)) {\n            inDegree.set(node, inDegree.get(node) - 1);\n            if (inDegree.get(node) === 0) {\n                queue.push(node); // All dependencies satisfied\n            }\n        }\n    }\n\n    // If result doesn't include all vertices, graph has a cycle\n    if (result.length !== graph.getVertices().length) {\n        throw new Error("Graph has a cycle - no topological order exists");\n    }\n    return result;\n}\n\n// Topological Sort - DFS-based\nfunction topologicalSortDFS(graph) {\n    const visited = new Set();\n    const stack = []; // Stores vertices in reverse topological order\n\n    function dfs(vertex) {\n        visited.add(vertex);\n        for (const { node } of graph.getNeighbors(vertex)) {\n            if (!visited.has(node)) {\n                dfs(node);\n            }\n        }\n        stack.push(vertex); // Add AFTER all descendants processed\n    }\n\n    for (const vertex of graph.getVertices()) {\n        if (!visited.has(vertex)) {\n            dfs(vertex);\n        }\n    }\n\n    return stack.reverse(); // Reverse gives topological order\n}\n\n// Example: Course prerequisites\nconst courses = new Graph(true); // Directed graph\ncourses.addEdge("CS101", "CS201");\ncourses.addEdge("CS101", "CS202");\ncourses.addEdge("CS201", "CS301");\ncourses.addEdge("CS202", "CS301");\ncourses.addEdge("MATH101", "CS202");\n\nconsole.log(topologicalSortKahn(courses));\n// Possible output: ["CS101", "MATH101", "CS201", "CS202", "CS301"]\n// (any valid ordering that respects prerequisites)`,

    breakdown: `• DAG requirement — Topological sort is ONLY possible on directed acyclic graphs. If a cycle exists (A depends on B, B depends on C, C depends on A), no valid ordering exists. Both algorithms can detect this: Kahn's fails to process all vertices; DFS finds a back edge.\n\n• Kahn's algorithm — Intuitive: start with tasks that have no prerequisites (in-degree 0), complete them, then their dependents become available. This naturally produces a valid execution schedule and also counts the length of the longest path (critical path in project scheduling).\n\n• DFS-based approach — A vertex's DFS finish time is after all reachable vertices finish. Reversing finish-time order gives topological order. More concise to code than Kahn's but less intuitive. Naturally extends to finding strongly connected components (Kosaraju's algorithm).\n\n• Multiple valid orderings — Most DAGs have many valid topological orderings. Kahn's with a priority queue can produce lexicographically smallest ordering. The number of valid orderings relates to the DAG's structure (width of the partial order).\n\n• Real-world applications — Package managers (npm, pip install dependencies in order), build systems (Make, Webpack compile in dependency order), spreadsheet cell evaluation (compute cells that others depend on first), course scheduling, and job scheduling with precedence constraints.`,
    summary: "Topological sort orders DAG vertices so all edges point forward, solving dependency resolution. Kahn's algorithm removes zero-in-degree vertices iteratively (BFS approach). DFS-based topological sort uses post-order finish times. Both run in O(V+E) and detect cycles. Essential for build systems, package managers, task schedulers, and any dependency-ordered processing."
  }
];
