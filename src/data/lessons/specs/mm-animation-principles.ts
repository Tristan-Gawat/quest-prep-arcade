// Pre-written spec lessons for: Animation Principles

export const mmAnimationPrinciplesLessons = [
  {
    title: "The 12 Principles of Animation",
    definition: "The 12 Principles of Animation are fundamental rules developed by Disney animators Ollie Johnston and Frank Thomas that create the illusion of life in animated characters and objects, applicable to both traditional and digital animation.",
    explanation: `The 12 Principles were codified in the 1981 book "The Illusion of Life" and remain the foundation of all animation work today. They describe how objects move in ways that feel natural and appealing to viewers. These principles apply equally to hand-drawn cartoons, 3D character animation, motion graphics, and UI/UX micro-interactions.\n\nThe principles address three core challenges: making movement feel physically believable (squash and stretch, timing, arcs), directing the viewer's attention (staging, anticipation, follow-through), and creating appeal (exaggeration, solid drawing, appeal). Together they transform mechanical movement into something that feels alive.\n\nSquash and Stretch is considered the most important principle — it gives objects a sense of weight and flexibility. A bouncing ball squashes on impact and stretches during fast movement. Anticipation prepares the viewer for an action (a character crouching before jumping). Follow-Through shows that parts of a character don't all stop at the same time (hair continuing to move after a character stops).\n\nIn digital interfaces, these principles manifest as button press animations (squash), loading anticipation states, elastic overshoots on modal appearances, and staggered element entrances. Understanding these principles elevates functional animation from mechanical to delightful.`,
    code: `// Squash and Stretch - Bouncing ball\n@keyframes bounce {\n    0% {\n        transform: translateY(0) scaleX(1) scaleY(1);\n    }\n    50% {\n        /* Stretch while falling */\n        transform: translateY(200px) scaleX(0.9) scaleY(1.1);\n    }\n    60% {\n        /* Squash on impact */\n        transform: translateY(220px) scaleX(1.2) scaleY(0.8);\n    }\n    100% {\n        transform: translateY(0) scaleX(1) scaleY(1);\n    }\n}\n\n/* Anticipation - Button press */\n.button:active {\n    /* Brief scale-down before action (anticipation) */\n    transform: scale(0.95);\n    transition: transform 0.1s ease;\n}\n\n/* Follow-Through & Overlapping Action */\n@keyframes slideInWithOvershoot {\n    0% { transform: translateX(-100%); }\n    70% { transform: translateX(5%); }  /* Overshoot */\n    85% { transform: translateX(-2%); } /* Settle back */\n    100% { transform: translateX(0); }  /* Final position */\n}\n\n/* Slow In / Slow Out (Ease) */\n.element {\n    /* Never use linear for organic motion */\n    transition: transform 0.4s cubic-bezier(0.25, 0.1, 0.25, 1);\n}\n\n/* Staging - Direct attention with motion */\n.highlight-element {\n    animation: pulse 2s ease-in-out infinite;\n}\n\n.background-elements {\n    /* Subtle, non-distracting motion */\n    animation: gentleSway 4s ease-in-out infinite;\n    opacity: 0.7;\n}`,
    breakdown: `• Squash & Stretch — Objects deform based on momentum and impact. Volume remains constant (wider = shorter). In UI, buttons compress on click, modals bounce on entry, and cards flex on drag.\n\n• Anticipation — A preparatory action before the main movement. A drawer slightly nudges closed before sliding open. A delete button shakes before confirming. Prepares the user for what's coming.\n\n• Follow-Through & Overlapping Action — Different parts of an object move at different rates. A dropdown's items arrive staggered. A notification slides in, overshoots slightly, then settles. Creates organic feel.\n\n• Slow In & Slow Out — Real objects accelerate and decelerate rather than moving at constant speed. This is why ease-in-out timing functions exist. Linear motion looks robotic and unnatural.\n\n• Arcs — Natural movement follows curved paths, not straight lines. Animate elements along bezier curves rather than straight X/Y translations for more lifelike motion.`,
    summary: "The 12 Principles of Animation provide the theoretical foundation for creating believable, appealing motion. Squash and stretch, anticipation, follow-through, and proper easing transform mechanical transitions into animations that feel natural and direct user attention effectively."
  },
  {
    title: "Keyframes & Interpolation",
    definition: "Keyframes are the defined states at specific points in time that an animator sets explicitly, while interpolation is the automatic calculation of all intermediate frames between keyframes to create smooth, continuous motion.",
    explanation: `In animation, a keyframe marks a significant moment — a pose, position, or state that defines the animation's structure. The term originates from traditional animation where senior animators drew the 'key' frames and assistants drew the 'in-between' frames. In digital animation, the computer handles the in-betweening (interpolation) automatically.\n\nInterpolation calculates the intermediate values between two keyframes. Linear interpolation (lerp) creates constant-speed movement between points: value = start + (end - start) * progress. While mathematically simple, linear interpolation rarely looks natural. More sophisticated interpolation methods like cubic splines, Catmull-Rom curves, or bezier curves create smoother, more organic transitions.\n\nDifferent properties require different interpolation approaches. Position and rotation interpolate smoothly as numbers, but colors need special handling (interpolating in HSL vs RGB produces different results). Discrete properties like display or visibility can't be interpolated — they switch at a specific keyframe.\n\nIn 3D animation, quaternion interpolation (slerp) is used for rotations instead of Euler angles to avoid gimbal lock. For UI animation, the Web Animations API and CSS both perform interpolation automatically, but understanding the underlying math helps when building custom animation systems or debugging unexpected behavior.`,
    code: `// Linear Interpolation (lerp) - Foundation of all tweening\nfunction lerp(start, end, progress) {\n    return start + (end - start) * progress;\n}\n\n// Animate a value from 0 to 100 over 1 second\nfunction animate() {\n    const duration = 1000; // ms\n    const startTime = performance.now();\n\n    function frame(currentTime) {\n        const elapsed = currentTime - startTime;\n        const progress = Math.min(elapsed / duration, 1);\n\n        // Linear interpolation\n        const x = lerp(0, 300, progress);\n        const opacity = lerp(0, 1, progress);\n\n        element.style.transform = \`translateX(\\\${x}px)\`;\n        element.style.opacity = opacity;\n\n        if (progress < 1) {\n            requestAnimationFrame(frame);\n        }\n    }\n    requestAnimationFrame(frame);\n}\n\n// Color interpolation in RGB\nfunction lerpColor(color1, color2, progress) {\n    const r = Math.round(lerp(color1.r, color2.r, progress));\n    const g = Math.round(lerp(color1.g, color2.g, progress));\n    const b = Math.round(lerp(color1.b, color2.b, progress));\n    return \`rgb(\\\${r}, \\\${g}, \\\${b})\`;\n}\n\n// Multi-keyframe interpolation\nconst keyframes = [\n    { time: 0, x: 0, y: 0 },\n    { time: 0.3, x: 100, y: -50 },\n    { time: 0.7, x: 250, y: 20 },\n    { time: 1.0, x: 400, y: 0 }\n];\n\nfunction getValueAtTime(keyframes, progress, property) {\n    // Find surrounding keyframes\n    let prev = keyframes[0];\n    let next = keyframes[keyframes.length - 1];\n\n    for (let i = 0; i < keyframes.length - 1; i++) {\n        if (progress >= keyframes[i].time &&\n            progress <= keyframes[i + 1].time) {\n            prev = keyframes[i];\n            next = keyframes[i + 1];\n            break;\n        }\n    }\n\n    // Local progress between these two keyframes\n    const range = next.time - prev.time;\n    const localProgress = (progress - prev.time) / range;\n\n    return lerp(prev[property], next[property], localProgress);\n}`,
    breakdown: `• Linear interpolation (lerp) — The formula start + (end - start) * t where t ranges from 0 to 1. Simple, predictable, but produces mechanical-looking motion. Use as the building block for more complex interpolation.\n\n• Keyframe spacing — Keyframes placed closer together in time create faster motion in that segment. Uneven spacing naturally creates acceleration and deceleration effects without explicit easing.\n\n• Multi-keyframe lookup — When animating through multiple keyframes, find the two surrounding keyframes for the current time, calculate local progress between them, then interpolate. This is how CSS @keyframes work internally.\n\n• Property-specific interpolation — Numeric values lerp directly, colors should interpolate in perceptually-uniform color spaces (oklch > hsl > rgb), and rotations need special handling to avoid spinning the wrong way around.\n\n• Spline interpolation — Cubic Hermite splines or Catmull-Rom curves pass smoothly through keyframe values without sharp direction changes, creating more natural motion than linear segments between keyframes.`,
    summary: "Keyframes define explicit animation states at specific times, while interpolation automatically calculates all intermediate values. Linear interpolation (lerp) is the mathematical foundation, with cubic splines and bezier curves providing smoother, more natural transitions between keyframe values."
  },
  {
    title: "Easing Functions & Timing",
    definition: "Easing functions map linear time progression (0 to 1) to non-linear value progression, controlling the rate of change during an animation to simulate natural acceleration, deceleration, elasticity, and bounce effects.",
    explanation: `In nature, nothing moves at constant speed. Objects accelerate from rest, decelerate before stopping, and overshoot their targets due to momentum. Easing functions simulate these physical behaviors mathematically. They take a normalized time input (0 at start, 1 at end) and output a transformed progress value that may ease in (start slow), ease out (end slow), or both.\n\nThe simplest easing functions are polynomial: easeInQuad squares the input (t*t), creating acceleration. easeOutQuad inverts this (1-(1-t)*(1-t)), creating deceleration. Higher powers (cubic, quartic, quintic) create more dramatic acceleration curves. The naming convention describes the behavior: 'In' means slow at the start, 'Out' means slow at the end, 'InOut' means slow at both ends.\n\nCSS uses cubic-bezier curves for easing, defined by two control points. The built-in keywords map to specific curves: 'ease' is cubic-bezier(0.25, 0.1, 0.25, 1), 'ease-in' is cubic-bezier(0.42, 0, 1, 1). Custom cubic-bezier values can create any smooth curve, though they cannot exceed the 0-1 output range (no overshoot). For overshoot/elastic effects in CSS, you need keyframe animations.\n\nTiming is equally important as easing. Perceived duration matters more than actual duration — a well-eased 300ms animation feels snappier than a poorly-timed 200ms linear one. UI animations should typically be 200-500ms. Micro-interactions (button presses, toggles) should be 100-200ms. Page transitions can be 300-500ms.`,
    code: `// Common easing functions\nconst Easings = {\n    // Polynomial easings\n    easeInQuad: (t) => t * t,\n    easeOutQuad: (t) => 1 - (1 - t) * (1 - t),\n    easeInOutQuad: (t) => t < 0.5\n        ? 2 * t * t\n        : 1 - Math.pow(-2 * t + 2, 2) / 2,\n\n    easeInCubic: (t) => t * t * t,\n    easeOutCubic: (t) => 1 - Math.pow(1 - t, 3),\n    easeInOutCubic: (t) => t < 0.5\n        ? 4 * t * t * t\n        : 1 - Math.pow(-2 * t + 2, 3) / 2,\n\n    // Overshoot (back easing)\n    easeOutBack: (t) => {\n        const c1 = 1.70158;\n        const c3 = c1 + 1;\n        return 1 + c3 * Math.pow(t - 1, 3) +\n               c1 * Math.pow(t - 1, 2);\n    },\n\n    // Elastic (spring-like)\n    easeOutElastic: (t) => {\n        if (t === 0 || t === 1) return t;\n        const p = 0.3;\n        return Math.pow(2, -10 * t) *\n               Math.sin((t - p / 4) *\n               (2 * Math.PI) / p) + 1;\n    },\n\n    // Bounce\n    easeOutBounce: (t) => {\n        const n1 = 7.5625;\n        const d1 = 2.75;\n        if (t < 1 / d1) return n1 * t * t;\n        if (t < 2 / d1) return n1 * (t -= 1.5 / d1) * t + 0.75;\n        if (t < 2.5 / d1) return n1 * (t -= 2.25 / d1) * t + 0.9375;\n        return n1 * (t -= 2.625 / d1) * t + 0.984375;\n    }\n};\n\n// CSS cubic-bezier equivalents\nconst cssEasings = {\n    ease: 'cubic-bezier(0.25, 0.1, 0.25, 1.0)',\n    easeIn: 'cubic-bezier(0.42, 0, 1.0, 1.0)',\n    easeOut: 'cubic-bezier(0, 0, 0.58, 1.0)',\n    easeInOut: 'cubic-bezier(0.42, 0, 0.58, 1.0)',\n    // Custom snappy ease\n    snappy: 'cubic-bezier(0.2, 0.8, 0.2, 1.0)'\n};\n\n// Using easing in animation\nfunction animateWithEasing(element, easing, duration) {\n    const start = performance.now();\n    function frame(time) {\n        const linear = Math.min((time - start) / duration, 1);\n        const eased = easing(linear);\n        element.style.transform = \`translateX(\\\${eased * 300}px)\`;\n        if (linear < 1) requestAnimationFrame(frame);\n    }\n    requestAnimationFrame(frame);\n}`,
    breakdown: `• Ease-out for entrances — Elements entering the screen should decelerate into place (ease-out). This feels like the element is arriving and settling, which matches user expectation for appearing content.\n\n• Ease-in for exits — Elements leaving should accelerate away (ease-in). The slow start gives users a moment to register the departure before it speeds off-screen.\n\n• Cubic-bezier in CSS — The four values define two control points of a bezier curve on a 1x1 grid. Y values > 1 or < 0 create overshoot. Tools like cubic-bezier.com let you visually design curves.\n\n• Duration guidelines — Micro-interactions: 100-200ms. Standard transitions: 200-400ms. Complex reveals: 400-700ms. Page transitions: 300-500ms. Longer isn't smoother — it's sluggish.\n\n• Spring physics — Real springs have mass, stiffness, and damping. Libraries like react-spring and Framer Motion use spring physics instead of duration-based easing, creating more natural and interruptible animations.`,
    summary: "Easing functions transform linear time into natural-feeling motion curves — accelerating, decelerating, overshooting, or bouncing. Choosing the right easing and duration for each context (entrances use ease-out, exits use ease-in) is critical for professional-feeling interfaces."
  },
  {
    title: "Skeletal Animation & Rigging",
    definition: "Skeletal animation uses a hierarchical bone structure (skeleton) bound to a mesh to deform and animate complex characters efficiently, where moving a parent bone automatically affects all connected child bones and their associated mesh vertices.",
    explanation: `Skeletal animation solves the problem of animating complex articulated characters like humans, animals, and robots. Rather than moving individual vertices of a 3D mesh (or points of a 2D sprite), animators manipulate a simplified bone hierarchy. The mesh is 'skinned' to the skeleton — each vertex is assigned influence weights from nearby bones, determining how much each bone's movement affects that vertex.\n\nThe skeleton is a tree structure: the root bone (usually the hips for humanoids) is the parent of the spine, which parents the chest, which parents both arms and the head. Moving the root moves everything. Rotating the upper arm rotates the forearm and hand automatically. This hierarchy makes complex poses achievable by adjusting relatively few bones.\n\nRigging is the process of building this control system. A rigger creates the skeleton, defines the hierarchy, paints skin weights (how much each bone influences each vertex), and adds higher-level controls like IK (Inverse Kinematics) handles. IK lets you position an end effector (like a foot) and automatically calculates the angles of all parent bones — essential for characters whose feet must stay planted on the ground.\n\nIn web and game contexts, skeletal animation is used in Spine (2D), DragonBones (2D), and various 3D engines. The Web Animations API and libraries like Three.js support skeletal animation for real-time web experiences. Animation data is compact — just bone transforms per frame rather than full mesh vertex positions.`,
    code: `// Skeletal Animation Concepts in JavaScript/Pseudocode\n\n// Bone hierarchy definition\nclass Bone {\n    constructor(name, parent = null) {\n        this.name = name;\n        this.parent = parent;\n        this.children = [];\n        this.localPosition = { x: 0, y: 0, z: 0 };\n        this.localRotation = { x: 0, y: 0, z: 0 };\n        this.localScale = { x: 1, y: 1, z: 1 };\n\n        if (parent) parent.children.push(this);\n    }\n\n    // World transform = parent world * local transform\n    getWorldTransform() {\n        const local = this.getLocalMatrix();\n        if (this.parent) {\n            return multiplyMatrices(\n                this.parent.getWorldTransform(),\n                local\n            );\n        }\n        return local;\n    }\n}\n\n// Build a simple humanoid skeleton\nconst root = new Bone('hips');\nconst spine = new Bone('spine', root);\nconst chest = new Bone('chest', spine);\nconst head = new Bone('head', chest);\nconst upperArmL = new Bone('upperArm_L', chest);\nconst lowerArmL = new Bone('lowerArm_L', upperArmL);\nconst handL = new Bone('hand_L', lowerArmL);\n\n// Skin weights: which bones affect each vertex\nconst skinWeights = [\n    // vertex 0: 70% upper arm, 30% lower arm\n    { vertex: 0, weights: [\n        { bone: 'upperArm_L', weight: 0.7 },\n        { bone: 'lowerArm_L', weight: 0.3 }\n    ]},\n    // vertex 1: 100% hand\n    { vertex: 1, weights: [\n        { bone: 'hand_L', weight: 1.0 }\n    ]}\n];\n\n// Apply skeletal deformation to mesh\nfunction deformMesh(vertices, skeleton, skinWeights) {\n    return vertices.map((vertex, i) => {\n        const weights = skinWeights[i].weights;\n        let finalPos = { x: 0, y: 0, z: 0 };\n\n        weights.forEach(({ bone, weight }) => {\n            const boneTransform = skeleton[bone].getWorldTransform();\n            const transformed = applyTransform(vertex, boneTransform);\n            finalPos.x += transformed.x * weight;\n            finalPos.y += transformed.y * weight;\n            finalPos.z += transformed.z * weight;\n        });\n\n        return finalPos;\n    });\n}`,
    breakdown: `• Bone hierarchy — Parent-child relationships mean rotating a shoulder automatically moves the elbow, wrist, and fingers. The skeleton tree propagates transforms from root to leaves via matrix multiplication.\n\n• Skin weights — Each mesh vertex can be influenced by up to 4 bones (for performance). Weight painting determines smooth deformation at joints — the elbow area blends between upper and lower arm bones.\n\n• Forward Kinematics (FK) — Rotate each bone explicitly from parent to child. Natural for arm swings and head turns where you set each joint angle directly.\n\n• Inverse Kinematics (IK) — Specify where the end of a chain should be (foot position) and solve backwards for joint angles. Essential for feet staying on ground and hands reaching targets.\n\n• Animation blending — Smoothly transition between animation clips (idle to walk to run) by interpolating bone transforms. Blend trees mix animations based on parameters like speed or direction.`,
    summary: "Skeletal animation uses hierarchical bone structures bound to meshes via skin weights, enabling complex character animation by manipulating relatively few bones. IK solvers, animation blending, and the parent-child transform propagation make this the standard approach for character animation in games and film."
  },
  {
    title: "Procedural Animation",
    definition: "Procedural animation generates motion algorithmically at runtime using mathematical functions, physics simulations, and rule-based systems rather than pre-authored keyframe data, enabling dynamic, responsive, and infinitely variable animations.",
    explanation: `Procedural animation creates movement through code rather than manually authored keyframes. Instead of an animator posing a character frame-by-frame, the computer generates motion based on rules, physics, or mathematical functions. This approach excels when animations need to respond to dynamic conditions — terrain, user input, or game state — that can't be predicted at authoring time.\n\nCommon procedural techniques include: sine-wave oscillation for breathing, hovering, and ambient motion; noise functions (Perlin/Simplex) for organic randomness like wind, fire, and camera shake; spring physics for responsive following and settling behavior; and constraint-based systems that maintain physical plausibility (feet staying on uneven ground).\n\nIn game development, procedural animation layers on top of keyframed animations. A walk cycle might be keyframed, but the character's lean into turns, foot placement on stairs, and head tracking a target are all procedural adjustments applied at runtime. This hybrid approach combines the artistic quality of hand-animated motion with the adaptability of procedural systems.\n\nFor web development, procedural animation powers particle systems, generative art, physics-based interactions (drag with momentum), and responsive ambient effects. The key advantage is infinite variation — no two instances of the animation look exactly alike, preventing the repetitive feel of looped keyframe animations.`,
    code: `// Procedural Animation Examples\n\n// 1. Sine wave hover/breathing effect\nfunction hoverAnimation(element, amplitude, frequency) {\n    let startTime = performance.now();\n\n    function frame(time) {\n        const elapsed = (time - startTime) / 1000;\n        const y = Math.sin(elapsed * frequency) * amplitude;\n        element.style.transform = \`translateY(\\\${y}px)\`;\n        requestAnimationFrame(frame);\n    }\n    requestAnimationFrame(frame);\n}\n\n// 2. Spring physics for smooth following\nclass Spring {\n    constructor(stiffness = 180, damping = 12) {\n        this.stiffness = stiffness;\n        this.damping = damping;\n        this.value = 0;\n        this.velocity = 0;\n        this.target = 0;\n    }\n\n    update(deltaTime) {\n        const force = -this.stiffness * (this.value - this.target);\n        const dampingForce = -this.damping * this.velocity;\n        this.velocity += (force + dampingForce) * deltaTime;\n        this.value += this.velocity * deltaTime;\n        return this.value;\n    }\n}\n\n// Mouse follower with spring physics\nconst springX = new Spring(120, 14);\nconst springY = new Spring(120, 14);\n\ndocument.addEventListener('mousemove', (e) => {\n    springX.target = e.clientX;\n    springY.target = e.clientY;\n});\n\nfunction updateFollower(element) {\n    const dt = 1 / 60;\n    const x = springX.update(dt);\n    const y = springY.update(dt);\n    element.style.transform = \`translate(\\\${x}px, \\\${y}px)\`;\n    requestAnimationFrame(() => updateFollower(element));\n}\n\n// 3. Perlin noise for organic motion\nfunction noise1D(x) {\n    // Simplified noise approximation\n    return Math.sin(x * 1.3) * 0.5 +\n           Math.sin(x * 2.7) * 0.3 +\n           Math.sin(x * 5.1) * 0.2;\n}\n\nfunction organicWander(element) {\n    let time = 0;\n    function frame() {\n        time += 0.01;\n        const x = noise1D(time) * 50;\n        const y = noise1D(time + 100) * 30;\n        const rotation = noise1D(time + 200) * 5;\n        element.style.transform =\n            \`translate(\\\${x}px, \\\${y}px) rotate(\\\${rotation}deg)\`;\n        requestAnimationFrame(frame);\n    }\n    requestAnimationFrame(frame);\n}`,
    breakdown: `• Sine waves — The fundamental building block of procedural motion. Varying amplitude, frequency, and phase creates breathing, hovering, pulsing, and wave effects. Combine multiple sine waves for complex organic patterns.\n\n• Spring simulation — Models mass-spring-damper systems. High stiffness = snappy response. High damping = no oscillation. Low damping = bouncy. Perfect for cursor followers, scroll physics, and elastic interactions.\n\n• Noise functions — Perlin/Simplex noise generates smooth, continuous randomness. Unlike Math.random(), noise is coherent (nearby inputs give similar outputs), creating natural-looking variation for wind, fire, and camera shake.\n\n• Layered procedural — Combine techniques at different frequencies: slow sine wave for major drift + fast noise for small jitter + spring response to user input. Each layer adds organic complexity.\n\n• Performance — Procedural animations run every frame, so keep calculations simple. Pre-compute expensive values, use lookup tables for complex math, and ensure spring simulations use fixed timesteps for stability.`,
    summary: "Procedural animation generates motion through code using sine waves, spring physics, and noise functions, producing dynamic, responsive animation that adapts to runtime conditions. It's essential for physics-based interactions, particle systems, and creating infinite variation that hand-animated keyframes cannot achieve."
  }
];
