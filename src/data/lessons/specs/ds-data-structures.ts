// Pre-written spec lessons for: Data Structures

export const dsDataStructuresLessons = [
  {
    title: "Arrays vs Linked Lists",
    definition: "Arrays and linked lists are the two most fundamental linear data structures in computer science. An array stores elements in contiguous memory locations with constant-time index access, while a linked list stores elements in nodes scattered throughout memory, each pointing to the next node via a reference.",

    explanation: `Arrays allocate a fixed block of contiguous memory. Because elements sit next to each other in RAM, accessing any element by index is O(1) — the CPU simply calculates the memory address as base + index * elementSize. This makes arrays ideal for random access patterns.\n\nHowever, arrays have significant drawbacks: inserting or deleting an element in the middle requires shifting all subsequent elements, making these operations O(n). Additionally, arrays have a fixed size — resizing requires allocating a new, larger block and copying all elements over (though dynamic arrays like JavaScript's Array amortize this cost).\n\nLinked lists solve the insertion/deletion problem elegantly. Each node contains data plus a pointer to the next node. Inserting or deleting at a known position is O(1) — you simply update the pointers. No shifting is required.\n\nThe tradeoff is that linked lists sacrifice random access. To reach the kth element, you must traverse k nodes from the head, making access O(n). Linked lists also use more memory per element due to storing pointers alongside data.\n\nIn practice, arrays are preferred when you need fast random access and know the size upfront (or can tolerate occasional resizing). Linked lists excel when you frequently insert/delete at arbitrary positions and primarily traverse sequentially.`,

    code: `// Linked List implementation in JavaScript\nclass Node {\n    constructor(value) {\n        this.value = value;\n        this.next = null;\n    }\n}\n\nclass LinkedList {\n    constructor() {\n        this.head = null;\n        this.size = 0;\n    }\n\n    // Insert at the beginning - O(1)\n    prepend(value) {\n        const newNode = new Node(value);\n        newNode.next = this.head;\n        this.head = newNode;\n        this.size++;\n    }\n\n    // Insert at end - O(n) for singly linked list\n    append(value) {\n        const newNode = new Node(value);\n        if (!this.head) {\n            this.head = newNode;\n        } else {\n            let current = this.head;\n            while (current.next) {\n                current = current.next;\n            }\n            current.next = newNode;\n        }\n        this.size++;\n    }\n\n    // Delete a node by value - O(n)\n    delete(value) {\n        if (!this.head) return null;\n        if (this.head.value === value) {\n            this.head = this.head.next;\n            this.size--;\n            return;\n        }\n        let current = this.head;\n        while (current.next && current.next.value !== value) {\n            current = current.next;\n        }\n        if (current.next) {\n            current.next = current.next.next;\n            this.size--;\n        }\n    }\n}\n\n// Comparison with Array operations:\n// Array: access O(1), insert at end O(1)*, insert at middle O(n)\n// Linked List: access O(n), insert at head O(1), insert at middle O(1)**\n// *amortized  **if you have a reference to the position`,

    breakdown: `• Memory layout — Arrays use contiguous memory (cache-friendly), while linked lists scatter nodes across the heap. This makes arrays significantly faster for sequential iteration due to CPU cache line prefetching.\n\n• Access time — Arrays provide O(1) random access via index arithmetic. Linked lists require O(n) traversal from the head to reach any arbitrary element.\n\n• Insertion/Deletion — Inserting into a linked list at a known node is O(1) pointer manipulation. Arrays require O(n) shifting of elements for mid-array insertions.\n\n• Memory overhead — Each linked list node stores an extra pointer (8 bytes on 64-bit systems). For small data types like integers, this can double memory usage compared to arrays.\n\n• Dynamic sizing — Linked lists grow and shrink naturally with each insertion/deletion. Arrays must be resized (reallocated and copied) when they exceed capacity, though dynamic arrays amortize this to O(1) per insertion.`,
    summary: "Arrays offer O(1) random access and cache-friendly memory layout but expensive mid-array insertions. Linked lists provide O(1) insertion/deletion at known positions but sacrifice random access. Choose based on your dominant access pattern: random reads favor arrays, frequent insertions/deletions favor linked lists."
  },

  {
    title: "Stacks & Queues",
    definition: "Stacks and queues are abstract data types that restrict how elements are added and removed. A stack follows Last-In-First-Out (LIFO) ordering — the most recently added element is removed first. A queue follows First-In-First-Out (FIFO) ordering — the earliest added element is removed first.",
    explanation: `A stack supports two primary operations: push (add to top) and pop (remove from top). Think of a stack of plates — you always add and remove from the top. This LIFO behavior makes stacks perfect for tracking nested operations: function call stacks, undo mechanisms, expression parsing, and backtracking algorithms.\n\nStacks can be implemented with either arrays or linked lists. Using an array, push and pop operate on the end of the array, both achieving O(1) time. Using a linked list, push and pop operate on the head, also O(1). The array implementation is more memory-efficient and cache-friendly.\n\nA queue supports enqueue (add to back) and dequeue (remove from front). Think of a line at a store — first person in line gets served first. FIFO ordering is essential for breadth-first search, task scheduling, buffering (like print queues), and handling asynchronous events.\n\nQueues are typically implemented with linked lists (O(1) enqueue and dequeue) or circular arrays. A naive array implementation would make dequeue O(n) due to shifting, but a circular array avoids this by wrapping around using modulo arithmetic.\n\nA deque (double-ended queue) generalizes both — it allows insertion and removal at both ends in O(1). JavaScript's array actually behaves like a deque with push/pop (back) and unshift/shift (front), though shift/unshift are O(n) internally.`,

    code: `// Stack implementation using an array\nclass Stack {\n    constructor() {\n        this.items = [];\n    }\n\n    push(element) {\n        this.items.push(element); // O(1)\n    }\n\n    pop() {\n        if (this.isEmpty()) return undefined;\n        return this.items.pop(); // O(1)\n    }\n\n    peek() {\n        return this.items[this.items.length - 1];\n    }\n\n    isEmpty() {\n        return this.items.length === 0;\n    }\n}\n\n// Queue implementation using a linked list\nclass QueueNode {\n    constructor(value) {\n        this.value = value;\n        this.next = null;\n    }\n}\n\nclass Queue {\n    constructor() {\n        this.front = null;\n        this.rear = null;\n        this.size = 0;\n    }\n\n    enqueue(element) {\n        const node = new QueueNode(element);\n        if (this.rear) {\n            this.rear.next = node;\n        }\n        this.rear = node;\n        if (!this.front) {\n            this.front = node;\n        }\n        this.size++;\n    }\n\n    dequeue() {\n        if (!this.front) return undefined;\n        const value = this.front.value;\n        this.front = this.front.next;\n        if (!this.front) this.rear = null;\n        this.size--;\n        return value;\n    }\n}\n\n// Practical example: validating balanced parentheses with a stack\nfunction isBalanced(str) {\n    const stack = new Stack();\n    const pairs = { ')': '(', ']': '[', '}': '{' };\n    for (const char of str) {\n        if ('([{'.includes(char)) {\n            stack.push(char);\n        } else if (')]}'.includes(char)) {\n            if (stack.pop() !== pairs[char]) return false;\n        }\n    }\n    return stack.isEmpty();\n}`,

    breakdown: `• LIFO vs FIFO — Stacks process the newest element first (like browser back button history). Queues process the oldest element first (like a printer job queue). This fundamental ordering dictates which problems each solves.\n\n• Time complexity — Both stacks and queues provide O(1) push/pop or enqueue/dequeue when properly implemented. Peek operations are also O(1). This constant-time guarantee is critical for performance.\n\n• Stack applications — Function call tracking (the call stack), expression evaluation (postfix notation), syntax parsing (matching brackets), DFS traversal, and undo/redo systems all rely on LIFO ordering.\n\n• Queue applications — BFS traversal, CPU task scheduling (round-robin), message queues in distributed systems, and buffering (streaming data) all require FIFO ordering to function correctly.\n\n• Implementation choice — Arrays are ideal for stacks (push/pop at end). Linked lists are ideal for queues (O(1) at both ends). Circular buffers offer a fixed-size array-based queue with O(1) operations and excellent cache performance.`,
    summary: "Stacks (LIFO) and queues (FIFO) are foundational abstract data types with O(1) core operations. Stacks are essential for recursion, parsing, and backtracking. Queues are essential for BFS, scheduling, and buffering. Understanding when to apply each is key to solving many algorithmic problems efficiently."
  },

  {
    title: "Hash Tables & Collision Resolution",
    definition: "A hash table (hash map) is a data structure that maps keys to values using a hash function to compute an index into an array of buckets. It provides average-case O(1) time complexity for insertions, deletions, and lookups, making it one of the most widely used data structures in software engineering.",
    explanation: `Hash tables work by converting keys into array indices through a hash function. Given a key, the hash function produces an integer, which is then mapped to an index using modulo: index = hash(key) % tableSize. The value is stored at that index in the underlying array.\n\nThe critical challenge is collisions — when two different keys hash to the same index. Since the hash function maps a potentially infinite key space to a finite array, collisions are inevitable (by the pigeonhole principle). How we handle collisions determines the hash table's real-world performance.\n\nChaining (separate chaining) handles collisions by storing a linked list at each bucket. When a collision occurs, the new key-value pair is simply appended to the list at that index. Lookup requires traversing the list at the computed index. Average case remains O(1) if the load factor (n/tableSize) stays low.\n\nOpen addressing handles collisions by probing for the next available slot within the array itself. Linear probing checks index+1, index+2, etc. Quadratic probing checks index+1², index+2², etc. Double hashing uses a second hash function to determine the probe step size. Open addressing has better cache performance but degrades as the table fills up.\n\nThe load factor (α = n/capacity) is the key metric. When α exceeds a threshold (typically 0.7-0.75), the table is resized (usually doubled) and all elements are rehashed. This keeps the average chain length short and probe sequences short, maintaining O(1) average performance.`,

    code: `// Hash Table with separate chaining\nclass HashTable {\n    constructor(size = 53) {\n        this.table = new Array(size);\n        this.size = size;\n        this.count = 0;\n    }\n\n    // Simple hash function for string keys\n    _hash(key) {\n        let total = 0;\n        const PRIME = 31; // Prime number reduces collisions\n        for (let i = 0; i < Math.min(key.length, 100); i++) {\n            const charCode = key.charCodeAt(i) - 96;\n            total = (total * PRIME + charCode) % this.size;\n        }\n        return total;\n    }\n\n    set(key, value) {\n        const index = this._hash(key);\n        if (!this.table[index]) {\n            this.table[index] = [];\n        }\n        // Check if key exists and update\n        const existing = this.table[index].find(pair => pair[0] === key);\n        if (existing) {\n            existing[1] = value;\n        } else {\n            this.table[index].push([key, value]);\n            this.count++;\n        }\n        // Resize if load factor > 0.75\n        if (this.count / this.size > 0.75) {\n            this._resize();\n        }\n    }\n\n    get(key) {\n        const index = this._hash(key);\n        if (!this.table[index]) return undefined;\n        const pair = this.table[index].find(pair => pair[0] === key);\n        return pair ? pair[1] : undefined;\n    }\n\n    delete(key) {\n        const index = this._hash(key);\n        if (!this.table[index]) return false;\n        const idx = this.table[index].findIndex(pair => pair[0] === key);\n        if (idx !== -1) {\n            this.table[index].splice(idx, 1);\n            this.count--;\n            return true;\n        }\n        return false;\n    }\n\n    _resize() {\n        const oldTable = this.table;\n        this.size = this.size * 2;\n        this.table = new Array(this.size);\n        this.count = 0;\n        for (const bucket of oldTable) {\n            if (bucket) {\n                for (const [key, value] of bucket) {\n                    this.set(key, value);\n                }\n            }\n        }\n    }\n}`,

    breakdown: `• Hash function quality — A good hash function distributes keys uniformly across buckets, minimizing collisions. It should be deterministic, fast to compute, and produce minimal clustering. Using prime numbers for table size helps distribute keys more evenly.\n\n• Separate chaining — Each bucket holds a linked list of key-value pairs. Simple to implement and handles high load factors gracefully (degrades linearly). Worst case is O(n) if all keys hash to the same bucket, but this is extremely rare with a good hash function.\n\n• Open addressing — All elements stored directly in the array. Better cache performance than chaining (no pointer chasing). However, deletion is complex (requires tombstone markers), and performance degrades sharply as load factor approaches 1.0.\n\n• Load factor and resizing — The load factor α = n/capacity determines average performance. Keeping α < 0.75 ensures most operations complete in 1-2 probes. Resizing doubles capacity and rehashes all elements in O(n), but happens infrequently enough to amortize to O(1).\n\n• Real-world usage — JavaScript Objects/Maps, Python dictionaries, Java HashMaps, and database indexes all use hash tables. They power caches, symbol tables in compilers, and route lookup in network routers.`,
    summary: "Hash tables achieve average O(1) lookups, insertions, and deletions by using hash functions to map keys to array indices. Collision resolution (chaining or open addressing) and load factor management via resizing are the keys to maintaining this performance. Hash tables are among the most important and frequently used data structures in all of computing."
  },

  {
    title: "Trees (Binary, BST, AVL)",
    definition: "A tree is a hierarchical data structure consisting of nodes connected by edges, with a single root node and no cycles. A Binary Search Tree (BST) maintains the invariant that left children are smaller and right children are larger than the parent. An AVL tree is a self-balancing BST that guarantees O(log n) operations by maintaining height balance.",
    explanation: `Binary trees are trees where each node has at most two children (left and right). They form the basis for many efficient data structures. A full binary tree has every node with either 0 or 2 children. A complete binary tree has all levels fully filled except possibly the last, which is filled left to right.\n\nA Binary Search Tree (BST) adds an ordering property: for every node, all values in the left subtree are less than the node's value, and all values in the right subtree are greater. This property enables binary search — at each node, you eliminate half the remaining elements. Average-case search, insertion, and deletion are all O(log n).\n\nHowever, BSTs can degenerate. If you insert sorted data into a BST, it becomes a linked list with O(n) operations. For example, inserting 1, 2, 3, 4, 5 creates a tree where every node only has a right child. This worst case motivates self-balancing trees.\n\nAVL trees solve this by maintaining a balance factor at each node: the difference in height between left and right subtrees must be at most 1. When an insertion or deletion violates this property, rotations restore balance. There are four rotation cases: left rotation, right rotation, left-right rotation, and right-left rotation.\n\nAVL trees guarantee O(log n) for all operations because the tree height is always O(log n). The tradeoff is additional complexity during insertions/deletions due to rotation logic and storing height/balance information at each node.`,

    code: `// Binary Search Tree implementation\nclass BSTNode {\n    constructor(value) {\n        this.value = value;\n        this.left = null;\n        this.right = null;\n    }\n}\n\nclass BST {\n    constructor() {\n        this.root = null;\n    }\n\n    // Insert a value - O(log n) average, O(n) worst\n    insert(value) {\n        const newNode = new BSTNode(value);\n        if (!this.root) {\n            this.root = newNode;\n            return;\n        }\n        let current = this.root;\n        while (true) {\n            if (value < current.value) {\n                if (!current.left) {\n                    current.left = newNode;\n                    return;\n                }\n                current = current.left;\n            } else {\n                if (!current.right) {\n                    current.right = newNode;\n                    return;\n                }\n                current = current.right;\n            }\n        }\n    }\n\n    // Search for a value - O(log n) average\n    search(value) {\n        let current = this.root;\n        while (current) {\n            if (value === current.value) return current;\n            if (value < current.value) current = current.left;\n            else current = current.right;\n        }\n        return null;\n    }\n\n    // In-order traversal returns sorted values\n    inOrder(node = this.root, result = []) {\n        if (node) {\n            this.inOrder(node.left, result);\n            result.push(node.value);\n            this.inOrder(node.right, result);\n        }\n        return result;\n    }\n}\n\n// AVL rotation concept (right rotation)\n// When left subtree is too tall:\n//       30              20\n//      /  \\            /  \\\n//    20    40  =>    10    30\n//   /  \\                  /  \\\n//  10   25              25    40`,

    breakdown: `• BST property — For every node N, all nodes in N's left subtree have values < N, and all nodes in N's right subtree have values > N. This property enables O(log n) binary search and in-order traversal produces sorted output.\n\n• Tree traversals — In-order (left, root, right) gives sorted order. Pre-order (root, left, right) is useful for copying/serializing trees. Post-order (left, right, root) is useful for deletion and expression evaluation.\n\n• Degeneration problem — An unbalanced BST can degrade to O(n) operations. Inserting elements in sorted order creates a linked list structure. This motivates self-balancing trees like AVL and Red-Black trees.\n\n• AVL rotations — Four cases: Left-Left (single right rotation), Right-Right (single left rotation), Left-Right (left then right rotation), Right-Left (right then left rotation). Each rotation takes O(1) time and restores the balance factor to {-1, 0, 1}.\n\n• Practical tradeoffs — AVL trees are more strictly balanced than Red-Black trees, making lookups faster. But AVL trees require more rotations during insertion/deletion. Red-Black trees are often preferred in practice (used in Java TreeMap, C++ std::map) because they require fewer rebalancing operations.`,
    summary: "Binary Search Trees provide O(log n) average-case operations through the BST ordering property, but can degenerate to O(n) with sorted input. AVL trees guarantee O(log n) worst-case by maintaining height balance through rotations. Understanding tree structures is fundamental to databases, file systems, and efficient searching."
  },

  {
    title: "Heaps & Priority Queues",
    definition: "A heap is a complete binary tree that satisfies the heap property: in a max-heap, every parent node is greater than or equal to its children; in a min-heap, every parent is less than or equal to its children. A priority queue is an abstract data type where elements are dequeued based on priority rather than insertion order, and heaps are the standard implementation.",
    explanation: `Heaps are remarkable because they combine the structure of a complete binary tree with efficient array storage. Since a heap is always complete (filled level by level, left to right), it can be stored in an array without any pointers: for a node at index i, its left child is at 2i+1, right child at 2i+2, and parent at Math.floor((i-1)/2).\n\nThe heap property ensures that the maximum (max-heap) or minimum (min-heap) element is always at the root (index 0). This makes finding the extreme element O(1). Insertion and deletion maintain the heap property through two key operations:\n\nBubble-up (swim): After inserting at the end of the array, compare the new element with its parent and swap if it violates the heap property. Repeat until the element reaches its correct position. This is O(log n) since the tree height is log n.\n\nBubble-down (sink): After removing the root (and replacing it with the last element), compare the element with its children and swap with the larger/smaller child as needed. Repeat until the heap property is restored. Also O(log n).\n\nPriority queues use heaps to efficiently serve the highest-priority element. Applications include Dijkstra's shortest path algorithm, event-driven simulation, job scheduling in operating systems, and the heap sort algorithm. Building a heap from an unsorted array can be done in O(n) time using bottom-up heap construction, which is faster than inserting elements one by one (O(n log n)).`,

    code: `// Min-Heap (Priority Queue) implementation\nclass MinHeap {\n    constructor() {\n        this.heap = [];\n    }\n\n    // Get parent/child indices\n    parent(i) { return Math.floor((i - 1) / 2); }\n    leftChild(i) { return 2 * i + 1; }\n    rightChild(i) { return 2 * i + 2; }\n\n    // Swap two elements\n    swap(i, j) {\n        [this.heap[i], this.heap[j]] = [this.heap[j], this.heap[i]];\n    }\n\n    // Insert element - O(log n)\n    insert(value) {\n        this.heap.push(value);\n        this._bubbleUp(this.heap.length - 1);\n    }\n\n    // Remove and return minimum - O(log n)\n    extractMin() {\n        if (this.heap.length === 0) return undefined;\n        const min = this.heap[0];\n        const last = this.heap.pop();\n        if (this.heap.length > 0) {\n            this.heap[0] = last;\n            this._bubbleDown(0);\n        }\n        return min;\n    }\n\n    // Peek at minimum without removing - O(1)\n    peek() {\n        return this.heap[0];\n    }\n\n    _bubbleUp(index) {\n        while (index > 0) {\n            const parentIdx = this.parent(index);\n            if (this.heap[parentIdx] <= this.heap[index]) break;\n            this.swap(parentIdx, index);\n            index = parentIdx;\n        }\n    }\n\n    _bubbleDown(index) {\n        const length = this.heap.length;\n        while (true) {\n            let smallest = index;\n            const left = this.leftChild(index);\n            const right = this.rightChild(index);\n            if (left < length && this.heap[left] < this.heap[smallest]) {\n                smallest = left;\n            }\n            if (right < length && this.heap[right] < this.heap[smallest]) {\n                smallest = right;\n            }\n            if (smallest === index) break;\n            this.swap(index, smallest);\n            index = smallest;\n        }\n    }\n}\n\n// Usage: find k smallest elements\nfunction kSmallest(arr, k) {\n    const heap = new MinHeap();\n    arr.forEach(val => heap.insert(val));\n    const result = [];\n    for (let i = 0; i < k; i++) {\n        result.push(heap.extractMin());\n    }\n    return result;\n}`,

    breakdown: `• Array representation — A complete binary tree maps perfectly to an array: node at index i has children at 2i+1 and 2i+2, parent at floor((i-1)/2). No pointers needed, excellent cache performance, and zero wasted space.\n\n• Heap operations — Insert (O(log n)): add to end, bubble up. Extract-min/max (O(log n)): remove root, replace with last element, bubble down. Peek (O(1)): return root. Build heap from array: O(n) using bottom-up construction.\n\n• Priority queue applications — Dijkstra's algorithm uses a min-heap to always process the closest unvisited vertex. OS schedulers use priority queues to run highest-priority tasks first. Event systems process events in chronological order.\n\n• Heap sort — Build a max-heap from the array (O(n)), then repeatedly extract the maximum and place it at the end. Total: O(n log n), in-place, but not stable. Useful when you need guaranteed O(n log n) without extra space.\n\n• Variations — Binary heap is most common, but d-ary heaps (d children per node) can be faster for decrease-key operations. Fibonacci heaps provide O(1) amortized insert and decrease-key, used in theoretical optimal Dijkstra's implementation.`,
    summary: "Heaps are complete binary trees stored in arrays that maintain parent-child ordering. They power priority queues with O(log n) insert and extract-min/max, plus O(1) peek. Heaps are essential for graph algorithms (Dijkstra's, Prim's), sorting (heap sort), and any scenario requiring efficient access to the extreme element."
  }
];
