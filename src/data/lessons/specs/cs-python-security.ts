// Pre-written spec lessons for: Python for Security

export const csPythonSecurityLessons = [
  {
    title: "Python for Security Automation",
    definition: "Python is the dominant programming language in cybersecurity due to its extensive standard library, vast ecosystem of security-focused packages, readable syntax for rapid prototyping, and cross-platform compatibility. Security professionals use Python for automating penetration testing tasks, building custom tools, parsing logs, interacting with APIs, and orchestrating complex security workflows.",

    explanation: `Python's dominance in security comes from several factors: the standard library includes modules for networking (socket, http), cryptography (hashlib, ssl), file manipulation (os, shutil), and process management (subprocess). Third-party libraries like Scapy (packet manipulation), Requests (HTTP), Paramiko (SSH), and PyCryptodome (cryptography) provide powerful capabilities with minimal code.\n\nSecurity automation eliminates repetitive manual tasks and ensures consistency. Common automation targets include: vulnerability scanning (checking hundreds of hosts for known CVEs), log analysis (parsing gigabytes of logs for indicators of compromise), incident response (automatically collecting forensic artifacts), compliance checking (validating configurations against security baselines), and reporting (generating formatted security assessment reports).\n\nPython's dynamic typing and interpreted nature make it ideal for rapid prototyping during penetration tests. When you discover a vulnerability, you can write a custom exploit or tool in minutes rather than hours. The REPL (interactive interpreter) allows testing code snippets live. Libraries like pwntools provide exploit development primitives, and frameworks like Metasploit have Python APIs.\n\nProfessional security tools written in Python include: Volatility (memory forensics), sqlmap (SQL injection automation), theHarvester (OSINT email harvesting), Impacket (network protocol manipulation), Prowler (AWS security auditing), and countless internal tools at security companies. Understanding Python is essentially a prerequisite for most security engineering roles.`,

    code: `# Python Security Automation - Foundation Script\nimport socket\nimport hashlib\nimport subprocess\nimport json\nimport os\nfrom datetime import datetime\nfrom pathlib import Path\n\n# === Security Automation Framework ===\nclass SecurityAutomation:\n    """Base class for security automation tasks"""\n    \n    def __init__(self, target: str, output_dir: str = "./reports"):\n        self.target = target\n        self.output_dir = Path(output_dir)\n        self.output_dir.mkdir(exist_ok=True)\n        self.results = []\n        self.start_time = datetime.now()\n    \n    def log(self, message: str, level: str = "INFO"):\n        """Structured logging for audit trail"""\n        timestamp = datetime.now().isoformat()\n        entry = f"[{timestamp}] [{level}] {message}"\n        self.results.append(entry)\n        print(entry)\n    \n    def check_port(self, port: int, timeout: float = 1.0) -> bool:\n        """Check if a single TCP port is open"""\n        try:\n            sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n            sock.settimeout(timeout)\n            result = sock.connect_ex((self.target, port))\n            sock.close()\n            return result == 0\n        except socket.error:\n            return False\n    \n    def file_integrity_check(self, filepath: str) -> dict:\n        """Calculate file hash for integrity monitoring"""\n        with open(filepath, 'rb') as f:\n            content = f.read()\n        return {\n            'file': filepath,\n            'md5': hashlib.md5(content).hexdigest(),\n            'sha256': hashlib.sha256(content).hexdigest(),\n            'size': len(content),\n            'checked_at': datetime.now().isoformat()\n        }\n    \n    def generate_report(self) -> str:\n        """Generate JSON security report"""\n        report = {\n            'target': self.target,\n            'start_time': self.start_time.isoformat(),\n            'end_time': datetime.now().isoformat(),\n            'findings': self.results\n        }\n        report_path = self.output_dir / f"report_{self.target}.json"\n        with open(report_path, 'w') as f:\n            json.dump(report, f, indent=2)\n        return str(report_path)\n\n# Usage example\nscanner = SecurityAutomation("192.168.1.1")\nscanner.log("Starting security assessment")\nfor port in [22, 80, 443, 8080, 3306]:\n    if scanner.check_port(port):\n        scanner.log(f"Port {port} is OPEN", "FINDING")\nscanner.log("Assessment complete")`,

    breakdown: `• SecurityAutomation base class — Provides reusable infrastructure (logging, reporting, timing) that all security scripts need. Professional tools always include structured logging for audit trails and reproducibility.\n\n• Socket-based port checking — Uses low-level TCP connections to detect open ports. connect_ex() returns 0 on success (port open) or an error code (port closed/filtered). The timeout prevents hanging on filtered ports.\n\n• File integrity monitoring — Calculates cryptographic hashes of files to detect unauthorized modifications. Security tools like OSSEC and Tripwire use this principle. Comparing hashes over time reveals if system files have been tampered with.\n\n• JSON report generation — Structured output enables integration with other tools (SIEM systems, dashboards, ticketing). Professional security assessments always produce documented, reproducible reports.\n\n• Modular design pattern — The class-based approach allows extending functionality through inheritance. You might create PortScanner, VulnerabilityChecker, and ComplianceAuditor subclasses that share the logging and reporting infrastructure.`,
    summary: "Python's rich standard library and security-focused ecosystem make it the language of choice for security automation. Building reusable frameworks with structured logging, modular design, and report generation transforms manual security tasks into repeatable, auditable processes."
  },

  {
    title: "Network Scanning with Python",
    definition: "Network scanning is the process of discovering active hosts, open ports, running services, and potential vulnerabilities across a network. Python enables building custom scanners that range from simple TCP connect scans to sophisticated service fingerprinting tools, providing the foundation for both defensive network monitoring and authorized penetration testing.",

    explanation: `Network scanning begins with host discovery — determining which IP addresses on a network have active systems. Techniques include ICMP ping (easily blocked by firewalls), TCP SYN to common ports (more reliable), and ARP requests (works on local networks). Python's socket library handles TCP scanning, while Scapy enables raw packet crafting for SYN scans and ARP discovery.\n\nPort scanning determines which services are accessible on a target. TCP Connect scans complete the full three-way handshake (SYN, SYN-ACK, ACK) — reliable but easily detected. SYN scans send only SYN packets and interpret responses: SYN-ACK means open, RST means closed, no response means filtered. UDP scanning sends UDP datagrams and waits for ICMP port unreachable responses (slow and unreliable).\n\nService fingerprinting goes beyond port discovery to identify what software is running. Techniques include: banner grabbing (connecting and reading the server's greeting message), probe-response analysis (sending specific inputs and analyzing responses), and version detection (comparing responses against known signatures). For example, connecting to port 22 might reveal "SSH-2.0-OpenSSH_8.9" — the exact software version, useful for vulnerability correlation.\n\nConcurrency is essential for practical scanning. Scanning 65535 ports sequentially with 1-second timeouts takes 18+ hours. Python's concurrent.futures (ThreadPoolExecutor), asyncio, or multiprocessing can scan thousands of ports in seconds. However, aggressive scanning can overwhelm targets or trigger IDS alerts, so rate limiting and permission are crucial.`,

    code: `# Educational purposes only - Network Scanner\n# Only scan networks you own or have written authorization to test\nimport socket\nimport concurrent.futures\nfrom typing import List, Dict, Tuple\nfrom datetime import datetime\n\nclass NetworkScanner:\n    """Multi-threaded network port scanner"""\n    \n    # Common service ports and their typical services\n    COMMON_PORTS = {\n        21: 'FTP', 22: 'SSH', 23: 'Telnet', 25: 'SMTP',\n        53: 'DNS', 80: 'HTTP', 110: 'POP3', 143: 'IMAP',\n        443: 'HTTPS', 445: 'SMB', 993: 'IMAPS', 995: 'POP3S',\n        3306: 'MySQL', 3389: 'RDP', 5432: 'PostgreSQL',\n        6379: 'Redis', 8080: 'HTTP-Alt', 8443: 'HTTPS-Alt',\n        27017: 'MongoDB'\n    }\n    \n    def __init__(self, target: str, timeout: float = 1.0):\n        self.target = target\n        self.timeout = timeout\n        self.open_ports = []\n    \n    def resolve_target(self) -> str:\n        """Resolve hostname to IP address"""\n        try:\n            ip = socket.gethostbyname(self.target)\n            print(f"Target: {self.target} -> {ip}")\n            return ip\n        except socket.gaierror:\n            raise ValueError(f"Cannot resolve: {self.target}")\n    \n    def scan_port(self, port: int) -> Tuple[int, bool, str]:\n        """Scan a single port with banner grab attempt"""\n        try:\n            sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n            sock.settimeout(self.timeout)\n            result = sock.connect_ex((self.target, port))\n            \n            banner = ""\n            if result == 0:\n                # Attempt banner grab\n                try:\n                    sock.send(b"HEAD / HTTP/1.0\\r\\n\\r\\n\")\n                    banner = sock.recv(1024).decode('utf-8', errors='ignore').strip()\n                except (socket.timeout, ConnectionResetError):\n                    pass\n            sock.close()\n            return (port, result == 0, banner)\n        except socket.error:\n            return (port, False, "")\n    \n    def scan_range(self, ports: List[int] = None, max_threads: int = 100) -> List[Dict]:\n        """Scan multiple ports using thread pool"""\n        if ports is None:\n            ports = list(self.COMMON_PORTS.keys())\n        \n        print(f"Scanning {len(ports)} ports on {self.target}...")\n        start = datetime.now()\n        results = []\n        \n        with concurrent.futures.ThreadPoolExecutor(max_workers=max_threads) as executor:\n            futures = {executor.submit(self.scan_port, port): port for port in ports}\n            \n            for future in concurrent.futures.as_completed(futures):\n                port, is_open, banner = future.result()\n                if is_open:\n                    service = self.COMMON_PORTS.get(port, 'Unknown')\n                    results.append({\n                        'port': port,\n                        'service': service,\n                        'banner': banner[:100] if banner else 'N/A'\n                    })\n        \n        elapsed = (datetime.now() - start).total_seconds()\n        print(f"Scan complete in {elapsed:.2f}s - {len(results)} open ports\")\n        return sorted(results, key=lambda x: x['port'])\n\n# Usage (authorized targets only!)\nscanner = NetworkScanner("scanme.nmap.org")  # nmap's public test host\nresults = scanner.scan_range()\nfor r in results:\n    print(f"  Port {r['port']:5d} [{r['service']:10s}] {r['banner'][:50]}")`,

    breakdown: `• ThreadPoolExecutor for concurrency — Spawns up to 100 threads scanning ports simultaneously. This reduces scan time from minutes to seconds. The as_completed() iterator processes results as they arrive, enabling real-time output.\n\n• Banner grabbing — After confirming a port is open, sends an HTTP HEAD request and reads the response. Servers often reveal software versions (Apache/2.4.52, OpenSSH_8.9) which attackers use to identify vulnerabilities.\n\n• Service identification — Mapping port numbers to expected services provides context. Port 3306 open suggests MySQL; port 6379 suggests Redis (often unsecured by default). This guides further investigation.\n\n• Socket timeout management — The 1-second timeout balances speed against reliability. Filtered ports (blocked by firewall) never respond, so without timeouts each would hang indefinitely. Shorter timeouts risk false negatives on slow networks.\n\n• Authorization is critical — Unauthorized port scanning is illegal in most jurisdictions. Always have written permission (scope of engagement document) before scanning. nmap.org/scanme is explicitly provided for safe testing.`,
    summary: "Network scanning with Python combines socket programming for port detection, threading for performance, and banner grabbing for service identification. Multi-threaded scanning can check thousands of ports in seconds, providing the reconnaissance foundation for security assessments."
  },

  {
    title: "Password Cracking Techniques",
    definition: "Password cracking is the process of recovering plaintext passwords from their stored hash representations. Security professionals use these techniques to audit password policies, test authentication systems during penetration tests, and demonstrate the importance of strong password practices. Understanding cracking methods is essential for building effective password storage defenses.",

    explanation: `Password cracking techniques include: brute force (trying all possible combinations), dictionary attacks (trying common passwords from wordlists), rule-based attacks (applying transformations to dictionary words — capitalize first letter, append numbers, leet-speak substitutions), rainbow tables (pre-computed hash lookup tables), and credential stuffing (testing breached username/password pairs against other services).\n\nThe speed of cracking depends on the hashing algorithm used. A modern GPU can compute approximately: 10 billion MD5 hashes/second, 3 billion SHA-1 hashes/second, 1 billion SHA-256 hashes/second. But only about 100,000 bcrypt hashes/second (cost factor 10) or 10 scrypt hashes/second. This is why password-specific hash functions are critical — they make each guess expensive.\n\nSalting defeats rainbow tables but not brute-force attacks. A salt is random data added to the password before hashing. Each user gets a unique salt, meaning identical passwords produce different hashes. An attacker must crack each hash individually rather than using pre-computed tables. However, if the hashing is fast (SHA-256), brute force is still viable against weak passwords.\n\nDefense strategies: enforce minimum password length (12+ characters), check against common password databases (like HaveIBeenPwned), use slow hash functions (bcrypt with cost 12+, Argon2id), implement account lockout after failed attempts, require multi-factor authentication (MFA), and educate users about passphrases. A random 4-word passphrase (correct-horse-battery-staple) has more entropy than most complex 8-character passwords.`,

    code: `# Educational purposes only - Password Auditing Techniques\n# Use only on systems you own or have authorization to test\nimport hashlib\nimport itertools\nimport time\nfrom typing import Optional\n\nclass PasswordAuditor:\n    """Demonstrates password cracking techniques for security auditing"""\n    \n    def __init__(self):\n        self.attempts = 0\n    \n    def hash_password(self, password: str, salt: str = "", algo: str = "sha256") -> str:\n        """Hash a password (simulating what's stored in a database)"""\n        salted = (salt + password).encode()\n        if algo == "md5":\n            return hashlib.md5(salted).hexdigest()\n        return hashlib.sha256(salted).hexdigest()\n    \n    def dictionary_attack(self, target_hash: str, wordlist: list, \n                          salt: str = "", algo: str = "sha256") -> Optional[str]:\n        """Try passwords from a wordlist"""\n        self.attempts = 0\n        for word in wordlist:\n            self.attempts += 1\n            if self.hash_password(word, salt, algo) == target_hash:\n                return word\n        return None\n    \n    def rule_based_attack(self, target_hash: str, base_words: list,\n                          salt: str = "", algo: str = "sha256") -> Optional[str]:\n        """Apply common transformations to dictionary words"""\n        self.attempts = 0\n        \n        def generate_variants(word):\n            """Common password patterns people use"""\n            variants = [\n                word,                    # original\n                word.capitalize(),       # Capitalize\n                word.upper(),            # UPPERCASE\n                word + "123",            # Append numbers\n                word + "!",             # Append symbol\n                word + "2024",          # Append year\n                word[0].upper() + word[1:] + "1",  # Cap + number\n                word.replace('a', '@').replace('e', '3'),  # Leet speak\n            ]\n            return variants\n        \n        for word in base_words:\n            for variant in generate_variants(word):\n                self.attempts += 1\n                if self.hash_password(variant, salt, algo) == target_hash:\n                    return variant\n        return None\n    \n    def brute_force(self, target_hash: str, charset: str, max_length: int = 4,\n                    salt: str = "", algo: str = "sha256") -> Optional[str]:\n        """Try all combinations (exponentially slow for longer passwords)"""\n        self.attempts = 0\n        for length in range(1, max_length + 1):\n            for combo in itertools.product(charset, repeat=length):\n                self.attempts += 1\n                guess = ''.join(combo)\n                if self.hash_password(guess, salt, algo) == target_hash:\n                    return guess\n        return None\n\n# === Demonstration ===\nauditor = PasswordAuditor()\n\n# Simulate a leaked database with SHA-256 hashes\nweak_password = "password123"\ntarget_hash = auditor.hash_password(weak_password)\n\n# Dictionary attack with common passwords\nprint("=== Dictionary Attack ===")\ncommon_passwords = ["123456", "password", "password123", "admin", "letmein",\n                    "welcome", "monkey", "dragon", "master", "qwerty"]\n\nstart = time.time()\nresult = auditor.dictionary_attack(target_hash, common_passwords)\nprint(f"  Found: '{result}' in {auditor.attempts} attempts\")\nprint(f"  Time: {time.time()-start:.4f}s\\n\")\n\n# Rule-based attack\nprint("=== Rule-Based Attack ===")\nstrong_password = "Summer2024!"\ntarget2 = auditor.hash_password(strong_password)\nbase_words = ["spring", "summer", "autumn", "winter", "password"]\n\nstart = time.time()\nresult2 = auditor.rule_based_attack(target2, base_words)\nprint(f"  Found: '{result2}' in {auditor.attempts} attempts\")\nprint(f"  Time: {time.time()-start:.4f}s\\n\")\n\n# Brute force (only practical for very short passwords)\nprint("=== Brute Force (4-char max) ===")\npin = "1337"\ntarget3 = auditor.hash_password(pin)\nstart = time.time()\nresult3 = auditor.brute_force(target3, "0123456789", max_length=4)\nprint(f"  Found: '{result3}' in {auditor.attempts} attempts\")\nprint(f"  Time: {time.time()-start:.4f}s")`,

    breakdown: `• Dictionary attack efficiency — Common password lists (like rockyou.txt with 14 million entries) crack a significant percentage of real passwords because humans choose predictable words. Most breached databases reveal that "123456" and "password" are always in the top 5.\n\n• Rule-based transformations — People think "Password1!" is strong, but rule engines try exactly these patterns: capitalize first letter, append numbers/symbols, leet-speak substitutions. Tools like Hashcat have hundreds of built-in rules that cover common human password construction patterns.\n\n• Brute force scaling problem — A 4-digit PIN has 10,000 combinations (instant). A 6-char lowercase password has 26^6 = 308 million combinations. An 8-char mixed-case+digits password has 62^8 = 218 trillion combinations. Length is the most effective defense against brute force.\n\n• Salt prevents pre-computation — Without a salt, an attacker can compute hashes for millions of common passwords once and look up any hash instantly (rainbow tables). With per-user salts, each hash must be attacked individually, multiplying the attacker's work by the number of users.\n\n• Defense recommendation — Use Argon2id (memory-hard, resistant to GPU attacks) with passwords of 12+ characters. Combined with account lockout and MFA, even weak passwords become impractical to exploit through direct cracking.`,
    summary: "Password cracking ranges from instant dictionary attacks against common passwords to infeasible brute-force against long random strings. Rule-based attacks defeat predictable human patterns. The defense triad is: slow hash functions (bcrypt/Argon2), enforced length requirements, and multi-factor authentication."
  },

  {
    title: "Web Scraping for OSINT",
    definition: "Open Source Intelligence (OSINT) involves collecting and analyzing publicly available information to build intelligence about targets. Python web scraping automates the gathering of data from websites, social media, public records, DNS records, and other open sources to support security assessments, threat intelligence, and reconnaissance during authorized penetration tests.",

    explanation: `OSINT reconnaissance is typically the first phase of any security assessment. Before attempting technical attacks, penetration testers gather information about the target: employee names and email formats (for phishing), technology stacks (from job postings and HTTP headers), subdomains and IP ranges (DNS enumeration), exposed credentials (breach databases), and organizational structure (LinkedIn).\n\nPython's Requests library handles HTTP communication, while BeautifulSoup and lxml parse HTML responses. For JavaScript-heavy sites, Selenium or Playwright provide browser automation. APIs (Shodan, VirusTotal, HaveIBeenPwned) offer structured data access. The combination enables building comprehensive intelligence gathering tools.\n\nKey OSINT data sources include: DNS records (subdomains, mail servers, TXT records with SPF/DKIM info), WHOIS data (domain registration, organization details), web archives (Wayback Machine reveals old pages, removed content, configuration files), certificate transparency logs (CT logs list all SSL certificates issued for a domain, revealing subdomains), GitHub/GitLab (accidental credential commits, internal tool names, infrastructure details), and job postings (technology stacks, security tool usage).\n\nEthical considerations are paramount: respect robots.txt, rate-limit requests to avoid denial-of-service, only access publicly available data, comply with GDPR and privacy laws, never access private accounts or bypass access controls. OSINT is legal because it uses public information, but the volume and combination of public data can create privacy concerns. Always operate within your engagement scope.`,

    code: `# Educational purposes only - OSINT Web Scraping\n# Respect robots.txt, rate limits, and legal boundaries\nimport requests\nfrom typing import List, Dict, Set\nimport re\nimport json\nimport time\nfrom urllib.parse import urljoin, urlparse\n\nclass OSINTGatherer:\n    """Automated OSINT collection from public sources"""\n    \n    def __init__(self, target_domain: str):\n        self.domain = target_domain\n        self.session = requests.Session()\n        self.session.headers.update({\n            'User-Agent': 'Mozilla/5.0 (Security Research)'\n        })\n        self.findings = {}\n    \n    def enumerate_subdomains_ct(self) -> List[str]:\n        """Find subdomains via Certificate Transparency logs"""\n        # crt.sh is a public CT log search engine\n        url = f"https://crt.sh/?q=%.{self.domain}&output=json"\n        try:\n            resp = self.session.get(url, timeout=10)\n            if resp.status_code == 200:\n                data = resp.json()\n                subdomains = set()\n                for entry in data:\n                    name = entry.get('name_value', '')\n                    # CT logs may contain wildcards and newlines\n                    for sub in name.split('\\n'):\n                        sub = sub.strip().lstrip('*.')\n                        if sub.endswith(self.domain):\n                            subdomains.add(sub)\n                self.findings['subdomains'] = sorted(subdomains)\n                return sorted(subdomains)\n        except Exception as e:\n            print(f"CT log query failed: {e}")\n        return []\n    \n    def check_security_headers(self, url: str = None) -> Dict:\n        """Analyze HTTP security headers"""\n        if url is None:\n            url = f"https://{self.domain}"\n        \n        try:\n            resp = self.session.get(url, timeout=5, allow_redirects=True)\n            headers = resp.headers\n            \n            security_headers = {\n                'Strict-Transport-Security': headers.get('Strict-Transport-Security', 'MISSING'),\n                'Content-Security-Policy': headers.get('Content-Security-Policy', 'MISSING'),\n                'X-Frame-Options': headers.get('X-Frame-Options', 'MISSING'),\n                'X-Content-Type-Options': headers.get('X-Content-Type-Options', 'MISSING'),\n                'X-XSS-Protection': headers.get('X-XSS-Protection', 'MISSING'),\n                'Server': headers.get('Server', 'Not disclosed'),\n                'X-Powered-By': headers.get('X-Powered-By', 'Not disclosed'),\n            }\n            self.findings['security_headers'] = security_headers\n            return security_headers\n        except Exception as e:\n            return {'error': str(e)}\n    \n    def find_email_pattern(self, sample_emails: List[str]) -> str:\n        """Deduce email format from known addresses"""\n        patterns = []\n        for email in sample_emails:\n            local = email.split('@')[0]\n            if '.' in local:\n                patterns.append('firstname.lastname')\n            elif local[0].isalpha() and local[1:].isalpha():\n                patterns.append('firstinitial+lastname')\n        most_common = max(set(patterns), key=patterns.count) if patterns else 'unknown'\n        return most_common\n    \n    def generate_report(self) -> str:\n        """Compile OSINT findings into structured report"""\n        report = {\n            'target': self.domain,\n            'timestamp': time.strftime('%Y-%m-%d %H:%M:%S'),\n            'findings': self.findings\n        }\n        return json.dumps(report, indent=2)\n\n# Usage example\ngatherer = OSINTGatherer("example.com")\nprint("=== Subdomain Enumeration (CT Logs) ===")\nsubdomains = gatherer.enumerate_subdomains_ct()\nfor sub in subdomains[:10]:\n    print(f"  {sub}")\n\nprint("\\n=== Security Header Analysis ===")\nheaders = gatherer.check_security_headers()\nfor header, value in headers.items():\n    status = "OK" if value != "MISSING" else "MISSING"\n    print(f"  {header}: [{status}] {value[:50]}")`,

    breakdown: `• Certificate Transparency logs — CAs must publicly log all issued certificates. Querying crt.sh reveals all subdomains that have SSL certificates, often including internal tools (jira.company.com, vpn.company.com) that aren't meant to be public knowledge.\n\n• Security header analysis — Missing headers reveal defensive gaps. No Content-Security-Policy suggests XSS vulnerability potential. A disclosed Server header (Apache/2.4.49) enables version-specific vulnerability lookup. No HSTS means potential SSL stripping attacks.\n\n• Email pattern discovery — Knowing the email format (john.smith@company.com) enables: targeted phishing, password reset abuse, username enumeration, and social engineering. Public sources like LinkedIn profiles often reveal enough to deduce patterns.\n\n• Rate limiting and ethics — The session uses identifiable User-Agent, respects timeouts, and queries public APIs. Responsible OSINT never hammers servers, accesses private data, or violates terms of service. Most APIs have rate limits (crt.sh allows reasonable queries).\n\n• Structured output for integration — JSON reports enable feeding OSINT data into other tools: subdomain lists into port scanners, email lists into phishing simulations, technology stacks into vulnerability scanners. OSINT is the foundation that guides all subsequent testing.`,
    summary: "OSINT web scraping automates intelligence gathering from public sources including CT logs, HTTP headers, and web content. Python enables building comprehensive reconnaissance tools that identify subdomains, security gaps, and organizational details — all from publicly available information."
  },

  {
    title: "Building Security Tools",
    definition: "Building custom security tools in Python involves combining networking, cryptography, file analysis, and automation into purpose-built applications that address specific security needs. Professional security engineers create tools for vulnerability assessment, incident response, malware analysis, and continuous security monitoring that integrate with existing security infrastructure.",

    explanation: `Security tools generally fall into categories: scanners (finding vulnerabilities), analyzers (examining artifacts like malware samples or log files), monitors (continuous observation for anomalies), and responders (automated incident response actions). Python excels at all of these due to its library ecosystem and integration capabilities.\n\nTool architecture matters for maintainability and extensibility. Use modular design with separate components for input parsing, core logic, output formatting, and configuration. Implement proper error handling (network tools encounter unreachable hosts, analyzers encounter malformed files). Include logging at multiple verbosity levels. Support multiple output formats (JSON for automation, human-readable for operators, CSV for spreadsheets).\n\nIntegration with security infrastructure is crucial. Real tools communicate with: SIEM systems (Splunk, ELK) via syslog or APIs, ticketing systems (Jira, ServiceNow) for findings management, Slack/Teams for alerting, databases for persistent storage, and CI/CD pipelines for DevSecOps. The Python requests library and official SDKs enable these integrations.\n\nPackaging and distribution: use click or argparse for CLI interfaces, setuptools or Poetry for packaging, Docker for consistent deployment environments, and systemd or cron for scheduling. Type hints improve code quality for security-critical tools. Comprehensive testing prevents false positives (which erode trust) and false negatives (which miss real threats).`,

    code: `# Building a Security Monitoring Tool\nimport hashlib\nimport json\nimport os\nimport time\nfrom pathlib import Path\nfrom datetime import datetime\nfrom typing import Dict, List, Optional\nfrom dataclasses import dataclass, asdict\n\n@dataclass\nclass SecurityAlert:\n    """Structured security alert"""\n    timestamp: str\n    severity: str  # LOW, MEDIUM, HIGH, CRITICAL\n    category: str\n    description: str\n    evidence: dict\n    remediation: str\n\nclass FileIntegrityMonitor:\n    """\n    Host-based Intrusion Detection: monitors files for unauthorized changes.\n    Similar to OSSEC, Tripwire, or AIDE but simplified for education.\n    """\n    \n    def __init__(self, watch_paths: List[str], baseline_file: str = "baseline.json"):\n        self.watch_paths = [Path(p) for p in watch_paths]\n        self.baseline_file = Path(baseline_file)\n        self.alerts: List[SecurityAlert] = []\n    \n    def compute_file_hash(self, filepath: Path) -> Dict:\n        """Compute multiple hashes and metadata for a file"""\n        stat = filepath.stat()\n        with open(filepath, 'rb') as f:\n            content = f.read()\n        return {\n            'path': str(filepath),\n            'sha256': hashlib.sha256(content).hexdigest(),\n            'size': stat.st_size,\n            'modified': stat.st_mtime,\n            'permissions': oct(stat.st_mode)[-3:]\n        }\n    \n    def create_baseline(self) -> Dict:\n        """Scan all watched paths and create integrity baseline"""\n        baseline = {}\n        for watch_path in self.watch_paths:\n            if watch_path.is_file():\n                info = self.compute_file_hash(watch_path)\n                baseline[str(watch_path)] = info\n            elif watch_path.is_dir():\n                for filepath in watch_path.rglob('*'):\n                    if filepath.is_file():\n                        info = self.compute_file_hash(filepath)\n                        baseline[str(filepath)] = info\n        \n        # Save baseline to disk\n        with open(self.baseline_file, 'w') as f:\n            json.dump(baseline, f, indent=2)\n        \n        print(f"Baseline created: {len(baseline)} files indexed\")\n        return baseline\n    \n    def check_integrity(self) -> List[SecurityAlert]:\n        """Compare current state against baseline"""\n        if not self.baseline_file.exists():\n            raise FileNotFoundError("No baseline exists. Run create_baseline() first.\")\n        \n        with open(self.baseline_file) as f:\n            baseline = json.load(f)\n        \n        # Check for modifications and deletions\n        for filepath_str, original in baseline.items():\n            filepath = Path(filepath_str)\n            \n            if not filepath.exists():\n                self.alerts.append(SecurityAlert(\n                    timestamp=datetime.now().isoformat(),\n                    severity=\"HIGH\",\n                    category=\"file_deleted\",\n                    description=f\"Monitored file was deleted: {filepath_str}\",\n                    evidence={'original_hash': original['sha256']},\n                    remediation=\"Investigate deletion. Restore from backup if unauthorized.\"\n                ))\n                continue\n            \n            current = self.compute_file_hash(filepath)\n            \n            if current['sha256'] != original['sha256']:\n                self.alerts.append(SecurityAlert(\n                    timestamp=datetime.now().isoformat(),\n                    severity=\"CRITICAL\",\n                    category=\"file_modified\",\n                    description=f\"File content changed: {filepath_str}\",\n                    evidence={\n                        'original_hash': original['sha256'],\n                        'current_hash': current['sha256'],\n                        'size_change': current['size'] - original['size']\n                    },\n                    remediation=\"Verify change was authorized. Check git blame or audit logs.\"\n                ))\n            \n            if current['permissions'] != original['permissions']:\n                self.alerts.append(SecurityAlert(\n                    timestamp=datetime.now().isoformat(),\n                    severity=\"MEDIUM\",\n                    category=\"permissions_changed\",\n                    description=f\"File permissions modified: {filepath_str}\",\n                    evidence={\n                        'original_perms': original['permissions'],\n                        'current_perms': current['permissions']\n                    },\n                    remediation=\"Review permission change. Ensure principle of least privilege.\"\n                ))\n        \n        return self.alerts\n    \n    def export_alerts(self, format: str = \"json\") -> str:\n        """Export alerts in various formats"""\n        if format == \"json\":\n            return json.dumps([asdict(a) for a in self.alerts], indent=2)\n        elif format == \"summary\":\n            lines = [f\"=== Security Alerts: {len(self.alerts)} findings ===\"]\n            for alert in self.alerts:\n                lines.append(f\"  [{alert.severity}] {alert.description}\")\n            return '\\n'.join(lines)\n\n# Usage demonstration\nmonitor = FileIntegrityMonitor(["/etc/passwd", "/etc/shadow", "/usr/bin"])\n# monitor.create_baseline()  # Run once to establish known-good state\n# alerts = monitor.check_integrity()  # Run periodically to detect changes\n# print(monitor.export_alerts("summary"))`,

    breakdown: `• Dataclass for structured alerts — SecurityAlert provides a consistent schema for all findings, making them easy to serialize to JSON, send to SIEM systems, or display in dashboards. The severity/category/remediation fields enable automated triage.\n\n• Baseline comparison approach — Computing file hashes establishes a known-good state. Any deviation triggers an alert. This is how enterprise tools like OSSEC detect rootkits that modify system binaries or configuration files.\n\n• Multiple detection categories — The tool detects content changes (hash mismatch), deletions (file missing), and permission changes (privilege escalation indicators). Each has different severity and remediation steps.\n\n• Export flexibility — JSON output feeds into Splunk/ELK for correlation with other security events. Summary format is human-readable for operators. CSV could be added for compliance reporting. Good tools support multiple consumers.\n\n• Production considerations — Real deployment would add: scheduling (cron or systemd timer), alerting (email, Slack, PagerDuty), exclusion patterns (ignore expected changes like log rotation), database storage for historical tracking, and integration with change management systems to auto-close alerts for approved changes.`,
    summary: "Building security tools requires combining domain expertise with software engineering practices: modular architecture, structured output, error handling, and integration capabilities. File integrity monitoring exemplifies how simple concepts (hash comparison) become powerful security controls when properly engineered."
  }
];
