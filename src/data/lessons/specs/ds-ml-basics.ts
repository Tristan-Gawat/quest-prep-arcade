// Pre-written spec lessons for: Machine Learning Basics

export const dsMlBasicsLessons = [
  {
    title: "What is Machine Learning?",
    definition: "Machine Learning (ML) is a subset of artificial intelligence where systems learn patterns from data without being explicitly programmed for each specific task. Instead of writing rules by hand, ML algorithms build mathematical models from training data that can make predictions or decisions on new, unseen data.",

    explanation: `Traditional programming follows a clear paradigm: you write explicit rules (if-then logic) to transform inputs into outputs. Machine Learning inverts this — you provide input-output examples (training data), and the algorithm discovers the rules automatically. This is powerful when rules are too complex to write manually (face recognition, language understanding, medical diagnosis).\n\nThe ML workflow consists of key stages: (1) Data collection and preparation — gathering representative training data. (2) Feature engineering — selecting and transforming relevant input variables. (3) Model selection — choosing an appropriate algorithm. (4) Training — the algorithm adjusts its parameters to fit the training data. (5) Evaluation — testing the model on unseen data. (6) Deployment — using the model in production.\n\nA fundamental concept is the bias-variance tradeoff. Bias is error from wrong assumptions (underfitting — model is too simple). Variance is error from sensitivity to training data fluctuations (overfitting — model memorizes noise). Good models balance both: complex enough to capture real patterns, simple enough to generalize to new data.\n\nOverfitting is the most common ML pitfall. A model that memorizes training data (100% training accuracy) may fail on new data. Techniques to combat overfitting include: regularization (penalizing complexity), cross-validation (testing on multiple data splits), early stopping (stopping training before overfitting), and gathering more training data.\n\nML is categorized into three main types: supervised learning (labeled examples), unsupervised learning (finding patterns without labels), and reinforcement learning (learning through trial-and-error rewards). Each addresses fundamentally different problem types.`,

    code: `// Simple demonstration: ML vs traditional programming\n\n// Traditional approach: hand-written spam rules\nfunction isSpamTraditional(email) {\n    if (email.includes("free money")) return true;\n    if (email.includes("click here now")) return true;\n    if (email.includes("limited offer")) return true;\n    // Need to manually add EVERY spam pattern...\n    return false;\n}\n\n// ML approach: learn patterns from labeled data\n// Step 1: Prepare training data\nconst trainingData = [\n    { text: "Free money click now!", label: "spam" },\n    { text: "Meeting at 3pm tomorrow", label: "not_spam" },\n    { text: "You won a prize!!!", label: "spam" },\n    { text: "Project deadline Friday", label: "not_spam" },\n    // ... thousands more examples\n];\n\n// Step 2: Extract features (simplified bag-of-words)\nfunction extractFeatures(text) {\n    const words = text.toLowerCase().split(/\\s+/);\n    return {\n        hasExclamation: text.includes("!"),\n        wordCount: words.length,\n        hasFree: words.includes("free"),\n        hasClick: words.includes("click"),\n        capsRatio: (text.match(/[A-Z]/g) || []).length / text.length\n    };\n}\n\n// Step 3: Train model (conceptual - learning weights)\nclass SimpleClassifier {\n    constructor() {\n        this.weights = {}; // Learned from data\n    }\n\n    train(data) {\n        // Algorithm adjusts weights to minimize errors\n        // on training data (gradient descent, etc.)\n        console.log("Training on", data.length, "examples...");\n    }\n\n    predict(features) {\n        // Use learned weights to classify new data\n        let score = 0;\n        for (const [feature, value] of Object.entries(features)) {\n            score += (this.weights[feature] || 0) * value;\n        }\n        return score > 0.5 ? "spam" : "not_spam";\n    }\n}\n\n// The model generalizes to NEW spam patterns it wasn't\n// explicitly programmed for — that's the power of ML`,

    breakdown: `• Learning from data — Instead of explicit rules, ML algorithms discover patterns in training data. The model adjusts internal parameters (weights) to minimize prediction errors. This enables handling complex patterns that are impossible to hand-code.\n\n• Training vs inference — Training is the expensive process of learning from data (can take hours/days). Inference is using the trained model to make predictions on new data (typically fast). Models are trained once and deployed for many inferences.\n\n• Bias-variance tradeoff — Simple models (high bias) underfit and miss patterns. Complex models (high variance) overfit and memorize noise. The goal is finding the sweet spot where the model captures real patterns without memorizing training peculiarities.\n\n• Generalization — The true test of an ML model is performance on unseen data. A model that achieves 99% on training data but 60% on test data has overfit. Always evaluate on held-out test sets that the model never saw during training.\n\n• Features matter — The input representation (features) dramatically affects model performance. Good features make patterns obvious to the algorithm. Feature engineering — choosing, transforming, and combining input variables — often matters more than algorithm choice.`,
    summary: "Machine Learning enables systems to learn patterns from data instead of following hard-coded rules. The key concepts are: training on labeled data, balancing underfitting vs overfitting (bias-variance), evaluating on unseen data, and feature engineering. ML excels when patterns are too complex for explicit programming."
  },

  {
    title: "Supervised vs Unsupervised Learning",
    definition: "Supervised learning trains models on labeled data (input-output pairs) to predict labels for new inputs. Unsupervised learning finds hidden patterns in unlabeled data without predefined outputs. These represent the two fundamental paradigms of machine learning, each suited to different problem types.",
    explanation: `Supervised learning is like learning with a teacher who provides correct answers. You give the algorithm examples with known labels: images labeled "cat" or "dog", emails labeled "spam" or "not spam", houses labeled with their sale prices. The algorithm learns the mapping from inputs to labels, then predicts labels for new, unlabeled inputs.\n\nSupervised learning splits into classification (predicting discrete categories: spam/not-spam, cat/dog/bird) and regression (predicting continuous values: house price, temperature, stock price). Common algorithms include linear regression, logistic regression, decision trees, random forests, support vector machines, and neural networks.\n\nUnsupervised learning is like learning without a teacher — discovering structure in unlabeled data. The algorithm finds natural groupings (clustering), reduces dimensionality (PCA), or detects anomalies without being told what to look for. You give it customer purchase histories and it discovers customer segments on its own.\n\nKey unsupervised techniques include: K-Means clustering (group similar data points), hierarchical clustering (build a tree of clusters), PCA (find the most important dimensions), t-SNE (visualize high-dimensional data in 2D/3D), and autoencoders (learn compressed representations).\n\nSemi-supervised learning combines both: a small amount of labeled data with a large amount of unlabeled data. This is practical because labeling is expensive (requires human experts) while unlabeled data is abundant. Self-supervised learning (used in modern NLP like BERT, GPT) creates its own labels from unlabeled data through techniques like masked word prediction.`,

    code: `// Supervised Learning: Simple K-Nearest Neighbors (KNN)\nfunction knnClassify(trainingData, newPoint, k = 3) {\n    // Calculate distance from newPoint to all training points\n    const distances = trainingData.map(point => ({\n        label: point.label,\n        distance: euclideanDistance(newPoint.features, point.features)\n    }));\n\n    // Sort by distance and take k nearest\n    distances.sort((a, b) => a.distance - b.distance);\n    const kNearest = distances.slice(0, k);\n\n    // Majority vote among k neighbors\n    const votes = {};\n    kNearest.forEach(neighbor => {\n        votes[neighbor.label] = (votes[neighbor.label] || 0) + 1;\n    });\n\n    return Object.entries(votes)\n        .sort((a, b) => b[1] - a[1])[0][0];\n}\n\nfunction euclideanDistance(a, b) {\n    return Math.sqrt(\n        a.reduce((sum, val, i) => sum + (val - b[i]) ** 2, 0)\n    );\n}\n\n// Unsupervised Learning: K-Means Clustering\nfunction kMeans(data, k, maxIter = 100) {\n    // Initialize k random centroids\n    let centroids = data.slice(0, k).map(p => [...p]);\n\n    for (let iter = 0; iter < maxIter; iter++) {\n        // Assign each point to nearest centroid\n        const clusters = Array.from({length: k}, () => []);\n        for (const point of data) {\n            let minDist = Infinity, closest = 0;\n            for (let i = 0; i < k; i++) {\n                const dist = euclideanDistance(point, centroids[i]);\n                if (dist < minDist) {\n                    minDist = dist;\n                    closest = i;\n                }\n            }\n            clusters[closest].push(point);\n        }\n\n        // Update centroids to cluster means\n        const newCentroids = clusters.map(cluster => {\n            if (cluster.length === 0) return centroids[0]; // handle empty\n            return cluster[0].map((_, dim) =>\n                cluster.reduce((sum, p) => sum + p[dim], 0) / cluster.length\n            );\n        });\n\n        // Check convergence\n        const moved = centroids.some((c, i) =>\n            euclideanDistance(c, newCentroids[i]) > 0.001);\n        centroids = newCentroids;\n        if (!moved) break;\n    }\n    return centroids;\n}`,

    breakdown: `• Supervised learning types — Classification predicts discrete categories (binary: yes/no; multi-class: cat/dog/bird). Regression predicts continuous values (price, temperature). The choice depends on whether your target variable is categorical or numerical.\n\n• Unsupervised learning types — Clustering groups similar items (customer segmentation). Dimensionality reduction compresses features (PCA). Anomaly detection finds outliers (fraud detection). Association rule learning finds co-occurring items (market basket analysis).\n\n• Evaluation differs — Supervised: measure accuracy, precision, recall, F1, MSE against known labels. Unsupervised: use internal metrics like silhouette score, or domain expert evaluation. Supervised evaluation is straightforward; unsupervised is inherently harder since there's no "correct answer."\n\n• Data requirements — Supervised needs labeled data (expensive to create — requires human annotation). Unsupervised works with unlabeled data (abundant and cheap). In practice, labeling is often the bottleneck, motivating semi-supervised and self-supervised approaches.\n\n• Choosing the paradigm — Use supervised when you have labeled examples and a clear prediction target. Use unsupervised for exploration, when labels don't exist, or for discovering unknown patterns. Many real systems combine both (cluster first, then classify within clusters).`,
    summary: "Supervised learning predicts labels from labeled training data (classification or regression). Unsupervised learning discovers hidden structure in unlabeled data (clustering, dimensionality reduction). The choice depends on data availability: supervised needs expensive labels but gives clear predictions; unsupervised works with raw data but requires interpretation of discovered patterns."
  },

  {
    title: "Linear Regression from Scratch",
    definition: "Linear regression is the simplest supervised learning algorithm for predicting continuous values. It models the relationship between input features (x) and output (y) as a linear equation: y = mx + b (or y = w1*x1 + w2*x2 + ... + bias for multiple features). The model learns optimal weights by minimizing the sum of squared differences between predictions and actual values.",
    explanation: `Linear regression finds the best-fit line through data points. For a single feature: y = wx + b, where w is the weight (slope) and b is the bias (y-intercept). For multiple features: y = w1*x1 + w2*x2 + ... + wn*xn + b. The "learning" process finds the weights that minimize prediction error.\n\nThe error metric used is Mean Squared Error (MSE): the average of (predicted - actual)² across all training examples. Squaring ensures all errors are positive and penalizes large errors more heavily. The goal: find weights w and bias b that minimize MSE on training data.\n\nGradient Descent is the optimization algorithm that finds optimal weights iteratively. Starting with random weights, it computes the gradient (direction of steepest increase in error), then moves weights in the opposite direction (decreasing error). The learning rate controls step size: too large causes oscillation, too small causes slow convergence.\n\nThe gradient descent update rule is: w = w - learning_rate * (dMSE/dw). For linear regression, this derivative has a closed-form expression, making the math clean. The process repeats for many iterations (epochs) until convergence — when the error stops decreasing significantly.\n\nLinear regression assumes a linear relationship between features and target. When the real relationship is non-linear, linear regression underfits. Solutions include polynomial regression (adding x², x³ features), or switching to non-linear models. Regularization (L1/L2) prevents overfitting when you have many features by penalizing large weights.`,

    code: `// Linear Regression from scratch using Gradient Descent\nclass LinearRegression {\n    constructor(learningRate = 0.01, iterations = 1000) {\n        this.lr = learningRate;\n        this.iterations = iterations;\n        this.weight = 0;\n        this.bias = 0;\n    }\n\n    // Train the model using gradient descent\n    train(X, y) {\n        const n = X.length;\n\n        for (let iter = 0; iter < this.iterations; iter++) {\n            // Forward pass: make predictions\n            const predictions = X.map(x => this.weight * x + this.bias);\n\n            // Compute gradients (partial derivatives of MSE)\n            let dWeight = 0, dBias = 0;\n            for (let i = 0; i < n; i++) {\n                const error = predictions[i] - y[i];\n                dWeight += error * X[i]; // dMSE/dw\n                dBias += error;          // dMSE/db\n            }\n            dWeight = (2 / n) * dWeight;\n            dBias = (2 / n) * dBias;\n\n            // Update weights (move opposite to gradient)\n            this.weight -= this.lr * dWeight;\n            this.bias -= this.lr * dBias;\n        }\n    }\n\n    predict(x) {\n        return this.weight * x + this.bias;\n    }\n\n    // Mean Squared Error\n    mse(X, y) {\n        const predictions = X.map(x => this.predict(x));\n        const sumSquaredError = predictions.reduce(\n            (sum, pred, i) => sum + (pred - y[i]) ** 2, 0\n        );\n        return sumSquaredError / X.length;\n    }\n}\n\n// Example: predict house price from square footage\nconst squareFeet = [750, 1000, 1250, 1500, 1750, 2000];\nconst prices =     [150, 200,  250,  300,  350,  400]; // thousands\n\nconst model = new LinearRegression(0.0000001, 5000);\nmodel.train(squareFeet, prices);\n\nconsole.log(model.predict(1600)); // ~320 (thousand)\nconsole.log("Weight:", model.weight); // ~0.2 (price per sqft)\nconsole.log("Bias:", model.bias); // ~0 (base price)\nconsole.log("MSE:", model.mse(squareFeet, prices));`,

    breakdown: `• Model equation — y = w*x + b (single feature) or y = W·X + b (multiple features as matrix multiplication). The weights define how much each feature contributes to the prediction. Learning means finding the optimal weights.\n\n• Mean Squared Error — MSE = (1/n) * sum((predicted - actual)²). It measures how far off predictions are from true values. Squaring penalizes large errors disproportionately and ensures the gradient points toward the minimum (convex function for linear regression).\n\n• Gradient Descent — Iteratively updates weights: w = w - lr * gradient. The gradient tells which direction increases error, so we move opposite. Learning rate is crucial: too high causes divergence, too low causes slow training. For linear regression, the loss surface is convex (one global minimum).\n\n• Feature scaling — Features with different scales (e.g., age 0-100 vs salary 0-100000) cause gradient descent to oscillate. Normalize features to similar ranges (z-score: (x-mean)/std) for faster convergence. This is essential in practice.\n\n• Regularization — L2 regularization (Ridge) adds penalty lambda*sum(w²) to the loss, pushing weights toward zero and preventing overfitting. L1 regularization (Lasso) adds lambda*sum(|w|), which can produce sparse models (some weights become exactly zero, acting as feature selection).`,
    summary: "Linear regression predicts continuous values by fitting y = wx + b to data. Gradient descent iteratively adjusts weights to minimize Mean Squared Error. The algorithm demonstrates core ML concepts: forward pass (prediction), loss computation, gradient calculation, and weight updates. Feature scaling and regularization are essential practical considerations."
  },

  {
    title: "Decision Trees & Random Forests",
    definition: "A decision tree is a supervised learning model that makes predictions by learning a hierarchy of if-then rules from data, splitting features at thresholds that best separate the target classes. A random forest is an ensemble of many decision trees, each trained on random subsets of data and features, whose combined predictions are more accurate and robust than any individual tree.",
    explanation: `Decision trees mirror human decision-making: at each node, ask a yes/no question about a feature (e.g., "Is age > 30?"), and branch accordingly until reaching a leaf node with a prediction. The tree is built top-down by choosing the best feature and split point at each node.\n\nThe key question is: which feature should we split on? We want splits that create the purest child nodes (most homogeneous in their labels). Two common metrics measure purity: Gini impurity (probability of misclassifying a randomly chosen sample) and Information Gain (reduction in entropy after splitting). At each node, we try all features and all possible thresholds, choosing the split that maximizes purity improvement.\n\nDecision trees have major advantages: they're interpretable (you can visualize the decision path), handle both numerical and categorical features, require no feature scaling, and can capture non-linear relationships. However, they tend to overfit — a deep tree can memorize training data by creating rules specific to individual examples.\n\nRandom forests solve overfitting through ensemble learning. The idea: many weak learners combined create a strong learner. Each tree in the forest is trained on a random bootstrap sample (sampling with replacement) of the data, and at each split, only a random subset of features is considered. This decorrelation between trees reduces variance.\n\nFor classification, the random forest takes a majority vote across all trees. For regression, it averages predictions. The random feature selection is crucial: if one feature is very strong, without randomization all trees would split on it first, making them correlated. Random feature subsets create diverse trees that make different errors, and averaging cancels out individual mistakes.`,

    code: `// Decision Tree Node\nclass TreeNode {\n    constructor() {\n        this.feature = null;    // Which feature to split on\n        this.threshold = null;  // Split threshold\n        this.left = null;       // Left child (feature <= threshold)\n        this.right = null;      // Right child (feature > threshold)\n        this.prediction = null; // Leaf prediction (if leaf node)\n    }\n}\n\n// Calculate Gini Impurity\nfunction giniImpurity(labels) {\n    const total = labels.length;\n    if (total === 0) return 0;\n    const counts = {};\n    labels.forEach(l => counts[l] = (counts[l] || 0) + 1);\n    let gini = 1;\n    for (const count of Object.values(counts)) {\n        const p = count / total;\n        gini -= p * p;\n    }\n    return gini; // 0 = pure, 0.5 = max impurity (binary)\n}\n\n// Find best split for a dataset\nfunction findBestSplit(data, labels) {\n    let bestGain = 0, bestFeature = 0, bestThreshold = 0;\n    const parentGini = giniImpurity(labels);\n    const numFeatures = data[0].length;\n\n    for (let feat = 0; feat < numFeatures; feat++) {\n        const values = [...new Set(data.map(row => row[feat]))].sort((a,b) => a-b);\n        for (let i = 0; i < values.length - 1; i++) {\n            const threshold = (values[i] + values[i+1]) / 2;\n            const leftIdx = [], rightIdx = [];\n            data.forEach((row, idx) => {\n                if (row[feat] <= threshold) leftIdx.push(idx);\n                else rightIdx.push(idx);\n            });\n\n            const leftLabels = leftIdx.map(i => labels[i]);\n            const rightLabels = rightIdx.map(i => labels[i]);\n\n            // Weighted Gini after split\n            const n = labels.length;\n            const weightedGini = (leftLabels.length/n) * giniImpurity(leftLabels)\n                + (rightLabels.length/n) * giniImpurity(rightLabels);\n            const gain = parentGini - weightedGini;\n\n            if (gain > bestGain) {\n                bestGain = gain;\n                bestFeature = feat;\n                bestThreshold = threshold;\n            }\n        }\n    }\n    return { feature: bestFeature, threshold: bestThreshold, gain: bestGain };\n}\n\n// Random Forest: train multiple trees on random subsets\n// Each tree sees random 70% of data + random sqrt(features)\n// Final prediction = majority vote across all trees`,

    breakdown: `• Splitting criteria — Gini impurity measures the probability of misclassification (0 = pure node). Information gain uses entropy (reduction in uncertainty). Both produce similar trees in practice. Choose the split that creates the purest child nodes.\n\n• Overfitting prevention — Limit tree depth (max_depth parameter), require minimum samples per leaf, or prune branches that don't improve validation accuracy. Without limits, a tree can create one leaf per training example (100% training accuracy, terrible generalization).\n\n• Random Forest ensemble — Train 100-1000 trees, each on a bootstrap sample (random 63% of data on average) with random feature subsets (sqrt(n_features) for classification). Majority vote combines predictions. This "bagging" technique reduces variance without increasing bias.\n\n• Feature importance — Random forests naturally provide feature importance: measure how much each feature reduces impurity across all trees. This helps identify which features matter most and is useful for feature selection and data understanding.\n\n• Practical advantages — Random forests work well "out of the box" with minimal tuning (unlike neural networks). They handle missing values, mixed feature types, and non-linear relationships. They're often the first algorithm to try on tabular/structured data before considering more complex models.`,
    summary: "Decision trees learn hierarchical if-then rules by splitting on the most informative features. They're interpretable but prone to overfitting. Random forests combine hundreds of decorrelated trees (trained on random data/feature subsets) through majority voting, achieving high accuracy and robustness. Random forests are among the best algorithms for tabular data."
  },

  {
    title: "Neural Networks Basics",
    definition: "A neural network is a computational model inspired by biological neurons, consisting of layers of interconnected nodes (neurons) that transform inputs through weighted connections and non-linear activation functions. By stacking layers, neural networks can learn arbitrarily complex non-linear mappings from inputs to outputs, powering modern AI applications like image recognition, language models, and game playing.",
    explanation: `A neural network consists of layers: an input layer (receives features), one or more hidden layers (learn intermediate representations), and an output layer (produces predictions). Each neuron computes a weighted sum of its inputs, adds a bias, then applies a non-linear activation function.\n\nThe fundamental computation at each neuron is: output = activation(w1*x1 + w2*x2 + ... + wn*xn + bias). Without activation functions, stacking layers would just compute linear transformations (a deep linear network equals a single linear layer). Non-linear activations (ReLU, sigmoid, tanh) are what give neural networks their power to model complex patterns.\n\nTraining uses backpropagation with gradient descent. Forward pass: compute predictions by passing inputs through all layers. Compute loss (error between prediction and true label). Backward pass: compute gradients of the loss with respect to every weight using the chain rule of calculus. Update all weights: w = w - learning_rate * gradient.\n\nCommon activation functions: ReLU (max(0, x)) is the default for hidden layers — simple, fast, and avoids the vanishing gradient problem. Sigmoid (1/(1+e^-x)) squashes output to [0,1], used for binary classification output. Softmax normalizes outputs to probabilities that sum to 1, used for multi-class classification output.\n\nDeep learning (networks with many hidden layers) has revolutionized AI because deeper networks can learn hierarchical representations: early layers detect simple features (edges in images, character patterns in text), while deeper layers compose these into complex concepts (faces, sentences). This hierarchical feature learning eliminates manual feature engineering.`,

    code: `// Simple Neural Network from scratch (1 hidden layer)\nclass NeuralNetwork {\n    constructor(inputSize, hiddenSize, outputSize) {\n        // Initialize weights randomly (small values)\n        this.weightsIH = this.randomMatrix(inputSize, hiddenSize);\n        this.biasH = new Array(hiddenSize).fill(0);\n        this.weightsHO = this.randomMatrix(hiddenSize, outputSize);\n        this.biasO = new Array(outputSize).fill(0);\n        this.lr = 0.1;\n    }\n\n    randomMatrix(rows, cols) {\n        return Array.from({length: rows}, () =>\n            Array.from({length: cols}, () => (Math.random() - 0.5) * 0.5)\n        );\n    }\n\n    // Activation: ReLU for hidden, Sigmoid for output\n    relu(x) { return Math.max(0, x); }\n    sigmoid(x) { return 1 / (1 + Math.exp(-x)); }\n    sigmoidDerivative(x) { return x * (1 - x); }\n\n    // Forward pass\n    forward(input) {\n        // Hidden layer: input -> weighted sum -> ReLU\n        this.hidden = this.biasH.map((b, j) => {\n            const sum = input.reduce((s, x, i) =>\n                s + x * this.weightsIH[i][j], b);\n            return this.relu(sum);\n        });\n\n        // Output layer: hidden -> weighted sum -> Sigmoid\n        this.output = this.biasO.map((b, k) => {\n            const sum = this.hidden.reduce((s, h, j) =>\n                s + h * this.weightsHO[j][k], b);\n            return this.sigmoid(sum);\n        });\n\n        return this.output;\n    }\n\n    // Training: forward pass + backpropagation\n    train(input, target) {\n        const output = this.forward(input);\n\n        // Output error and gradient\n        const outputErrors = target.map((t, i) => t - output[i]);\n        const outputGradients = output.map((o, i) =>\n            outputErrors[i] * this.sigmoidDerivative(o));\n\n        // Update output weights\n        for (let j = 0; j < this.hidden.length; j++) {\n            for (let k = 0; k < this.output.length; k++) {\n                this.weightsHO[j][k] += this.lr * outputGradients[k] * this.hidden[j];\n            }\n        }\n\n        return outputErrors.reduce((s, e) => s + e*e, 0); // MSE\n    }\n}\n\n// XOR problem (not linearly separable - needs hidden layer)\nconst nn = new NeuralNetwork(2, 4, 1);\nconst xorData = [[0,0],[0,1],[1,0],[1,1]];\nconst xorLabels = [[0],[1],[1],[0]];\n\n// Train for 10000 epochs\nfor (let epoch = 0; epoch < 10000; epoch++) {\n    for (let i = 0; i < 4; i++) {\n        nn.train(xorData[i], xorLabels[i]);\n    }\n}`,

    breakdown: `• Neuron computation — Each neuron computes: output = activation(sum(weights * inputs) + bias). The weights determine how much each input matters. The activation function introduces non-linearity, enabling the network to learn complex patterns.\n\n• Activation functions — ReLU (max(0,x)): default for hidden layers, fast and avoids vanishing gradients. Sigmoid (1/(1+e^-x)): outputs probability [0,1], for binary classification. Softmax: outputs probability distribution summing to 1, for multi-class. Without activations, any network collapses to linear regression.\n\n• Backpropagation — The chain rule of calculus propagates error gradients from output back through all layers. Each weight gets a gradient indicating how much it contributed to the error. This is the key algorithm that makes training deep networks possible.\n\n• Universal approximation — A neural network with even one hidden layer (sufficiently wide) can approximate any continuous function. Deeper networks are more parameter-efficient: they can represent the same functions with exponentially fewer neurons than shallow networks.\n\n• Practical considerations — Weight initialization matters (too large/small causes gradient issues). Batch normalization stabilizes training. Dropout randomly disables neurons during training to prevent overfitting. Modern frameworks (PyTorch, TensorFlow) handle backprop automatically with autograd.`,
    summary: "Neural networks stack layers of neurons with weighted connections and non-linear activations to learn complex patterns. Training uses backpropagation (chain rule) to update all weights via gradient descent. Deep networks learn hierarchical feature representations automatically, powering breakthroughs in vision, language, and game playing AI."
  }
];
