// Pre-written spec lessons for: Networking Fundamentals

export const csNetworkingBasicsLessons = [
  {
    title: "What is TCP/IP?",
    definition: "TCP/IP (Transmission Control Protocol/Internet Protocol) is the foundational communication protocol suite that powers the internet. TCP ensures reliable, ordered delivery of data between applications, while IP handles addressing and routing packets across networks.",
    explanation: `TCP/IP is a layered protocol stack with four layers: Application (HTTP, FTP, DNS), Transport (TCP, UDP), Internet (IP, ICMP), and Network Access (Ethernet, WiFi). Each layer has a specific responsibility.\n\nTCP provides connection-oriented communication. Before data is sent, a three-way handshake establishes the connection: SYN, SYN-ACK, ACK. This guarantees both sides are ready. Data is broken into segments, numbered for ordering, and acknowledged. If a segment is lost, TCP retransmits it.\n\nIP handles logical addressing (IPv4: 32-bit like 192.168.1.1, IPv6: 128-bit). Routers use IP addresses to forward packets hop-by-hop toward the destination. Each packet may take a different path — TCP reassembles them in order at the destination.\n\nUDP (User Datagram Protocol) is TCP's lightweight alternative — no handshake, no guaranteed delivery, no ordering. It's faster and used for real-time applications like gaming, video streaming, and DNS lookups where speed matters more than reliability.`,
    code: `# TCP Three-Way Handshake Simulation\nimport socket\n\n# SERVER: Listening for connections\nserver = socket.socket(socket.AF_INET, socket.SOCK_STREAM)  # TCP socket\nserver.bind(('0.0.0.0', 8080))\nserver.listen(5)  # Backlog of 5 connections\nprint("[*] Server listening on port 8080")\n\nconn, addr = server.accept()  # Blocks until client connects\nprint(f"[+] Connection from {addr[0]}:{addr[1]}")\n\n# CLIENT: Initiating TCP handshake\nclient = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\nclient.connect(('127.0.0.1', 8080))  # SYN -> SYN-ACK -> ACK\nprint("[+] Connected to server")\n\n# Sending data over TCP (reliable, ordered)\nclient.send(b"Hello, server!")  # Data is segmented internally\ndata = conn.recv(1024)  # Server receives\nprint(f"[*] Received: {data.decode()}")\n\n# UDP Example (connectionless, fast)\nudp_sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)  # UDP\nudp_sock.sendto(b"Fast message", ('127.0.0.1', 9090))  # No handshake!`,
    breakdown: `\u2022 socket.SOCK_STREAM = TCP \u2014 Stream-based, connection-oriented. The OS handles segmentation, ordering, and retransmission automatically.\n\n\u2022 socket.SOCK_DGRAM = UDP \u2014 Datagram-based, connectionless. Each sendto() is independent. No guarantee the other side receives it.\n\n\u2022 server.listen(5) \u2014 Tells the OS to queue up to 5 pending connections. After 5, new attempts get refused until accept() is called.\n\n\u2022 Three-way handshake happens inside connect() \u2014 Client sends SYN, server responds SYN-ACK, client confirms ACK. Only then does connect() return.\n\n\u2022 conn.recv(1024) \u2014 Reads up to 1024 bytes from the TCP stream. TCP may deliver data in chunks different from how it was sent (stream-based, not message-based).`,
    summary: "TCP/IP is a 4-layer protocol stack: Application, Transport, Internet, Network Access. TCP provides reliable ordered delivery via three-way handshake and retransmission. UDP provides fast connectionless communication. IP handles addressing and routing between networks."
  },
  {
    title: "Understanding Ports & Services",
    definition: "Ports are 16-bit numbers (0-65535) that identify specific processes/services on a machine. When combined with an IP address, a port creates a unique endpoint (socket) for network communication. Well-known ports (0-1023) map to standard services like HTTP (80), HTTPS (443), SSH (22).",
    explanation: `Think of IP addresses as street addresses and ports as apartment numbers. The IP gets the packet to the right machine; the port delivers it to the right application. A web server on port 80 and an SSH server on port 22 can coexist on the same machine.\n\nPort ranges: Well-known (0-1023) require root/admin privileges and are assigned by IANA. Registered (1024-49151) are used by applications. Dynamic/ephemeral (49152-65535) are assigned temporarily by the OS for client connections.\n\nPort scanning is a fundamental reconnaissance technique. By sending packets to various ports and analyzing responses, you can determine which services are running, their versions, and potential vulnerabilities. Tools like Nmap automate this process.\n\nCommon ports to know: 21 (FTP), 22 (SSH), 23 (Telnet), 25 (SMTP), 53 (DNS), 80 (HTTP), 110 (POP3), 143 (IMAP), 443 (HTTPS), 445 (SMB), 3306 (MySQL), 3389 (RDP), 5432 (PostgreSQL), 6379 (Redis), 8080 (HTTP alt).`,
    code: `# Port Scanner - Discover open services on a target\nimport socket\nimport concurrent.futures\n\ndef scan_port(host, port, timeout=1):\n    """Attempt to connect to a specific port"""\n    try:\n        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n        sock.settimeout(timeout)\n        result = sock.connect_ex((host, port))  # Returns 0 if open\n        sock.close()\n        return port, result == 0\n    except:\n        return port, False\n\ndef scan_host(host, ports=range(1, 1025)):\n    """Scan common ports on a host"""\n    print(f"[*] Scanning {host}...")\n    open_ports = []\n    \n    # Use thread pool for speed (scan 100 ports simultaneously)\n    with concurrent.futures.ThreadPoolExecutor(max_workers=100) as executor:\n        futures = {executor.submit(scan_port, host, p): p for p in ports}\n        for future in concurrent.futures.as_completed(futures):\n            port, is_open = future.result()\n            if is_open:\n                service = socket.getservbyport(port, 'tcp') if port < 1024 else 'unknown'\n                print(f"  [+] Port {port}/tcp OPEN - {service}")\n                open_ports.append(port)\n    \n    return open_ports\n\n# Example usage (only scan systems you own/have permission!)\nresults = scan_host("127.0.0.1")\nprint(f"\\n[*] Found {len(results)} open ports")`,
    breakdown: `\u2022 connect_ex() returns 0 on success \u2014 Unlike connect() which raises an exception on failure, connect_ex() returns an error code. 0 means the port accepted the TCP connection (it's open).\n\n\u2022 ThreadPoolExecutor(max_workers=100) \u2014 Scans 100 ports simultaneously. Without threading, scanning 1024 ports with 1s timeout would take ~17 minutes. With threads, it takes seconds.\n\n\u2022 sock.settimeout(1) \u2014 If the port doesn't respond within 1 second, it's likely filtered (firewall dropping packets) or the host is down.\n\n\u2022 getservbyport() \u2014 Looks up the standard service name for a port number from the system's services database (/etc/services on Linux).\n\n\u2022 Port states: OPEN (service accepting connections), CLOSED (port reachable but no service), FILTERED (firewall blocking - no response).`,
    summary: "Ports identify specific services on a machine (0-65535). Well-known ports (0-1023) map to standard services. Port scanning reveals which services are running and potentially vulnerable. Always get authorization before scanning any system you don't own."
  },
  {
    title: "HTTP vs HTTPS Deep Dive",
    definition: "HTTP (Hypertext Transfer Protocol) transmits data in plaintext, making it vulnerable to eavesdropping and tampering. HTTPS adds TLS (Transport Layer Security) encryption, providing confidentiality, integrity, and authentication through certificates and cryptographic handshakes.",
    explanation: `HTTP sends everything as readable text. Anyone on the same network (WiFi, ISP, router) can intercept and read passwords, cookies, form data, and page content. This is called a Man-in-the-Middle (MITM) attack.\n\nHTTPS wraps HTTP inside a TLS tunnel. The TLS handshake: (1) Client sends supported cipher suites, (2) Server responds with its certificate and chosen cipher, (3) Client verifies certificate against trusted CAs, (4) Both derive session keys using asymmetric crypto, (5) All subsequent data encrypted with symmetric keys (AES).\n\nCertificates are issued by Certificate Authorities (CAs) like Let's Encrypt, DigiCert. They bind a public key to a domain name. The browser checks: Is the certificate expired? Is the CA trusted? Does the domain match? Was it revoked? If any check fails, the browser shows a warning.\n\nHTTPS prevents: eavesdropping (encryption), tampering (integrity via MAC), impersonation (certificate verification). It does NOT prevent: server-side vulnerabilities, XSS, SQL injection, or attacks on the application logic itself.`,
    code: `# Demonstrating HTTP vs HTTPS differences\nimport requests\nimport ssl\nimport socket\n\n# === HTTP Request (PLAINTEXT - anyone can read this!) ===\n# What an attacker sees on the network:\n# GET /login HTTP/1.1\n# Host: example.com\n# Cookie: session=abc123\n# (entire request visible!)\n\nresponse = requests.get('http://example.com')\nprint(f"HTTP Status: {response.status_code}")\nprint(f"Headers visible: {dict(response.headers)}")\n\n# === HTTPS Request (ENCRYPTED) ===\n# What an attacker sees: encrypted gibberish\n# Only the destination IP and port 443 are visible\n\nresponse = requests.get('https://example.com', verify=True)\nprint(f"HTTPS Status: {response.status_code}")\n\n# === Inspecting a site's TLS certificate ===\ndef check_certificate(hostname):\n    context = ssl.create_default_context()\n    with socket.create_connection((hostname, 443)) as sock:\n        with context.wrap_socket(sock, server_hostname=hostname) as ssock:\n            cert = ssock.getpeercert()\n            print(f"\\n[*] Certificate for {hostname}:")\n            print(f"    Issuer: {dict(x[0] for x in cert['issuer'])}")\n            print(f"    Subject: {dict(x[0] for x in cert['subject'])}")\n            print(f"    Valid from: {cert['notBefore']}")\n            print(f"    Valid until: {cert['notAfter']}")\n            print(f"    Serial: {cert['serialNumber']}")\n\ncheck_certificate('github.com')`,
    breakdown: `\u2022 verify=True \u2014 Tells requests to validate the server's TLS certificate against trusted CAs. If False, you're vulnerable to MITM (the connection is encrypted but you might be talking to an attacker).\n\n\u2022 ssl.create_default_context() \u2014 Loads the system's trusted CA certificates. These are the authorities your OS trusts to vouch for website identities.\n\n\u2022 wrap_socket(server_hostname=...) \u2014 Performs the TLS handshake and enables SNI (Server Name Indication), which tells the server which certificate to present (important for shared hosting).\n\n\u2022 getpeercert() \u2014 Returns the server's X.509 certificate details after the handshake completes. This is the same info your browser shows when you click the padlock icon.\n\n\u2022 HTTP on port 80 vs HTTPS on port 443 \u2014 Same protocol (HTTP), but HTTPS wraps it in TLS encryption before sending over the network.`,
    summary: "HTTP transmits in plaintext (vulnerable to eavesdropping). HTTPS adds TLS encryption via certificate-based handshake. TLS provides confidentiality, integrity, and authentication. Always verify certificates to prevent MITM attacks. HTTPS protects data in transit, not application-level vulnerabilities."
  },
  {
    title: "How DNS Resolution Works",
    definition: "DNS (Domain Name System) translates human-readable domain names (like google.com) into IP addresses (like 142.250.80.46). It's a hierarchical, distributed database with caching at every level. DNS is often called the 'phonebook of the internet.'",
    explanation: `When you type google.com in your browser, a multi-step resolution process begins:\n\n1. Browser cache \u2014 checks if it recently resolved this domain\n2. OS cache \u2014 checks the local DNS resolver cache\n3. Recursive resolver (usually your ISP) \u2014 if not cached, begins the lookup\n4. Root nameserver \u2014 knows where to find .com, .org, .net TLD servers\n5. TLD nameserver (.com) \u2014 knows which nameserver is authoritative for google.com\n6. Authoritative nameserver \u2014 returns the actual IP address for google.com\n\nDNS record types: A (IPv4 address), AAAA (IPv6), CNAME (alias), MX (mail server), TXT (arbitrary text, used for SPF/DKIM), NS (nameserver), SOA (zone authority).\n\nDNS security concerns: DNS spoofing (returning fake IPs), DNS hijacking (redirecting queries), DNS tunneling (hiding data in DNS queries to bypass firewalls). DNSSEC adds cryptographic signatures to prevent tampering.`,
    code: `# DNS Resolution and Reconnaissance\nimport socket\nimport subprocess\n\n# === Basic DNS lookup ===\ndef resolve_domain(domain):\n    """Resolve domain to IP address(es)"""\n    try:\n        # getaddrinfo returns all addresses (IPv4 + IPv6)\n        results = socket.getaddrinfo(domain, None)\n        ips = set()\n        for result in results:\n            ip = result[4][0]\n            ips.add(ip)\n        return list(ips)\n    except socket.gaierror as e:\n        return f"Resolution failed: {e}"\n\n# === Reverse DNS lookup ===\ndef reverse_lookup(ip):\n    """Find domain name from IP address"""\n    try:\n        hostname, _, _ = socket.gethostbyaddr(ip)\n        return hostname\n    except socket.herror:\n        return "No reverse DNS record"\n\n# === DNS record enumeration ===\ndef enumerate_dns(domain):\n    """Query different record types"""\n    record_types = ['A', 'AAAA', 'MX', 'NS', 'TXT', 'CNAME']\n    print(f"\\n[*] DNS Enumeration for {domain}")\n    print("=" * 50)\n    for rtype in record_types:\n        try:\n            # Using system dig/nslookup via subprocess\n            result = subprocess.run(\n                ['dig', '+short', domain, rtype],\n                capture_output=True, text=True, timeout=5\n            )\n            if result.stdout.strip():\n                print(f"  [{rtype:5}] {result.stdout.strip()}")\n        except Exception:\n            pass\n\n# Example usage\nprint(f"google.com -> {resolve_domain('google.com')}")\nprint(f"8.8.8.8 -> {reverse_lookup('8.8.8.8')}")\nenumerate_dns('example.com')`,
    breakdown: `\u2022 socket.getaddrinfo() \u2014 Performs the full DNS resolution process through the OS resolver. Returns all address families (IPv4 + IPv6) and socket types.\n\n\u2022 socket.gethostbyaddr() \u2014 Reverse DNS: given an IP, finds the associated hostname via PTR records. Not all IPs have reverse DNS configured.\n\n\u2022 dig +short \u2014 A command-line DNS tool that queries specific record types. '+short' gives just the answer, not the full DNS response with headers and authority sections.\n\n\u2022 MX records \u2014 Reveal the mail servers for a domain. Useful for reconnaissance to identify email infrastructure and potential phishing targets.\n\n\u2022 TXT records \u2014 Often contain SPF (email sender policy), DKIM keys, domain verification tokens, and sometimes sensitive information accidentally exposed.`,
    summary: "DNS translates domains to IPs through a hierarchical lookup: browser cache -> OS cache -> recursive resolver -> root -> TLD -> authoritative nameserver. Key record types: A, AAAA, MX, NS, TXT, CNAME. DNS is a prime reconnaissance target and vulnerable to spoofing without DNSSEC."
  },
  {
    title: "Packet Structure & Wireshark Intro",
    definition: "Network packets are structured data units with headers at each protocol layer (Ethernet, IP, TCP/UDP, Application). Wireshark is a packet analyzer that captures and decodes network traffic in real-time, allowing you to inspect every byte of communication.",
    explanation: `A typical HTTP packet has these layers (like an envelope inside an envelope):\n\nEthernet Frame: Source MAC, Destination MAC, EtherType (14 bytes header)\n  \u2514\u2500 IP Packet: Source IP, Dest IP, TTL, Protocol, Length (20 bytes header)\n       \u2514\u2500 TCP Segment: Source Port, Dest Port, Seq#, Ack#, Flags (20 bytes header)\n            \u2514\u2500 HTTP Data: "GET /index.html HTTP/1.1\\r\\nHost: example.com..."\n\nWireshark captures packets at the network interface level. It can decode 3000+ protocols and display them in a human-readable format. Key features: capture filters (limit what's captured), display filters (limit what's shown), following TCP streams (reassembling conversations), and protocol statistics.\n\nUseful Wireshark display filters: \`http\` (HTTP traffic only), \`tcp.port == 443\` (HTTPS traffic), \`ip.addr == 192.168.1.1\` (specific host), \`dns\` (DNS queries/responses), \`tcp.flags.syn == 1\` (new connections).\n\nPacket analysis is essential for: troubleshooting network issues, detecting intrusions, analyzing malware communication, forensics investigations, and understanding how protocols work at the byte level.`,
    code: `# Packet Crafting and Analysis with Scapy\n# Scapy lets you build custom packets layer by layer\nfrom scapy.all import IP, TCP, UDP, DNS, DNSQR, sr1, sniff, wrpcap\n\n# === Build a TCP SYN packet (first step of handshake) ===\nsyn_packet = IP(dst="example.com") / TCP(dport=80, flags="S")\nprint(f"[*] Packet layers: {syn_packet.summary()}")\nprint(f"    IP src: {syn_packet[IP].src}")\nprint(f"    IP dst: {syn_packet[IP].dst}")\nprint(f"    TCP sport: {syn_packet[TCP].sport}")\nprint(f"    TCP dport: {syn_packet[TCP].dport}")\nprint(f"    TCP flags: {syn_packet[TCP].flags}")\n\n# === Craft a DNS query ===\ndns_query = IP(dst="8.8.8.8") / UDP(dport=53) / DNS(\n    rd=1,  # Recursion desired\n    qd=DNSQR(qname="example.com")\n)\nprint(f"\\n[*] DNS query for: example.com")\n\n# === Packet sniffing (capture live traffic) ===\ndef packet_callback(pkt):\n    if pkt.haslayer(TCP):\n        src = f"{pkt[IP].src}:{pkt[TCP].sport}"\n        dst = f"{pkt[IP].dst}:{pkt[TCP].dport}"\n        flags = str(pkt[TCP].flags)\n        print(f"  TCP: {src} -> {dst} [{flags}]")\n\n# Capture 10 packets (requires root/admin)\n# sniff(count=10, prn=packet_callback, filter="tcp")\n\n# === Save packets to .pcap file (readable by Wireshark) ===\npackets = [syn_packet, dns_query]\n# wrpcap("captured.pcap", packets)\nprint("\\n[*] Packets ready for Wireshark analysis")`,
    breakdown: `\u2022 IP(dst="example.com") / TCP(dport=80, flags="S") \u2014 Scapy uses the / operator to stack protocol layers. This creates an IP packet containing a TCP segment with SYN flag set (connection initiation).\n\n\u2022 flags="S" \u2014 TCP flags: S=SYN, A=ACK, F=FIN, R=RST, P=PSH. A SYN packet starts the three-way handshake. "SA" would be a SYN-ACK response.\n\n\u2022 DNS with rd=1 \u2014 Recursion Desired bit. Tells the DNS server to fully resolve the query (follow the delegation chain) rather than just giving a referral.\n\n\u2022 sniff(filter="tcp") \u2014 Uses BPF (Berkeley Packet Filter) syntax to capture only TCP packets. This is the same filter language Wireshark and tcpdump use.\n\n\u2022 wrpcap() \u2014 Writes packets to a .pcap file that Wireshark can open. This is the standard packet capture format used by all network analysis tools.`,
    summary: "Packets are layered structures: Ethernet > IP > TCP/UDP > Application data. Wireshark captures and decodes all layers in real-time. Scapy (Python) lets you craft custom packets for testing. Key skills: reading packet headers, following TCP streams, and using display filters to isolate relevant traffic."
  }
];
