// Pre-written spec lessons for: Sound Design

export const mmSoundDesignLessons = [
  {
    title: "Digital Audio Fundamentals",
    definition: "Digital audio represents sound as a sequence of discrete numerical samples captured at regular intervals, where sample rate determines frequency resolution (how high a pitch can be captured) and bit depth determines dynamic range (the difference between the quietest and loudest representable sounds).",

    explanation: `Sound is a continuous pressure wave in air, but computers work with discrete numbers. Analog-to-Digital Conversion (ADC) captures sound by measuring the amplitude of the waveform at regular intervals (sampling) and storing each measurement as a number with finite precision (quantization). The resulting stream of numbers is digital audio.\n\nSample rate defines how many measurements per second are taken. The Nyquist theorem states that you must sample at least twice the highest frequency you want to capture. Human hearing extends to roughly 20kHz, so CD-quality audio samples at 44,100 Hz (44.1kHz) — slightly above the 40kHz minimum. Professional audio often uses 48kHz (video standard), 96kHz, or even 192kHz for production headroom.\n\nBit depth determines the precision of each sample — how many possible amplitude values exist. 16-bit audio has 65,536 possible values (96dB dynamic range, the CD standard). 24-bit audio has 16.7 million values (144dB dynamic range, the professional production standard). Higher bit depth means lower noise floor and more headroom for processing, not higher quality playback.\n\nAudio file formats include: WAV/AIFF (uncompressed PCM — full quality, large files), FLAC (lossless compression — identical quality, ~50% smaller), MP3/AAC/Opus (lossy compression — perceptible quality loss at low bitrates, dramatically smaller). For production work, always use uncompressed or lossless formats. Lossy formats are for final delivery only.`,
    code: `// Web Audio API: Digital audio fundamentals\nconst audioCtx = new AudioContext({\n    sampleRate: 48000  // 48kHz sample rate\n});\n\n// Create a buffer representing 1 second of audio\nconst sampleRate = audioCtx.sampleRate; // 48000\nconst duration = 1; // 1 second\nconst buffer = audioCtx.createBuffer(\n    1,                          // mono (1 channel)\n    sampleRate * duration,      // 48000 samples\n    sampleRate                  // at 48000 Hz\n);\n\n// Access raw sample data (Float32Array, range -1 to +1)\nconst samples = buffer.getChannelData(0);\n\n// Generate a pure sine wave at 440Hz (A4 note)\nconst frequency = 440;\nfor (let i = 0; i < samples.length; i++) {\n    const time = i / sampleRate;\n    samples[i] = Math.sin(2 * Math.PI * frequency * time);\n}\n\n// Bit depth concept:\n// 16-bit: values quantized to 65,536 steps\n// 24-bit: values quantized to 16,777,216 steps\n// Web Audio uses 32-bit float internally (-1.0 to +1.0)\n\n// Decibels: logarithmic loudness scale\nfunction amplitudeToDb(amplitude) {\n    return 20 * Math.log10(Math.abs(amplitude));\n}\nfunction dbToAmplitude(db) {\n    return Math.pow(10, db / 20);\n}\n// -6 dB = half amplitude, -12 dB = quarter amplitude\n// 0 dBFS = maximum digital level (clipping threshold)\n\n// Nyquist demonstration\nconst maxFrequency = sampleRate / 2; // 24000 Hz\n// Any frequency above this will ALIAS (fold back)\n// creating false frequencies below Nyquist`,

    breakdown: `• Sample rate — 44.1kHz captures up to 22.05kHz (above human hearing). 48kHz is the video/broadcast standard. Higher rates (96/192kHz) provide headroom for pitch-shifting and time-stretching in production, not audible quality improvement for playback.\n\n• Bit depth — 16-bit gives 96dB dynamic range (quiet room to pain threshold). 24-bit gives 144dB (well beyond physical reality). Always record and process in 24-bit; dither down to 16-bit only for final CD delivery.\n\n• Decibels (dB) — A logarithmic scale matching human perception. +6dB = double the amplitude. 0 dBFS (Full Scale) is the maximum digital level — exceeding it causes hard clipping (harsh distortion). Always leave headroom below 0 dBFS.\n\n• Aliasing — Frequencies above the Nyquist limit (half the sample rate) fold back as false lower frequencies. Anti-aliasing filters in ADC converters remove ultrasonic content before sampling to prevent this artifact.\n\n• Dithering — When reducing bit depth (24-bit to 16-bit), quantization creates correlated distortion. Dithering adds tiny random noise that converts this distortion into a constant, inaudible noise floor. Always dither when reducing bit depth.`,
    summary: "Digital audio captures sound as discrete samples at a specific rate (44.1-48kHz) and precision (16-24 bit). Sample rate determines the maximum representable frequency via the Nyquist theorem, bit depth determines dynamic range, and understanding decibels and dithering ensures clean signal processing."
  },
  {
    title: "Synthesis: Subtractive & FM",
    definition: "Sound synthesis generates audio electronically or digitally from basic waveforms. Subtractive synthesis shapes sound by filtering harmonics from harmonically-rich oscillator waveforms, while FM (Frequency Modulation) synthesis creates complex timbres by using one oscillator's output to modulate another oscillator's frequency.",

    explanation: `Subtractive synthesis is the most intuitive and widely-used synthesis method, pioneered by analog synthesizers like the Moog and Roland Juno. The signal chain is: Oscillator (generates a harmonically rich waveform) → Filter (removes unwanted harmonics) → Amplifier (controls volume over time via envelope). By starting with a bright, buzzy waveform and filtering away frequencies, you 'sculpt' the sound you want.\n\nThe oscillator generates basic waveforms: sawtooth (all harmonics — buzzy, brass-like), square/pulse (odd harmonics — hollow, clarinet-like), triangle (weak odd harmonics — mellow, flute-like), and noise (all frequencies — for percussion and texture). Mixing multiple oscillators at different pitches (detuned for thickness, or at intervals for chords) provides the raw tonal material.\n\nThe filter is the defining element of subtractive synthesis. A low-pass filter (LPF) removes frequencies above its cutoff point — sweeping the cutoff down makes the sound darker and more muffled. Filter resonance boosts frequencies at the cutoff point, creating a pronounced peak. Envelope modulation of filter cutoff (the filter 'opens' on key press then closes) creates the classic synth pluck/bass sound.\n\nFM synthesis, made famous by the Yamaha DX7, works entirely differently. Instead of filtering, it creates harmonic complexity by modulating one oscillator's frequency with another. A 'carrier' oscillator produces the audible tone while a 'modulator' oscillator changes the carrier's frequency rapidly. The ratio between carrier and modulator frequencies determines whether the result sounds harmonic (musical) or inharmonic (metallic/bell-like).`,
    code: `// Web Audio API: Subtractive Synthesis\nconst audioCtx = new AudioContext();\n\n// SUBTRACTIVE SYNTH: Oscillator -> Filter -> Amp\nfunction createSubtractiveSynth(frequency, filterCutoff) {\n    // Oscillator: harmonically rich sawtooth wave\n    const osc = audioCtx.createOscillator();\n    osc.type = 'sawtooth';\n    osc.frequency.value = frequency; // e.g., 220 Hz (A3)\n\n    // Filter: Low-pass removes high harmonics\n    const filter = audioCtx.createBiquadFilter();\n    filter.type = 'lowpass';\n    filter.frequency.value = filterCutoff; // e.g., 1500 Hz\n    filter.Q.value = 5; // Resonance: peak at cutoff\n\n    // Amplifier with ADSR envelope\n    const amp = audioCtx.createGain();\n    amp.gain.value = 0;\n\n    // Signal chain: osc -> filter -> amp -> output\n    osc.connect(filter);\n    filter.connect(amp);\n    amp.connect(audioCtx.destination);\n\n    // ADSR Envelope on amplifier\n    const now = audioCtx.currentTime;\n    amp.gain.setValueAtTime(0, now);\n    amp.gain.linearRampToValueAtTime(0.8, now + 0.01); // Attack\n    amp.gain.linearRampToValueAtTime(0.5, now + 0.1);  // Decay\n    // Sustain holds at 0.5\n    // Release triggered on note-off\n\n    // Filter envelope (cutoff sweeps open then closed)\n    filter.frequency.setValueAtTime(200, now);\n    filter.frequency.linearRampToValueAtTime(filterCutoff, now + 0.05);\n    filter.frequency.exponentialRampToValueAtTime(400, now + 0.5);\n\n    osc.start(now);\n    return { osc, filter, amp };\n}\n\n// FM SYNTHESIS: Modulator modulates Carrier frequency\nfunction createFMSynth(carrierFreq, modFreq, modDepth) {\n    // Modulator oscillator\n    const modulator = audioCtx.createOscillator();\n    modulator.frequency.value = modFreq; // e.g., 440 Hz\n\n    // Modulation depth (how much frequency deviation)\n    const modGain = audioCtx.createGain();\n    modGain.gain.value = modDepth; // e.g., 500 (Hz deviation)\n\n    // Carrier oscillator (the audible output)\n    const carrier = audioCtx.createOscillator();\n    carrier.frequency.value = carrierFreq; // e.g., 220 Hz\n\n    const amp = audioCtx.createGain();\n    amp.gain.value = 0.3;\n\n    // FM routing: modulator -> modGain -> carrier.frequency\n    modulator.connect(modGain);\n    modGain.connect(carrier.frequency); // Modulates frequency!\n    carrier.connect(amp);\n    amp.connect(audioCtx.destination);\n\n    modulator.start();\n    carrier.start();\n    // Ratio 2:1 = octave harmonics (musical)\n    // Ratio 1.41:1 = inharmonic (bell/metallic)\n}`,

    breakdown: `• Oscillator waveforms — Sawtooth (buzz, all harmonics), Square (hollow, odd harmonics), Triangle (soft, few harmonics), Sine (pure, no harmonics). These are the raw materials for subtractive synthesis. Combine and detune multiple oscillators for thickness.\n\n• Filter types — Low-pass (removes highs, most common), High-pass (removes lows, thins sound), Band-pass (isolates a frequency band), Notch (removes a narrow band). Filter slope (12dB/oct, 24dB/oct) determines how aggressively frequencies are cut.\n\n• ADSR Envelope — Attack (time to reach peak), Decay (time to fall to sustain level), Sustain (held level while key is pressed), Release (time to fade after key release). Applied to amplitude for volume shape and filter cutoff for timbral evolution.\n\n• FM ratios — Carrier:Modulator frequency ratio determines timbre character. Integer ratios (1:1, 2:1, 3:1) produce harmonic, musical tones. Non-integer ratios (1:1.41, 1:3.14) produce inharmonic, metallic/bell tones. FM excels at electric pianos, bells, and metallic sounds.\n\n• Modulation index — In FM synthesis, the depth of modulation controls brightness/complexity. Low index = subtle, gentle timbres. High index = harsh, complex spectra. Animating the index over time (via envelope) creates evolving, expressive sounds.`,
    summary: "Subtractive synthesis sculpts sound by filtering harmonics from rich oscillator waveforms using filters and ADSR envelopes, while FM synthesis creates complex timbres through frequency modulation between oscillators. These two methods cover most synthetic sound design needs from warm bass to metallic bells."
  },
  {
    title: "Sampling & Sample Manipulation",
    definition: "Sampling is the technique of recording or digitally capturing real-world sounds (or segments of existing recordings) and repurposing them as playable instruments or sound design elements, with manipulation techniques like pitch-shifting, time-stretching, slicing, and layering to transform the original material.",

    explanation: `Sampling revolutionized music production by allowing any recorded sound to become a musical instrument. A piano can be sampled at multiple velocities and pitches to create a realistic virtual instrument. A drum hit can be sliced, processed, and triggered rhythmically. Even ambient textures, vocal snippets, or household sounds can become melodic or percussive elements through creative manipulation.\n\nThe basic sampler workflow is: record or import audio, set the playback root note (the pitch at which the sample plays at original speed), define the playback region (start/end points, loop points), and map it across a keyboard range. When you play a note higher than the root, the sample plays faster (shorter + higher pitch). Lower notes play slower (longer + lower pitch). Multi-sample instruments record the source at many pitches and velocities to avoid obvious pitch-shifting artifacts.\n\nTime-stretching and pitch-shifting are the two most powerful sample manipulation tools. Time-stretching changes duration without affecting pitch (making a vocal phrase fit a different tempo). Pitch-shifting changes pitch without affecting duration (transposing a melody). Algorithms like phase vocoding, granular resynthesis, and elastique handle these transformations, each with different quality tradeoffs.\n\nGranular synthesis takes sampling further by slicing audio into tiny grains (1-100ms) and recombining them in various ways — scattering, overlapping, randomizing pitch and position. This can transform a simple recording into evolving textures, clouds of sound, or frozen moments that sustain indefinitely. It bridges the gap between sampling and synthesis.`,
    code: `// Web Audio API: Sampling & Manipulation\nconst audioCtx = new AudioContext();\n\n// Load and play a sample\nasync function loadSample(url) {\n    const response = await fetch(url);\n    const arrayBuffer = await response.arrayBuffer();\n    const audioBuffer = await audioCtx.decodeAudioData(arrayBuffer);\n    return audioBuffer;\n}\n\n// Play sample at different pitches (chromatic sampler)\nfunction playSampleAtPitch(buffer, semitones) {\n    const source = audioCtx.createBufferSource();\n    source.buffer = buffer;\n\n    // Pitch shifting via playback rate\n    // Each semitone = multiply rate by 2^(1/12)\n    source.playbackRate.value = Math.pow(2, semitones / 12);\n    // +12 semitones = double speed (1 octave up)\n    // -12 semitones = half speed (1 octave down)\n\n    source.connect(audioCtx.destination);\n    source.start();\n    return source;\n}\n\n// Sample slicing for drum patterns\nfunction sliceSample(buffer, numSlices) {\n    const sliceDuration = buffer.duration / numSlices;\n    const slices = [];\n\n    for (let i = 0; i < numSlices; i++) {\n        slices.push({\n            startTime: i * sliceDuration,\n            duration: sliceDuration,\n            play: () => {\n                const source = audioCtx.createBufferSource();\n                source.buffer = buffer;\n                source.connect(audioCtx.destination);\n                source.start(0, i * sliceDuration, sliceDuration);\n            }\n        });\n    }\n    return slices;\n}\n\n// Granular synthesis concept\nfunction granularPlayback(buffer, grainSize, density, scatter) {\n    // grainSize: 0.02-0.1 seconds (20-100ms)\n    // density: grains per second\n    // scatter: random position offset\n\n    const interval = 1000 / density; // ms between grains\n\n    setInterval(() => {\n        const source = audioCtx.createBufferSource();\n        source.buffer = buffer;\n\n        // Random position in buffer + scatter\n        const position = Math.random() * buffer.duration;\n        const offset = (Math.random() - 0.5) * scatter;\n\n        // Envelope each grain to avoid clicks\n        const grainAmp = audioCtx.createGain();\n        grainAmp.gain.setValueAtTime(0, audioCtx.currentTime);\n        grainAmp.gain.linearRampToValueAtTime(0.3,\n            audioCtx.currentTime + grainSize * 0.1);\n        grainAmp.gain.linearRampToValueAtTime(0,\n            audioCtx.currentTime + grainSize);\n\n        source.connect(grainAmp);\n        grainAmp.connect(audioCtx.destination);\n        source.start(0, position + offset, grainSize);\n    }, interval);\n}`,

    breakdown: `• Playback rate pitch shifting — The simplest pitch change method: playing a sample faster raises pitch but shortens duration. Each semitone = rate * 2^(1/12). This 'chipmunk effect' is acceptable for drums but obvious on sustained sounds.\n\n• Multi-sampling — Recording an instrument at every 2-3 semitones and at 3-4 velocity levels (soft, medium, loud, fortissimo) creates realistic virtual instruments. Round-robin (cycling through multiple recordings of the same note) prevents the 'machine gun effect.'\n\n• Time-stretching algorithms — Phase vocoder (good for sustained sounds, can smear transients), granular (versatile but can sound grainy), WSOLA (waveform similarity overlap-add, good for speech). Each algorithm has sweet spots and artifacts at extreme stretch factors.\n\n• Loop points — Sustaining sounds need seamless loop regions. Set loop start/end at zero-crossings where the waveform crosses zero amplitude. Crossfade the loop boundary to hide the seam. Bad loop points create audible clicks or timbral shifts.\n\n• Layering — Combine multiple samples for complex sounds. A snare drum might layer: a top mic sample (snap/attack), bottom mic (snare wire buzz), room mic (ambience), and a synthesized sub-frequency layer (weight). Each layer contributes a different character.`,
    summary: "Sampling captures real-world audio as playable instruments, with pitch controlled via playback rate and multi-sampling providing realism. Manipulation techniques like time-stretching, slicing, granular synthesis, and layering transform raw recordings into entirely new sonic textures and instruments."
  },
  {
    title: "Effects Processing (Reverb, Delay, EQ)",
    definition: "Audio effects process sound signals to alter their character: reverb simulates acoustic space reflections, delay creates time-based echoes, and EQ (equalization) boosts or cuts specific frequency bands to shape the tonal balance of a sound.",

    explanation: `Audio effects are the tools that transform raw recorded or synthesized sounds into polished, professional audio. They can simulate physical environments (reverb, delay), shape frequency content (EQ, filters), control dynamics (compression, limiting), or create entirely new sonic textures (distortion, chorus, flanging). Understanding when and how to apply effects is what separates amateur productions from professional ones.\n\nReverb is arguably the most important effect in audio production. It simulates the reflections that occur when sound bounces off surfaces in a physical space — a small room produces quick, dense reflections while a cathedral produces long, spacious decay. Key parameters include: pre-delay (time before first reflections), decay time (RT60 — how long until reflections fade by 60dB), diffusion (density of reflections), and wet/dry mix (how much reverb versus original signal).\n\nDelay creates distinct echoes by repeating the input signal after a time interval. Unlike reverb (thousands of overlapping reflections), delay produces identifiable repeating copies. Syncing delay time to musical tempo (quarter notes, eighth notes, dotted patterns) creates rhythmic interest. Feedback controls how many times the echo repeats. Modulating delay time creates chorus and flanging effects.\n\nEQ shapes the frequency spectrum of a sound. A parametric EQ allows you to boost or cut at any frequency with adjustable bandwidth (Q factor). High-pass filters remove rumble below a cutoff, low-pass filters tame harsh highs, and bell curves notch out problematic resonances or boost presence. EQ decisions should be corrective first (fixing problems) and creative second (enhancing character).`,
    code: `// Web Audio API: Effects Processing\nconst audioCtx = new AudioContext();\n\n// REVERB using ConvolverNode (impulse response)\nasync function createReverb(impulseUrl) {\n    const convolver = audioCtx.createConvolver();\n    const response = await fetch(impulseUrl);\n    const buffer = await response.arrayBuffer();\n    convolver.buffer = await audioCtx.decodeAudioData(buffer);\n\n    // Wet/Dry mix\n    const dryGain = audioCtx.createGain();\n    const wetGain = audioCtx.createGain();\n    dryGain.gain.value = 0.7;  // 70% dry\n    wetGain.gain.value = 0.3;  // 30% wet\n\n    return { convolver, dryGain, wetGain };\n}\n\n// DELAY with feedback\nfunction createDelay(delayTime, feedback) {\n    const delay = audioCtx.createDelay(5.0); // max 5 seconds\n    delay.delayTime.value = delayTime; // e.g., 0.375s (dotted 8th at 120bpm)\n\n    const feedbackGain = audioCtx.createGain();\n    feedbackGain.gain.value = feedback; // 0-0.9 (>0.95 = runaway!)\n\n    const filter = audioCtx.createBiquadFilter();\n    filter.type = 'lowpass';\n    filter.frequency.value = 3000; // Darken repeats (natural decay)\n\n    // Feedback loop: delay -> filter -> feedback -> delay\n    delay.connect(filter);\n    filter.connect(feedbackGain);\n    feedbackGain.connect(delay);\n\n    return { delay, feedbackGain, filter };\n}\n\n// PARAMETRIC EQ\nfunction createParametricEQ() {\n    // High-pass: remove rumble below 80Hz\n    const highPass = audioCtx.createBiquadFilter();\n    highPass.type = 'highpass';\n    highPass.frequency.value = 80;\n    highPass.Q.value = 0.7;\n\n    // Low-shelf: boost warmth at 200Hz\n    const lowShelf = audioCtx.createBiquadFilter();\n    lowShelf.type = 'lowshelf';\n    lowShelf.frequency.value = 200;\n    lowShelf.gain.value = 3; // +3dB\n\n    // Peaking (bell): cut muddy 400Hz\n    const midCut = audioCtx.createBiquadFilter();\n    midCut.type = 'peaking';\n    midCut.frequency.value = 400;\n    midCut.Q.value = 2; // Narrow cut\n    midCut.gain.value = -4; // -4dB\n\n    // High-shelf: add 'air' above 10kHz\n    const highShelf = audioCtx.createBiquadFilter();\n    highShelf.type = 'highshelf';\n    highShelf.frequency.value = 10000;\n    highShelf.gain.value = 2; // +2dB\n\n    // Chain: input -> HP -> LS -> Mid -> HS -> output\n    highPass.connect(lowShelf);\n    lowShelf.connect(midCut);\n    midCut.connect(highShelf);\n\n    return { highPass, lowShelf, midCut, highShelf };\n}`,

    breakdown: `• Reverb types — Algorithmic reverb generates reflections mathematically (adjustable, CPU-efficient). Convolution reverb uses recorded impulse responses of real spaces (photorealistic acoustics, less adjustable). Use convolution for realism, algorithmic for creative effects.\n\n• Delay sync — Calculate delay time from BPM: quarterNote = 60/BPM seconds. Eighth = quarter/2, dotted eighth = quarter*0.75. Synced delays create rhythmic patterns that enhance musicality. Unsynced delays create washy, ambient textures.\n\n• EQ philosophy — Cut narrow, boost wide. Cutting removes problems surgically (a resonant 2kHz ring). Boosting enhances character broadly (gentle high-shelf for 'air'). If everything needs boosting, the problem is elsewhere (recording quality, arrangement).\n\n• Send vs Insert — Insert effects process 100% of the signal in-line (EQ, compression). Send effects receive a copy of the signal (reverb, delay) and blend in parallel. Send effects allow one reverb instance shared by multiple tracks, saving CPU and creating cohesive space.\n\n• Effect order matters — EQ before reverb means you reverb the EQ'd signal (clean reverb). Reverb before EQ means you can EQ the reverb tail separately. Compression before delay means consistent echo levels. Standard chain: EQ -> Compression -> Modulation -> Delay -> Reverb.`,
    summary: "Effects processing transforms raw audio through reverb (spatial simulation), delay (time-based echoes synced to tempo), and EQ (frequency shaping with surgical cuts and broad boosts). Understanding signal routing (send vs insert), effect chain order, and parameter relationships is essential for professional audio production."
  },
  {
    title: "Spatial Audio & 3D Sound",
    definition: "Spatial audio positions sound sources in three-dimensional space around the listener, using techniques like binaural rendering, ambisonics, and channel-based surround systems to create an immersive auditory experience where sounds appear to come from specific directions and distances.",

    explanation: `Spatial audio is the technology behind immersive sound in VR, games, cinema, and emerging music formats. It exploits the psychoacoustic cues humans use to locate sound sources: Interaural Time Difference (ITD — sound arrives at the nearer ear first), Interaural Level Difference (ILD — the head shadows the far ear, reducing high frequencies), and spectral cues from the outer ear (pinna) that encode elevation information.\n\nBinaural audio renders 3D sound for headphone playback by simulating how each ear would receive sound from a given position. Head-Related Transfer Functions (HRTFs) are measured filters that capture how sound changes as it travels around a human head and ears. Applying the correct HRTF pair for a source position makes sound appear to come from that specific location in space — even with just two speakers (headphones).\n\nAmbisonics is a full-sphere surround format that captures (or synthesizes) a complete 3D sound field. Unlike channel-based systems (5.1, 7.1) that target specific speaker positions, ambisonics is speaker-independent — it can be decoded to any speaker arrangement or rendered binaurally for headphones. First-order ambisonics uses 4 channels (W, X, Y, Z), higher orders add more spatial resolution.\n\nIn web and game development, the Web Audio API's PannerNode provides basic 3D positioning, while libraries like Resonance Audio (Google) and Steam Audio offer advanced HRTF rendering, room acoustics simulation, and occlusion modeling (sound muffled by walls). These systems update sound positions and properties in real-time as the listener or sources move, creating convincing spatial experiences.`,
    code: `// Web Audio API: 3D Spatial Audio\nconst audioCtx = new AudioContext();\n\n// Set up the listener (camera/player position)\nconst listener = audioCtx.listener;\nlistener.positionX.value = 0;\nlistener.positionY.value = 1.6; // Head height\nlistener.positionZ.value = 0;\n// Listener orientation (forward and up vectors)\nlistener.forwardX.value = 0;\nlistener.forwardY.value = 0;\nlistener.forwardZ.value = -1; // Looking into screen\nlistener.upX.value = 0;\nlistener.upY.value = 1;\nlistener.upZ.value = 0;\n\n// Create a 3D positioned sound source\nfunction create3DSource(buffer, x, y, z) {\n    const source = audioCtx.createBufferSource();\n    source.buffer = buffer;\n    source.loop = true;\n\n    const panner = audioCtx.createPanner();\n    // HRTF model for realistic headphone spatialization\n    panner.panningModel = 'HRTF';\n    // Distance attenuation model\n    panner.distanceModel = 'inverse';\n    panner.refDistance = 1;      // Full volume distance\n    panner.maxDistance = 100;    // Silence distance\n    panner.rolloffFactor = 1;    // Attenuation curve\n    // Cone for directional sounds (e.g., speaker)\n    panner.coneInnerAngle = 60;  // Full volume cone\n    panner.coneOuterAngle = 120; // Attenuated zone\n    panner.coneOuterGain = 0.3;  // Volume outside cone\n\n    // Position the sound in 3D space\n    panner.positionX.value = x;\n    panner.positionY.value = y;\n    panner.positionZ.value = z;\n\n    source.connect(panner);\n    panner.connect(audioCtx.destination);\n    source.start();\n\n    return { source, panner };\n}\n\n// Example: Sounds positioned around listener\nconst campfire = create3DSource(fireBuffer, 3, 0, -2);\nconst river = create3DSource(waterBuffer, -5, 0, 1);\nconst bird = create3DSource(birdBuffer, 1, 4, -3);\n\n// Animate source position (orbiting sound)\nfunction orbitSound(panner, radius, speed) {\n    let angle = 0;\n    function update() {\n        angle += speed;\n        panner.positionX.value = Math.cos(angle) * radius;\n        panner.positionZ.value = Math.sin(angle) * radius;\n        requestAnimationFrame(update);\n    }\n    update();\n}\n\n// Update listener position from game/VR camera\nfunction updateListenerFromCamera(camera) {\n    listener.positionX.value = camera.position.x;\n    listener.positionY.value = camera.position.y;\n    listener.positionZ.value = camera.position.z;\n    // Forward direction from camera rotation\n    const forward = camera.getWorldDirection();\n    listener.forwardX.value = forward.x;\n    listener.forwardY.value = forward.y;\n    listener.forwardZ.value = forward.z;\n}`,

    breakdown: `• HRTF (Head-Related Transfer Function) — Personalized filters measured for each ear that encode how sound from different directions is modified by the head, ears, and torso. Generic HRTFs work reasonably well; personalized measurements provide dramatically better externalization.\n\n• Distance cues — Real-world distance perception uses: volume (inverse square law), high-frequency absorption (distant sounds are duller), wet/dry ratio (more reverb = more distant), and early reflection timing. Implement all four for convincing distance.\n\n• Ambisonics — Scene-based audio format that captures the complete sound field. First-order (4 channels) provides basic directionality. Third-order (16 channels) gives precise localization. Decoded to speakers or headphones at playback time, making it future-proof.\n\n• Occlusion & obstruction — When a wall is between source and listener, muffle the sound (low-pass filter + volume reduction). Obstruction (partial blockage) reduces direct sound but not reflections. These cues reinforce visual spatial awareness in games and VR.\n\n• Performance in games — Spatial audio processing (HRTF convolution per source, room simulation, occlusion raycasts) is CPU-intensive. Budget for 20-50 fully spatialized sources; use simpler panning for non-critical ambient sounds. Prioritize sources closest to the listener.`,
    summary: "Spatial audio creates immersive 3D sound using HRTF-based binaural rendering for headphones, ambisonics for speaker-independent surround, and distance/occlusion modeling for realistic environments. The Web Audio API's PannerNode with HRTF mode provides accessible 3D positioning for web and game applications."
  }
];
