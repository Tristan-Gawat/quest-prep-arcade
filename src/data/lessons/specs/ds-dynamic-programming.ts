// Pre-written spec lessons for: Dynamic Programming

export const dsDynamicProgrammingLessons = [
  {
    title: "What is Dynamic Programming?",
    definition: "Dynamic Programming (DP) is an algorithmic technique that solves complex problems by breaking them into simpler overlapping subproblems, solving each subproblem only once, and storing the results to avoid redundant computation. It applies when a problem has optimal substructure (optimal solution contains optimal solutions to subproblems) and overlapping subproblems.",

    explanation: `Dynamic Programming is one of the most powerful problem-solving techniques in computer science. It transforms exponential-time recursive solutions into polynomial-time iterative solutions by eliminating redundant work. The name was coined by Richard Bellman in the 1950s.\n\nTwo conditions must be met for DP to apply: (1) Optimal substructure — the optimal solution to the problem can be constructed from optimal solutions of its subproblems. (2) Overlapping subproblems — the same subproblems are solved multiple times during recursion. Without overlapping subproblems, plain divide-and-conquer suffices (like merge sort).\n\nConsider computing Fibonacci(5). The naive recursive approach computes Fib(3) twice, Fib(2) three times, and Fib(1) five times. This redundancy causes exponential O(2^n) time complexity. DP stores each Fibonacci value after computing it once, reducing time to O(n).\n\nDP problems typically follow a pattern: (1) Define the state — what information characterizes a subproblem? (2) Define the recurrence relation — how does the solution to a larger problem relate to smaller subproblems? (3) Define base cases — what are the trivial subproblems with known solutions? (4) Determine computation order — ensure subproblems are solved before they're needed.\n\nDP appears everywhere: shortest paths (Bellman-Ford, Floyd-Warshall), sequence alignment in bioinformatics, optimal game strategies, resource allocation, text justification, and countless interview problems. Learning to recognize DP patterns and formulate recurrences is essential for algorithmic problem solving.`,

    code: `// Fibonacci: Demonstrating why DP matters\n\n// Naive recursive approach - O(2^n) time!\nfunction fibNaive(n) {\n    if (n <= 1) return n;\n    return fibNaive(n - 1) + fibNaive(n - 2);\n    // fib(5) calls fib(4) + fib(3)\n    // fib(4) calls fib(3) + fib(2) <-- fib(3) computed AGAIN\n    // Total calls grows exponentially\n}\n\n// DP approach (tabulation) - O(n) time, O(n) space\nfunction fibDP(n) {\n    if (n <= 1) return n;\n    const dp = new Array(n + 1);\n    dp[0] = 0;\n    dp[1] = 1;\n    for (let i = 2; i <= n; i++) {\n        dp[i] = dp[i - 1] + dp[i - 2]; // Each value computed once\n    }\n    return dp[n];\n}\n\n// Space-optimized DP - O(n) time, O(1) space\nfunction fibOptimal(n) {\n    if (n <= 1) return n;\n    let prev2 = 0, prev1 = 1;\n    for (let i = 2; i <= n; i++) {\n        const current = prev1 + prev2;\n        prev2 = prev1;\n        prev1 = current;\n    }\n    return prev1;\n}\n\n// Comparison:\n// fibNaive(40) takes ~1 second (2^40 operations)\n// fibDP(40) takes microseconds (40 operations)\n// fibOptimal(40) same speed, but O(1) space\n\nconsole.log(fibDP(10)); // 55\nconsole.log(fibOptimal(50)); // 12586269025`,

    breakdown: `• Optimal substructure — The optimal solution to the whole problem is built from optimal solutions to subproblems. For Fibonacci: fib(n) = fib(n-1) + fib(n-2). For shortest path: shortest path to v = shortest path to u + edge(u,v) for some predecessor u.\n\n• Overlapping subproblems — The same subproblem appears multiple times in the recursion tree. Fibonacci is classic: fib(5) needs fib(3) which is also needed by fib(4). Without overlap, DP isn't needed (use plain divide-and-conquer instead).\n\n• Exponential to polynomial — Naive recursion on overlapping subproblems yields exponential time because it recomputes the same results. DP computes each subproblem once and stores it, reducing Fibonacci from O(2^n) to O(n), and many other problems from exponential to polynomial.\n\n• Identifying DP problems — Look for: counting problems (how many ways?), optimization problems (minimum cost, maximum value), and decision problems (is it possible?) where choices affect future choices. If a greedy approach doesn't work but the problem has recursive structure, it's likely DP.\n\n• DP vs Greedy vs Divide-and-Conquer — Greedy makes locally optimal choices (no backtracking). Divide-and-conquer splits into independent subproblems (no overlap). DP handles overlapping subproblems where local greedy choices aren't globally optimal.`,
    summary: "Dynamic Programming eliminates redundant computation by storing subproblem solutions. It applies when a problem has optimal substructure and overlapping subproblems, transforming exponential recursive solutions into polynomial ones. Mastering DP requires practice in defining states, recurrences, and base cases."
  },

  {
    title: "Memoization (Top-Down)",
    definition: "Memoization is a top-down dynamic programming technique where you write the natural recursive solution but add a cache (memo table) to store results of subproblems as they're computed. When a subproblem is encountered again, the cached result is returned instead of recomputing it.",
    explanation: `Memoization preserves the natural recursive structure of a solution while eliminating redundant computation. You start with the original problem, recurse into subproblems as needed, and cache results in a hash map or array. The next time the same subproblem is needed, you return the cached value in O(1).\n\nThe implementation pattern is straightforward: (1) Create a memo object (hash map or array). (2) At the start of your recursive function, check if the result for the current state is already in the memo. If yes, return it. (3) If not, compute the result recursively as normal. (4) Before returning, store the result in the memo.\n\nMemoization is often easier to implement than tabulation because you can start with the recursive solution and simply add caching. It also only computes subproblems that are actually needed (lazy evaluation). If some subproblems are never reached from the original problem, memoization skips them entirely.\n\nHowever, memoization has drawbacks: recursion uses stack space (risk of stack overflow for deep recursion), function call overhead is significant for many small subproblems, and the hash map has overhead compared to array-based tabulation. For problems with very deep recursion (e.g., n > 10000), tabulation is preferred.\n\nMemoization works naturally with multi-dimensional state spaces. If your subproblem is characterized by two parameters (i, j), your memo can be a 2D array or a map with composite keys. This makes it intuitive for problems like grid traversal, string matching, and interval DP.`,

    code: `// Memoization pattern: Climbing Stairs\n// How many distinct ways to climb n stairs (1 or 2 steps at a time)?\n\n// Without memoization - O(2^n) time\nfunction climbStairsNaive(n) {\n    if (n <= 2) return n;\n    return climbStairsNaive(n - 1) + climbStairsNaive(n - 2);\n}\n\n// With memoization - O(n) time, O(n) space\nfunction climbStairs(n, memo = {}) {\n    if (n <= 2) return n;\n    if (memo[n] !== undefined) return memo[n]; // Cache hit!\n\n    memo[n] = climbStairs(n - 1, memo) + climbStairs(n - 2, memo);\n    return memo[n];\n}\n\n// More complex example: Minimum path sum in a grid\n// Find minimum cost path from top-left to bottom-right\nfunction minPathSum(grid) {\n    const rows = grid.length;\n    const cols = grid[0].length;\n    const memo = Array.from({length: rows}, () => new Array(cols).fill(-1));\n\n    function dp(i, j) {\n        // Base case: reached destination\n        if (i === 0 && j === 0) return grid[0][0];\n        // Out of bounds\n        if (i < 0 || j < 0) return Infinity;\n        // Check memo\n        if (memo[i][j] !== -1) return memo[i][j];\n\n        // Recurrence: come from above or from left\n        memo[i][j] = grid[i][j] + Math.min(\n            dp(i - 1, j),  // from above\n            dp(i, j - 1)   // from left\n        );\n        return memo[i][j];\n    }\n\n    return dp(rows - 1, cols - 1);\n}\n\nconst grid = [\n    [1, 3, 1],\n    [1, 5, 1],\n    [4, 2, 1]\n];\nconsole.log(minPathSum(grid)); // 7 (path: 1->3->1->1->1)`,

    breakdown: `• Top-down approach — Start from the original problem and recurse into smaller subproblems. The recursion naturally defines the computation order — subproblems are solved on-demand, not pre-computed.\n\n• Cache check pattern — Every memoized function starts with: (1) base case check, (2) memo lookup, (3) compute and store. This pattern is universal and mechanical to apply once you have the recursive solution.\n\n• Lazy evaluation advantage — Memoization only computes subproblems reachable from the original problem. If the recursion tree doesn't touch certain subproblems, they're never computed. This can be more efficient than tabulation when the state space is sparse.\n\n• Stack depth limitation — Deep recursion can cause stack overflow (typically limited to ~10,000 frames in JavaScript, ~1,000 in Python by default). For large n, either increase the stack limit, use iterative tabulation, or convert to iterative memoization using an explicit stack.\n\n• Multi-dimensional memoization — For subproblems with multiple parameters (i, j, k...), use nested arrays or a Map with composite keys. The memo structure mirrors the state space dimensions.`,
    summary: "Memoization adds caching to recursive solutions — check the memo before computing, store results after computing. It preserves the intuitive recursive structure while achieving polynomial time. Best suited for problems where not all subproblems are needed and recursion depth is manageable."
  },

  {
    title: "Tabulation (Bottom-Up)",
    definition: "Tabulation is a bottom-up dynamic programming technique where you fill a table (array) iteratively, starting from the smallest subproblems and building up to the final answer. Unlike memoization, tabulation doesn't use recursion — it determines the computation order explicitly and fills entries in that order.",
    explanation: `Tabulation inverts the top-down approach: instead of starting at the target and recursing down, you start at the base cases and build up. You create a DP table (usually an array), initialize base cases, then fill remaining entries using the recurrence relation in a specific order that ensures dependencies are already computed.\n\nThe iterative nature of tabulation eliminates recursion overhead and stack overflow risk. It also tends to have better cache performance because it accesses memory sequentially. For most DP problems, tabulation is slightly faster than memoization in practice due to lower constant factors.\n\nTo convert a memoized solution to tabulation: (1) Determine the table dimensions (same as memo dimensions). (2) Initialize base cases in the table. (3) Determine the fill order so that when you compute dp[i], all values it depends on are already filled. (4) Write nested loops to fill the table in that order. (5) Return the entry corresponding to the original problem.\n\nSpace optimization is a major advantage of tabulation. When a recurrence only depends on the previous row (or previous few values), you can reduce space from O(n²) to O(n) or even O(1). For example, Fibonacci only needs the previous two values, reducing space from O(n) to O(1). Many 2D DP problems can be solved with just two rows.\n\nTabulation is preferred when: all subproblems will be needed, the state space is dense, the problem has deep recursion that would overflow the stack, or when space optimization is critical.`,

    code: `// Tabulation: Coin Change Problem\n// Find minimum number of coins to make amount\nfunction coinChange(coins, amount) {\n    // dp[i] = minimum coins needed to make amount i\n    const dp = new Array(amount + 1).fill(Infinity);\n    dp[0] = 0; // Base case: 0 coins needed for amount 0\n\n    // Fill table bottom-up\n    for (let i = 1; i <= amount; i++) {\n        for (const coin of coins) {\n            if (coin <= i && dp[i - coin] !== Infinity) {\n                dp[i] = Math.min(dp[i], dp[i - coin] + 1);\n            }\n        }\n    }\n\n    return dp[amount] === Infinity ? -1 : dp[amount];\n}\n\nconsole.log(coinChange([1, 5, 10, 25], 36)); // 3 (25+10+1)\nconsole.log(coinChange([2], 3)); // -1 (impossible)\n\n// 2D Tabulation: Unique Paths in Grid\n// Count paths from top-left to bottom-right (only right/down)\nfunction uniquePaths(m, n) {\n    // dp[i][j] = number of ways to reach cell (i, j)\n    const dp = Array.from({length: m}, () => new Array(n).fill(0));\n\n    // Base cases: first row and first column all have 1 path\n    for (let i = 0; i < m; i++) dp[i][0] = 1;\n    for (let j = 0; j < n; j++) dp[0][j] = 1;\n\n    // Fill table: each cell = paths from above + paths from left\n    for (let i = 1; i < m; i++) {\n        for (let j = 1; j < n; j++) {\n            dp[i][j] = dp[i-1][j] + dp[i][j-1];\n        }\n    }\n\n    return dp[m-1][n-1];\n}\n\nconsole.log(uniquePaths(3, 7)); // 28\n\n// Space-optimized: only keep current row\nfunction uniquePathsOptimized(m, n) {\n    let row = new Array(n).fill(1);\n    for (let i = 1; i < m; i++) {\n        for (let j = 1; j < n; j++) {\n            row[j] += row[j-1]; // current = above + left\n        }\n    }\n    return row[n-1];\n}`,

    breakdown: `• Bottom-up construction — Start from base cases (smallest subproblems) and iteratively fill the table toward the final answer. No recursion needed. The loop order ensures all dependencies are available when needed.\n\n• Table dimensions — Match the state parameters. 1D state (like amount in coin change) needs a 1D array. 2D state (like grid position i,j) needs a 2D array. The table size determines both time and space complexity.\n\n• Fill order determination — Examine the recurrence to find dependencies. If dp[i] depends on dp[i-1] and dp[i-2], fill left-to-right. If dp[i][j] depends on dp[i-1][j] and dp[i][j-1], fill top-to-bottom, left-to-right.\n\n• Space optimization — If the recurrence only references the previous row (or previous k entries), keep only those in memory. 2D problems often reduce to 1D. 1D problems may reduce to O(1) space. This is a common interview optimization step.\n\n• Tabulation vs Memoization tradeoffs — Tabulation: no recursion overhead, better cache performance, enables space optimization, computes ALL subproblems. Memoization: more intuitive, handles sparse state spaces better, naturally skips unreachable subproblems, easier to code initially.`,
    summary: "Tabulation builds solutions bottom-up by filling a table iteratively from base cases to the final answer. It avoids recursion overhead and stack overflow while enabling powerful space optimizations. The key skill is determining table dimensions, base cases, fill order, and identifying opportunities to reduce space."
  },

  {
    title: "Classic DP: Fibonacci & Knapsack",
    definition: "The Fibonacci sequence and the 0/1 Knapsack problem are two classic dynamic programming examples that illustrate fundamental DP patterns. Fibonacci demonstrates simple 1D state with linear dependencies, while Knapsack demonstrates 2D state with an include/exclude decision pattern that appears in countless optimization problems.",
    explanation: `The Fibonacci sequence (0, 1, 1, 2, 3, 5, 8, 13...) has the recurrence F(n) = F(n-1) + F(n-2) with base cases F(0)=0, F(1)=1. While simple, it perfectly demonstrates DP: naive recursion is O(2^n) due to exponential overlapping subproblems, while DP reduces it to O(n) with memoization/tabulation, and O(1) space with rolling variables.\n\nThe 0/1 Knapsack problem is far more representative of real DP challenges: given n items with weights and values, and a knapsack with capacity W, find the maximum value you can carry without exceeding the weight limit. Each item can be taken or left (0/1 — no fractions).\n\nThe Knapsack recurrence is: dp[i][w] = max(dp[i-1][w], dp[i-1][w-weight[i]] + value[i]). This means: for each item i at capacity w, the optimal value is the better of (1) not taking item i (dp[i-1][w]) or (2) taking item i (its value plus the best value achievable with remaining capacity). Base case: dp[0][w] = 0 for all w (no items means no value).\n\nThe state is 2D: which items have been considered (i) and remaining capacity (w). Time complexity is O(n*W), space is O(n*W) or O(W) with space optimization. Note this is pseudo-polynomial — it depends on the value of W, not the input size.\n\nThe Knapsack pattern (include/exclude decision) appears in many problems: subset sum, partition equal subset, rod cutting, and coin change. Recognizing this pattern is key to solving a wide class of optimization DP problems.`,

    code: `// 0/1 Knapsack Problem\nfunction knapsack(weights, values, capacity) {\n    const n = weights.length;\n    // dp[i][w] = max value using first i items with capacity w\n    const dp = Array.from({length: n + 1},\n        () => new Array(capacity + 1).fill(0));\n\n    for (let i = 1; i <= n; i++) {\n        for (let w = 0; w <= capacity; w++) {\n            // Option 1: Don't take item i\n            dp[i][w] = dp[i-1][w];\n\n            // Option 2: Take item i (if it fits)\n            if (weights[i-1] <= w) {\n                const takeIt = dp[i-1][w - weights[i-1]] + values[i-1];\n                dp[i][w] = Math.max(dp[i][w], takeIt);\n            }\n        }\n    }\n\n    return dp[n][capacity];\n}\n\n// Example: items with weight/value pairs, capacity = 7\nconst weights = [1, 3, 4, 5];\nconst values  = [1, 4, 5, 7];\nconsole.log(knapsack(weights, values, 7)); // 9 (items 2+3: weight 7, value 9)\n\n// Space-optimized Knapsack (1D array)\nfunction knapsackOptimized(weights, values, capacity) {\n    const dp = new Array(capacity + 1).fill(0);\n\n    for (let i = 0; i < weights.length; i++) {\n        // Traverse RIGHT to LEFT to avoid using same item twice\n        for (let w = capacity; w >= weights[i]; w--) {\n            dp[w] = Math.max(dp[w], dp[w - weights[i]] + values[i]);\n        }\n    }\n\n    return dp[capacity];\n}\n\nconsole.log(knapsackOptimized(weights, values, 7)); // 9\n\n// Reconstructing which items were selected\nfunction knapsackWithItems(weights, values, capacity) {\n    const n = weights.length;\n    const dp = Array.from({length: n + 1},\n        () => new Array(capacity + 1).fill(0));\n\n    for (let i = 1; i <= n; i++) {\n        for (let w = 0; w <= capacity; w++) {\n            dp[i][w] = dp[i-1][w];\n            if (weights[i-1] <= w) {\n                dp[i][w] = Math.max(dp[i][w],\n                    dp[i-1][w - weights[i-1]] + values[i-1]);\n            }\n        }\n    }\n\n    // Backtrack to find selected items\n    const selected = [];\n    let w = capacity;\n    for (let i = n; i > 0; i--) {\n        if (dp[i][w] !== dp[i-1][w]) {\n            selected.push(i - 1); // Item i was taken\n            w -= weights[i - 1];\n        }\n    }\n    return { maxValue: dp[n][capacity], items: selected };\n}`,

    breakdown: `• Fibonacci as DP introduction — Simple 1D state, linear recurrence (F[n] = F[n-1] + F[n-2]). Demonstrates the core DP transformation: exponential recursion O(2^n) becomes linear O(n) with caching. Space optimizes from O(n) to O(1) since only two previous values are needed.\n\n• Knapsack state definition — State is (item_index, remaining_capacity). The key insight: for each item, you make a binary choice (take/skip). The recurrence captures both options and takes the maximum.\n\n• Space optimization trick — Since dp[i] only depends on dp[i-1], use a single row and iterate capacity RIGHT-TO-LEFT. This prevents using an item multiple times (ensures we reference the previous row's values, not current row's updated values).\n\n• Pseudo-polynomial complexity — Knapsack is O(n*W) which looks polynomial but W is not the input size — it's the value of a number in the input. If W is exponential in the number of bits needed to represent it, the algorithm is exponential. This is why Knapsack is NP-complete.\n\n• Solution reconstruction — After filling the DP table, backtrack from dp[n][W] to find which items were included. If dp[i][w] differs from dp[i-1][w], item i was taken. This technique works for most DP problems to recover the actual solution, not just the optimal value.`,
    summary: "Fibonacci illustrates basic DP with a simple linear recurrence. The 0/1 Knapsack problem introduces the include/exclude pattern fundamental to optimization DP: for each item, compare taking vs. skipping it. These patterns form the foundation for solving subset sum, partition problems, and resource allocation challenges."
  },

  {
    title: "DP on Strings (LCS, Edit Distance)",
    definition: "String DP problems involve finding optimal alignments, subsequences, or transformations between strings. The two most important examples are Longest Common Subsequence (LCS) — finding the longest sequence of characters that appears in both strings in order — and Edit Distance (Levenshtein distance) — finding the minimum number of insert/delete/replace operations to transform one string into another.",
    explanation: `String DP problems are characterized by a 2D state space where the dimensions represent positions in the two input strings. The recurrence typically considers what happens at the current characters of both strings and makes decisions based on whether they match.\n\nLongest Common Subsequence (LCS): Given strings X and Y, find the longest subsequence common to both. A subsequence maintains relative order but needn't be contiguous. For example, LCS("ABCBDAB", "BDCAB") = "BCAB" (length 4). The recurrence: if X[i] == Y[j], then dp[i][j] = dp[i-1][j-1] + 1 (extend the LCS). Otherwise, dp[i][j] = max(dp[i-1][j], dp[i][j-1]) (skip a character from either string).\n\nEdit Distance (Levenshtein): Given strings s1 and s2, find the minimum operations (insert, delete, replace) to transform s1 into s2. For example, edit_distance("kitten", "sitting") = 3 (substitute k→s, substitute e→i, insert g). The recurrence: if s1[i] == s2[j], dp[i][j] = dp[i-1][j-1] (no operation needed). Otherwise, dp[i][j] = 1 + min(dp[i-1][j-1], dp[i-1][j], dp[i][j-1]) representing replace, delete, or insert respectively.\n\nBoth problems have O(m*n) time and space complexity where m and n are string lengths. Space can be optimized to O(min(m,n)) since each row only depends on the previous row. These algorithms are used in DNA sequence alignment (bioinformatics), spell checkers, diff tools (comparing files), and version control systems.\n\nThe key to string DP is thinking about the last characters: what happens at position (i,j)? Either the characters match (great, extend the solution) or they don't (try all possible operations and take the best one). This reasoning leads directly to the recurrence relation.`,

    code: `// Longest Common Subsequence (LCS)\nfunction lcs(text1, text2) {\n    const m = text1.length, n = text2.length;\n    // dp[i][j] = LCS length for text1[0..i-1] and text2[0..j-1]\n    const dp = Array.from({length: m + 1},\n        () => new Array(n + 1).fill(0));\n\n    for (let i = 1; i <= m; i++) {\n        for (let j = 1; j <= n; j++) {\n            if (text1[i-1] === text2[j-1]) {\n                dp[i][j] = dp[i-1][j-1] + 1; // Characters match\n            } else {\n                dp[i][j] = Math.max(dp[i-1][j], dp[i][j-1]);\n            }\n        }\n    }\n    return dp[m][n];\n}\n\nconsole.log(lcs("ABCBDAB", "BDCAB")); // 4 ("BCAB")\n\n// Edit Distance (Levenshtein Distance)\nfunction editDistance(word1, word2) {\n    const m = word1.length, n = word2.length;\n    // dp[i][j] = min operations to convert word1[0..i-1] to word2[0..j-1]\n    const dp = Array.from({length: m + 1},\n        () => new Array(n + 1).fill(0));\n\n    // Base cases: converting to/from empty string\n    for (let i = 0; i <= m; i++) dp[i][0] = i; // delete all\n    for (let j = 0; j <= n; j++) dp[0][j] = j; // insert all\n\n    for (let i = 1; i <= m; i++) {\n        for (let j = 1; j <= n; j++) {\n            if (word1[i-1] === word2[j-1]) {\n                dp[i][j] = dp[i-1][j-1]; // No operation needed\n            } else {\n                dp[i][j] = 1 + Math.min(\n                    dp[i-1][j-1], // Replace\n                    dp[i-1][j],   // Delete from word1\n                    dp[i][j-1]    // Insert into word1\n                );\n            }\n        }\n    }\n    return dp[m][n];\n}\n\nconsole.log(editDistance("kitten", "sitting")); // 3\nconsole.log(editDistance("sunday", "saturday")); // 3\n\n// Reconstruct LCS string (backtracking)\nfunction lcsString(text1, text2) {\n    const m = text1.length, n = text2.length;\n    const dp = Array.from({length: m + 1},\n        () => new Array(n + 1).fill(0));\n\n    for (let i = 1; i <= m; i++) {\n        for (let j = 1; j <= n; j++) {\n            if (text1[i-1] === text2[j-1]) dp[i][j] = dp[i-1][j-1] + 1;\n            else dp[i][j] = Math.max(dp[i-1][j], dp[i][j-1]);\n        }\n    }\n\n    // Backtrack to find the actual subsequence\n    let i = m, j = n, result = "";\n    while (i > 0 && j > 0) {\n        if (text1[i-1] === text2[j-1]) {\n            result = text1[i-1] + result;\n            i--; j--;\n        } else if (dp[i-1][j] > dp[i][j-1]) {\n            i--;\n        } else {\n            j--;\n        }\n    }\n    return result;\n}`,

    breakdown: `• 2D state space — Both LCS and Edit Distance use dp[i][j] where i and j represent positions in the two strings. The table is (m+1) x (n+1) to accommodate empty string base cases. This 2D state pattern is standard for comparing two sequences.\n\n• LCS recurrence logic — If characters match (text1[i-1] == text2[j-1]), they extend the LCS. If not, we try advancing in either string and take the better result. This captures all possible ways to align the two strings.\n\n• Edit Distance operations — The three operations map to table directions: replace (diagonal dp[i-1][j-1]), delete (up dp[i-1][j]), insert (left dp[i][j-1]). Each adds cost 1 except when characters already match (diagonal with cost 0).\n\n• Time/Space complexity — Both are O(m*n) time and space. Space can be optimized to O(min(m,n)) using two rows since each row only depends on the previous. For LCS string reconstruction, the full table must be kept.\n\n• Real-world applications — LCS: diff tools (showing file changes), DNA/protein sequence alignment, plagiarism detection. Edit Distance: spell correction (suggest words within edit distance 1-2), fuzzy string matching, auto-complete, and DNA mutation analysis.`,
    summary: "LCS and Edit Distance are canonical string DP problems with 2D state spaces. LCS finds the longest common subsequence by matching characters or skipping them. Edit Distance finds minimum transformations using insert, delete, and replace operations. Both run in O(m*n) time and are foundational in bioinformatics, text processing, and version control."
  }
];
