// Pre-written lessons for C# Module: Generics

export const csharpGenericsLessons = [
  {
    title: "What is Generics?",
    definition: "Generics in C# allow you to define type-safe classes, methods, and interfaces that work with any data type, specified at compile time through type parameters like <T>.",
    explanation: `Generics solve the problem of writing duplicate code for different types. Without generics, you'd need IntList, StringList, PlayerList — each with identical logic but different types. With generics, List<T> works for any type while maintaining full type safety.\n\nThe type parameter <T> is a placeholder that gets replaced with a real type when you use the class. List<int> replaces T with int everywhere. List<string> replaces T with string. The compiler checks all operations are valid for the specified type.\n\nUnlike using 'object' (which requires casting and loses type safety), generics preserve type information at compile time. If you have List<int>, the compiler won't let you accidentally add a string — the error is caught before the program even runs.\n\nGenerics also avoid boxing/unboxing overhead for value types. Storing an int in a List<object> boxes it (wraps in a heap object). List<int> stores ints directly without boxing, which is significantly faster for large collections.`,
    code: `// Generic class definition\nclass Inventory<T>\n{\n    private readonly List<T> _items = new();\n    public int Count => _items.Count;\n\n    public void Add(T item) => _items.Add(item);\n    public T Get(int index) => _items[index];\n    public bool Contains(T item) => _items.Contains(item);\n}\n\n// Using with different types — fully type-safe\nvar weapons = new Inventory<string>();\nweapons.Add("Sword");\nweapons.Add("Bow");\n// weapons.Add(42); // COMPILE ERROR! Type-safe!\n\nvar scores = new Inventory<int>();\nscores.Add(100);\nscores.Add(250);\n// string s = scores.Get(0); // COMPILE ERROR!\n\n// Generic method\nstatic T FindMax<T>(T[] items) where T : IComparable<T>\n{\n    T max = items[0];\n    foreach (T item in items)\n        if (item.CompareTo(max) > 0)\n            max = item;\n    return max;\n}\n\nint maxScore = FindMax(new[] { 10, 85, 42, 99, 7 });\nstring last = FindMax(new[] { "apple", "banana", "cherry" });`,
    breakdown: `\u2022 class Inventory<T> \u2014 T is the type parameter. When you create Inventory<string>, every T in the class becomes string.\n\n\u2022 private readonly List<T> _items \u2014 The internal storage uses the same type parameter. Type safety flows through the entire implementation.\n\n\u2022 weapons.Add(42) would fail \u2014 Compile-time error because Inventory<string> only accepts strings. This is the key benefit over using object.\n\n\u2022 where T : IComparable<T> \u2014 Constraint requiring T to be comparable. Without this, item.CompareTo() wouldn't compile because arbitrary T has no CompareTo method.\n\n\u2022 FindMax works with int[] and string[] \u2014 Because both int and string implement IComparable. The same algorithm works for any comparable type.`,
    summary: "Generics provide type-safe, reusable code that works with any data type. List<T> replaces type-specific lists. Constraints (where T : interface) enable calling specific methods on T. The compiler catches type mismatches at compile time, not runtime."
  },
  {
    title: "How Generics works",
    definition: "Generics work through type erasure at the IL level for reference types (shared code) and specialization for value types (unique code per type). The JIT compiler generates optimized machine code for each concrete type used.",
    explanation: `The .NET runtime handles generics through a combination of code sharing and specialization. For reference types (classes), all List<string>, List<Player>, List<Enemy> share the same compiled code — they're all just pointers internally.\n\nFor value types (int, double, struct), the JIT creates specialized code for each type. List<int> gets different machine code than List<double> because they have different sizes and operations. This ensures optimal performance without boxing.\n\nGeneric constraints (where T : IComparable) are checked at compile time. The compiler verifies that any type used as T actually implements the required interface. This catches errors early and enables the generic code to call methods defined by the constraint.\n\nAt the IL (Intermediate Language) level, generic types retain their type parameters — unlike Java's erasure, .NET generics are reified. You can reflect on them at runtime: typeof(List<int>) is different from typeof(List<string>).`,
    code: `// How the runtime handles generics\n// Reference types share implementation\nvar stringList = new List<string>(); // shares code with\nvar playerList = new List<Player>();  // all reference-type Lists\n\n// Value types get specialized code\nvar intList = new List<int>();     // unique JIT code for int\nvar doubleList = new List<double>(); // unique JIT code for double\n\n// Constraint enforcement at compile time\nclass SortedCollection<T> where T : IComparable<T>\n{\n    private readonly List<T> _items = new();\n\n    public void Insert(T item)\n    {\n        int index = _items.BinarySearch(item); // works because of constraint!\n        if (index < 0) index = ~index;\n        _items.Insert(index, item);\n    }\n}\n\n// Without constraint, BinarySearch wouldn't compile\n// because T wouldn't be guaranteed to be comparable\n\n// Runtime type checking with generics\nConsole.WriteLine(typeof(List<int>));    // System.Collections.Generic.List\`1[System.Int32]\nConsole.WriteLine(typeof(List<string>)); // System.Collections.Generic.List\`1[System.String]\n// They're different types at runtime! (reification)`,
    breakdown: `\u2022 Reference types share code \u2014 List<string> and List<Player> use the same JIT-compiled code because references are all the same size (pointer-sized).\n\n\u2022 Value types get specialized \u2014 List<int> needs different code than List<double> because they have different sizes and arithmetic operations.\n\n\u2022 BinarySearch works because of constraint \u2014 The where T : IComparable<T> constraint guarantees the elements can be compared, making binary search valid.\n\n\u2022 typeof(List<int>) differs from typeof(List<string>) \u2014 .NET reifies generics (keeps type info at runtime), unlike Java which erases them. You can reflect on generic types.\n\n\u2022 SortedCollection maintains order automatically \u2014 The Insert method uses BinarySearch to find the correct position, keeping elements sorted as they're added.`,
    summary: "Reference type generics share compiled code; value type generics get specialized versions for performance. .NET reifies generic types (preserved at runtime). Constraints are checked at compile time, ensuring type safety without runtime overhead."
  },
  {
    title: "Generics syntax & usage",
    definition: "Generic syntax uses angle brackets <T> for type parameters, 'where' clauses for constraints, and supports multiple type parameters. Common conventions: T (type), TKey/TValue (dictionaries), TResult (return types).",
    explanation: `Generic type parameters are declared in angle brackets after the type or method name. Constraints follow the 'where' keyword and specify what capabilities T must have.\n\nCommon constraints include: where T : class (must be reference type), where T : struct (must be value type), where T : new() (must have parameterless constructor), where T : IInterface (must implement interface), where T : BaseClass (must inherit from class).\n\nMultiple type parameters are separated by commas: Dictionary<TKey, TValue>. Each can have its own constraints. Constraints enable calling specific methods on T — without constraints, you can only use object methods (ToString, Equals, GetHashCode).\n\nGeneric methods can exist independently of generic classes. A non-generic class can have generic methods, and generic classes can have additional generic methods with different type parameters.`,
    code: `// Multiple type parameters\nclass Pair<TFirst, TSecond>\n{\n    public TFirst First { get; init; }\n    public TSecond Second { get; init; }\n    public void Deconstruct(out TFirst f, out TSecond s)\n    {\n        f = First; s = Second;\n    }\n}\n\nvar score = new Pair<string, int>\n    { First = "Alice", Second = 1500 };\nvar (name, points) = score;\n\n// Multiple constraints\nclass Repository<T> where T : class, IEntity, new()\n{\n    public T Create()\n    {\n        var entity = new T(); // possible due to new() constraint\n        entity.Id = Guid.NewGuid(); // possible due to IEntity\n        return entity;\n    }\n}\n\n// Generic method with type inference\nstatic TResult Transform<TSource, TResult>(\n    TSource input, Func<TSource, TResult> converter)\n    => converter(input);\n\n// Compiler infers types from arguments\nvar result = Transform(42, x => x.ToString());\n// TSource=int, TResult=string (inferred!)\n\n// Covariance and contravariance\nIEnumerable<object> items = new List<string>(); // covariant (out)\nAction<string> handler = (object o) => Console.WriteLine(o); // contravariant (in)`,
    breakdown: `\u2022 Pair<TFirst, TSecond> \u2014 Two type parameters for heterogeneous pairs. Each has its own type. Deconstruct enables tuple-like usage.\n\n\u2022 where T : class, IEntity, new() \u2014 Multiple constraints: must be reference type, must implement IEntity, must have parameterless constructor. All three required simultaneously.\n\n\u2022 new T() \u2014 Only possible with new() constraint. Without it, the compiler can't guarantee T has a parameterless constructor.\n\n\u2022 Type inference \u2014 Transform(42, x => x.ToString()) infers TSource=int from 42 and TResult=string from the lambda return type. No explicit type arguments needed.\n\n\u2022 Covariance (out) \u2014 IEnumerable<string> can be assigned to IEnumerable<object> because strings ARE objects. Only works for output positions.`,
    summary: "Multiple type parameters support complex generic types. Constraints can combine class/struct, interfaces, and new(). Type inference often eliminates explicit type arguments. Covariance/contravariance enable flexible generic interface assignments."
  },
  {
    title: "Practical examples of Generics",
    definition: "Generics are used extensively in real applications for collections (List<T>, Dictionary<TKey,TValue>), repositories, service abstractions, and algorithms that work across multiple data types.",
    explanation: `Generic collections (List<T>, Dictionary<TKey,TValue>, Queue<T>, Stack<T>) are the most common use of generics in everyday C#. The repository pattern uses generics to create reusable data access layers.\n\nGeneric service interfaces enable dependency injection patterns where the same service contract works for different entity types. IRepository<Player>, IRepository<Enemy>, IRepository<Item> all share the interface but operate on different data.\n\nAlgorithms like sorting, searching, and filtering are naturally generic — the logic doesn't change based on the data type, only the comparison operations do. This is why LINQ methods are all generic.\n\nEvent systems often use generics for type-safe event payloads: EventHandler<DamageEvent>, EventHandler<LevelUpEvent>. The handler knows exactly what data it receives without casting.`,
    code: `// === Generic Repository Pattern ===\ninterface IEntity { Guid Id { get; set; } }\n\nclass GameRepository<T> where T : IEntity\n{\n    private readonly Dictionary<Guid, T> _store = new();\n\n    public void Save(T entity) => _store[entity.Id] = entity;\n    public T? FindById(Guid id) => _store.GetValueOrDefault(id);\n    public IEnumerable<T> FindAll() => _store.Values;\n    public void Delete(Guid id) => _store.Remove(id);\n}\n\n// Same repository works for any entity type\nvar playerRepo = new GameRepository<Player>();\nvar itemRepo = new GameRepository<Item>();\nvar questRepo = new GameRepository<Quest>();\n\n// === Generic Event System ===\nclass EventBus\n{\n    private readonly Dictionary<Type, List<Delegate>> _handlers = new();\n\n    public void Subscribe<T>(Action<T> handler)\n    {\n        var type = typeof(T);\n        if (!_handlers.ContainsKey(type))\n            _handlers[type] = new();\n        _handlers[type].Add(handler);\n    }\n\n    public void Publish<T>(T eventData)\n    {\n        if (_handlers.TryGetValue(typeof(T), out var list))\n            foreach (var h in list)\n                ((Action<T>)h)(eventData);\n    }\n}\n\nrecord DamageEvent(string Target, int Amount);\nvar bus = new EventBus();\nbus.Subscribe<DamageEvent>(e => Console.WriteLine($"{e.Target} took {e.Amount} dmg"));\nbus.Publish(new DamageEvent("Goblin", 50));`,
    breakdown: `\u2022 GameRepository<T> where T : IEntity \u2014 One repository class works for Players, Items, Quests \u2014 any entity. The constraint ensures all entities have an Id for storage.\n\n\u2022 Three repos from one class \u2014 playerRepo, itemRepo, questRepo all get full type safety. No casting, no runtime errors from wrong types.\n\n\u2022 EventBus with generic Subscribe/Publish \u2014 Type-safe event system. Subscribe<DamageEvent> only receives DamageEvent instances. No downcasting needed.\n\n\u2022 typeof(T) as dictionary key \u2014 Each event type maps to its own handler list. The type system ensures type safety at publish time.\n\n\u2022 record DamageEvent \u2014 Records work great as event payloads \u2014 immutable, value equality, and concise syntax.`,
    summary: "The Repository pattern uses generics for reusable data access across entity types. Generic event systems provide type-safe publish/subscribe. Any algorithm independent of specific types (sorting, caching, mapping) benefits from generics."
  },
  {
    title: "Generics best practices",
    definition: "Best practices include using constraints to enforce capabilities, preferring generic collections over object-based ones, keeping type parameter counts low, and using meaningful type parameter names.",
    explanation: `Use constraints to make generic code useful — unconstrained T only has object methods. where T : IComparable<T> enables sorting. where T : new() enables creating instances.\n\nPrefer generic collections (List<T>) over non-generic ones (ArrayList). Non-generic collections store objects, requiring casting and boxing. Generic collections are type-safe and faster.\n\nKeep type parameter counts low (1-2 ideally, 3 maximum). More parameters make types hard to understand and use. If you need many, consider splitting into smaller generic types.\n\nUse meaningful names: T for single type parameters, TKey/TValue for dictionaries, TSource/TResult for transformations. Only use single-letter T when the meaning is obvious from context.`,
    code: `// DO: Use constraints to enable functionality\nstatic T Clamp<T>(T value, T min, T max) where T : IComparable<T>\n    => value.CompareTo(min) < 0 ? min\n     : value.CompareTo(max) > 0 ? max\n     : value;\n\nint clamped = Clamp(150, 0, 100); // 100\n\n// DO: Prefer generic collections\nList<int> scores = new();      // GOOD: type-safe, no boxing\n// ArrayList scores = new();   // BAD: stores object, requires casting\n\n// DO: Use meaningful type parameter names\ninterface IMapper<TSource, TDestination>\n{\n    TDestination Map(TSource source);\n}\n\n// DON'T: Too many type parameters\n// interface IService<T1, T2, T3, T4, T5> // unreadable!\n\n// DO: Consider generic interfaces over concrete implementations\ninterface ICache<TKey, TValue>\n{\n    TValue? Get(TKey key);\n    void Set(TKey key, TValue value, TimeSpan? expiry = null);\n}\n\n// Multiple implementations possible\nclass MemoryCache<TKey, TValue> : ICache<TKey, TValue> { /* ... */ }\nclass RedisCache<TKey, TValue> : ICache<TKey, TValue> { /* ... */ }`,
    breakdown: `\u2022 Clamp<T> with IComparable<T> \u2014 A single method clamps any comparable type (int, double, DateTime, etc.). The constraint makes CompareTo available.\n\n\u2022 List<int> vs ArrayList \u2014 Generic List is type-safe (compile-time errors for wrong types) and avoids boxing (value types stored directly). ArrayList stores objects requiring casting.\n\n\u2022 IMapper<TSource, TDestination> \u2014 Meaningful names make the interface self-documenting. You immediately know which type is input and which is output.\n\n\u2022 ICache<TKey, TValue> \u2014 Generic interface enables multiple implementations (memory, Redis, file) with the same contract. Swap implementations without changing consumers.\n\n\u2022 Limiting type parameters \u2014 More than 3 type parameters makes a type hard to use and understand. Split into smaller types if needed.`,
    summary: "Use constraints to make generic code useful. Prefer generic collections over object-based ones. Use meaningful type parameter names (TSource, TResult). Keep parameter counts low. Define generic interfaces for swappable implementations."
  }
];
